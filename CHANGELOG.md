# Changelog

## 1.16.2 (TBD)

### Features

- [FEATURE][all] **You can now opt in to sharing usage data, so we can find out where the wallet actually breaks.** It is **off by default** and does nothing until you turn it on. You are asked once, after creating or importing a wallet, and "Not now" is a real answer that is never re-asked; the toggle lives in Settings → General → "Help improve Wallet" and turning it off takes effect immediately, since the setting is re-checked before every single send. On Firefox there is a second switch — Firefox asks separately, at install and under about:addons, whether the extension may collect technical and interaction data — and both have to be on: declining Firefox's prompt stops collection regardless of the in-app toggle, and turning either one off is enough. Other browsers have no equivalent switch and are unaffected, which is a distinction the code makes explicitly rather than inferring from an API call failing. What goes out is deliberately tiny and has no free-text field at all: which of sixteen activities ran (opening, unlocking, creating, importing, recovering, returning, funding, sharing a receive address, sending, swapping, opening an Earn position, connecting a site, approving a site's transaction, rotating a guardian, handling a note, viewing activity), whether it started or ended, how it ended (completed / cancelled / errored), a broad error category from a fixed list of eight, how long it took, which screen you got as far as — from a fixed list of fourteen screen names, so that giving up on the amount field can be told apart from getting to the final review and deciding not to sign, which is the entire point of collecting any of this and was not answerable without it — the app version, and the platform. Alongside those sixteen activities, the wallet also reports whether the work it does *after* you have finished asking actually succeeded — and this is the half that was missing, because every one of those activities is reported as finished the moment a transaction is queued, which is before anything has been proved or submitted. A remote prover being down and failing every transaction for everyone would therefore have been invisible twice over: no usage event, because no activity was still open to fail, and no crash report, because the pipeline catches those errors and turns them into an on-screen failure rather than letting one reach a crash handler. Each such report names the kind of work (sending, receiving, swapping, earning, bridging, a guardian change, a site's transaction, proving, or one of our services being unreachable), whether it succeeded, how long it took, the same broad error category from the same list of eight, and the stage it got to — which is the difference between a prover being down and a node refusing the transaction, and is the single most useful thing a failure can say. Only some transactions can name that stage precisely: an ordinary send runs execute, prove and submit under one opaque stage, so it reports that stage honestly rather than guessing between them — the first cut of this folded it in with submission, which would have filed every prover outage on the commonest transaction type as a node problem, the exact conclusion the feature exists to prevent. Successes are reported as well as failures, because a count of failures with no denominator cannot distinguish a crisis from a rounding error. Nothing about the transaction itself travels with any of it: not the recipient, the amount, the asset, the site, or the endpoint. A proof that had to fall back from the delegated prover to the local one is reported as the success it was, marked as a fallback — those are invisible in every other signal, since the transaction lands and the only trace is someone who waited twice. These reports carry no activity id, deliberately: pairing one to the activity that started it would mean writing a telemetry value onto the transaction record, and that record is stored on disk, which would turn an in-memory value into a durable one and give up the property the whole design rests on. An address, an amount, a note ID or an error message has nowhere to go — the payload type has no field one could occupy, so a leak fails `yarn ts` rather than relying on review, and a build-time allowlist restates the eleven permitted keys independently. The same setting turns on crash reporting — which the consent prompt now names in as many words, rather than leaving a user to discover from the policy that agreeing meant stack traces too — sending a scrubbed error type and stack trace: addresses, amounts, long hex values, digit runs, credentials in URLs and any value stored under a name like `password` or `seed` are replaced before sending, scrubbing runs twice via independent paths, and a report containing anything that reads as a recovery phrase is discarded whole rather than partly redacted. There is **no persistent identifier of any kind** — no user, device, or install id, no advertising id, no cookie. Two random values are involved and neither is ever written down: one pairs an activity's start to its end, and one groups the activities of a single run of the app so that a visit can be read as a sequence rather than as unconnected fragments. The second renews itself after thirty minutes of inactivity and is gone for good when the app closes, so activities within one run belong together and nothing joins one run to the next — the bound is stated plainly in the policy rather than left to be inferred. That is the strongest privacy position available and it has one honest cost, stated plainly in the policy rather than buried: because nothing identifies you, we cannot delete "your" data on request, since there is no way to find it — everything is deleted automatically 90 days after collection instead. Two processors are involved, both EU-hosted and only while the setting is on: Aptabase (Germany) for usage events and Sentry (EU) for crashes, configured to store no IP addresses and forward nothing onward. The Aptabase side is written here rather than taken off the shelf — no vendor SDK is installed — because their own client does two things this design does not allow. It keeps a session identifier in device storage and reuses it on a four-hour timeout, which makes it outlive restarts and turns it into the durable identifier this design refuses to hold; the field instead carries the in-memory per-run value described above, which no restart can survive. And it fills in the operating system version, the locale and the device model — three fingerprinting signals the wallet does not collect and which their format does not require — so those stay empty, a test fails if any of the three ever appears in an outgoing message, and a second test fails if the telemetry code so much as reaches an API that could compute one. The fixed field list is re-applied by hand on the way into their envelope, because the properties bag they define is an open object where the type checker stops being able to catch a stray field on its own. Events go one per request rather than in batches of 25, since the extension's background worker can be killed at any moment and a buffer of queued events is a buffer that gets thrown away. The endpoint comes from the app key's own region, and a missing or malformed key stops sending without raising an error, so a build that was never configured stays inert. We do not describe any of this as anonymous: any server sees the IP address a request came from, and a claim that leans on a charitable reading is not one worth making. Three things were removed on the way in, each of which had been quietly worse than nothing: a dead analytics scaffold whose payload type was an unbounded `properties?: object` bag and which had already seeded every existing install with a permanent random user id (now deleted on upgrade); a second, broken log-upload path whose consent check failed *open* when no preference had been stored, and which only ever leaked nothing because its transport was an empty stub; and the `@segment/analytics-node` dependency, whose entire purpose is fanning user data out to other vendors. Separately, six of the app's entry pages were loading Inter and Nunito from Google's font servers, which disclosed your IP address and browser to a third party on every single launch, before any consent prompt had asked you anything — those fonts now ship inside the app, and nothing it needs to draw itself is fetched from anyone else. The privacy policy, the iOS privacy manifest, the Firefox data-collection declaration and the Play data-safety answers are all updated to match, and the promises above are held in place by automated assertions that fail the build rather than by anybody remembering: that the setting defaults to off against genuinely empty storage, that there is exactly one place each kind of data can leave from, that no persistent identifier can be minted or stored, that forbidden values cannot survive at the transport boundary in any encoding, and that no advertising identifier or cross-app tracking framework can enter the tree at any of the three levels a dependency can arrive by. Known limits are written down rather than implied: the crash scrubber is a filter and not a guarantee, and a retry that succeeds after an error is recorded only as the error, so the data overstates how often people get stuck.
- [FEATURE][all] Configurable spending limits. Settings > Spending limits sets a rolling 24-hour and/or 7-day cap per account and token; raising or removing a cap needs the same strict authentication as spending over one. A transfer that would cross a cap is stopped at review with the amount, the overage and when the allowance next frees up, and can be sent only after a one-time authentication that is bound to that exact transfer and cannot be replayed. Wallet sends, swaps, bridged sends, Earn deposits and dApp-requested transfers are all counted and all enforced at a single point. A dApp custom transaction is enforced on the net value the approval-time dry run says leaves the account, offsetting only assets that same request brought in; a request whose effects cannot be attributed to it is refused while a limit is set. Two transfers started at once cannot both spend the same remaining allowance. Limits and spend history are stored only on this installation and are removed when app data is reset.
- [FEATURE][all] Configurable spending limits. Settings > Spending limits sets one rolling 24-hour cap per account, in US dollars, covering every asset the wallet can price - an asset without a price, including MIDEN, is never limited. Raising or removing the cap needs the same strict authentication as spending over it. A transfer that would cross the cap is stopped at review with the dollar amount, the overage and when the allowance next frees up, and can be sent only after a one-time authentication that is bound to that exact transfer and cannot be replayed. Wallet sends, swaps, bridged sends, Earn deposits and dApp-requested transfers are all valued in dollars and enforced at a single point; a covered asset whose price cannot be checked right now offers the same one-time authentication on a wallet-owned transfer and is refused outright on a dApp request. A dApp custom transaction is enforced on the net dollar value the approval-time dry run says leaves the account, offsetting only assets that same request brought in; a request whose effects cannot be attributed to it is refused while a cap is set. Two transfers started at once cannot both spend the same remaining allowance. Limits and spend history are stored only on this installation and are removed when app data is reset.
- [FEATURE][all] Advanced Settings can export the current account as a CLI-compatible `.mac` file after password, passcode, or device-security confirmation. The flow warns that the file grants access to the account's funds and requires an explicit acknowledgement before saving or sharing it (#537).

### Changes

- [CHANGE][all] Send's review page is quieter: the address in body text, the network as a plain value, no fee note, "Expires" with Edit, and the reclaim reassurance as one caption under the card.
- [CHANGE][all] Send's first page is titled like the other tabs, its address field fits two lines on a small phone and Paste, Address Book and Scan stay on one row.
- [CHANGE][all] Explore is reorganised like an app store: featured app, categories, grouped lists and recents, with the tab bars' bouncy motion. Search moves to a header button as on Activity: it narrows the apps as you type and opens a typed or pasted URL.
- [CHANGE][all] Home's five actions and their flows share the account card palette: each tab's icon and its flow's accent are the same brand colour, and text in that colour uses a darker shade of it.
- [CHANGE][all] Text across the app uses one named type scale, so titles, labels, values and body copy match everywhere.
- [CHANGE][all] Settings sub-pages, the guardian explainer, Explore, Earn and token detail set their text through the named type scale.
- [CHANGE][all] The seed phrase backup and check, private-key and wallet-file import, password, wallet-ready, side-panel handoff, Forgot password and Reset required screens share the onboarding layout: 28px titles, text fields, grey cards and notices, the main action pinned at the bottom, and sentence-case labels.
- [CHANGE][all] Onboarding uses the shared layout and components: every step after Welcome has a back chevron beside its progress bar (the hardware back gesture does the same), steps slide in like pushed pages, recovery and guardian choices are radio cards, import types are rows, the seed phrase is entered in two columns, and the test-network notice asks you to tick all three facts before I understand opens.
- [CHANGE][all] Choosing a guardian (onboarding and Rotate Guardian) uses one self-contained card per operator: its logo on a brand tile, name, "Operated by · location" line and a "Current" or "Offline" tag inside the card, an orange ring and check for the choice, and arrow-key selection; Learn more and Use a custom URL are text actions, and Review rotation and Guardian settings share the same layout and logo tile.
- [CHANGE][all] Copying anything shows the same animated confirmation: the copy icon morphs to a check (or the label rolls to Copied).
- [CHANGE][mobile] The passcode keypad sits in thumb reach at the bottom of the screen, with larger keys and a Face ID key.
- [CHANGE][all] Receive fits on one screen with aligned QR, address, notice and actions.
- [CHANGE][all] The Home balance card keeps its plain brand account colour, with a clearer label, currency and change pill and a hairline above the address footer.
- [CHANGE][mobile] The passcode keypad sits in thumb reach at the bottom of the screen, with larger keys and a biometric key that shows Face ID or a fingerprint to match the device.
- [CHANGE][all] Transaction details close with the back button only; the extra X and Close button are gone.
- [CHANGE][all] OpenZeppelin's logo follows its brand kit: official colours and mark, on a white tile.
- [CHANGE][all] Authorized dApps pages use the shared Settings layout: the dApps switch is a row with its explanation under it, and each connected dApp is a section named after its site, with its origin, network, account and permissions in a grey detail card and a red Disconnect button in place of the unlabelled ✕. With no connected dApps, the page now says so instead of staying blank.
- [CHANGE][all] Settings preference and developer pages use the same layout: General settings, Language, Advanced settings, Edit Miden Faucet ID, Networks and Developer settings show their options as rows in grey groups, with the explanation for each switch under it. Tapping anywhere on a switch's row flips it. Language marks the current choice with a check, the account public key has a Copy action, and Save or Reset stay pinned at the bottom. Developer settings' reset to a fresh wallet is now a red destructive button.
- [CHANGE][all] Settings security pages share one layout: Keys, Reveal Private Key, Remove seed phrase, Verify seed phrase, Encrypted wallet file and Guardian Settings use the shared header, a 16px margin, grey list groups and section labels, and put their actions in a footer pinned to the bottom. Keys lists Reveal Private Key and Rotate Guardian as rows and gives Rotate device key its own section with a compact button, without the black rule. Remove seed phrase is a red destructive button beside Cancel.
- [CHANGE][all] Chips, badges and small actions share one `Pill` component (two sizes, one set of tones), so the network chip and the recipient step's Paste, Address Book, Scan and "Add to contacts?" pills are the same height, padding and type scale. Every tone carries a 1px border, so selecting a chip no longer resizes it.
- [CHANGE][all] Send flow: a recent recipient shows its network as a badge on the avatar instead of a chip on the second line, and the address appears once (an unnamed recipient used to show the same shortened address twice).
- [CHANGE][all] Contacts get their own avatar: a color and initials derived from the contact, in the address book, the send contact picker and recent recipients, instead of the same orange Miden image for everyone.
- [CHANGE][all] Send flow: "Add to contacts?" opens a sheet that shows the address in full as a card to confirm and asks only for a name. A `0x` contact also picks its destination network, which is saved with it and preselected when the contact is chosen later; saving a `0x` contact used to fail with "Invalid address". Duplicate addresses are now caught regardless of letter case.
- [CHANGE][all] Send flow: the recipient step is titled "Send to" and, like the add-contact sheet, shows a Network row for every valid address: Miden for a Miden address, and for a `0x` address the bridge networks as chips (shown as a fact and selected automatically while Sepolia is the only one).
- [CHANGE][all] Network chips wear their own network's soft tint from Bread's palette (warm peach for Miden, periwinkle for Ethereum networks), with the logo kept as-is; a selected chip deepens its border in the same color.
- [CHANGE][all] One search bar across the wallet (Explore, Activity, the token picker and the address book): a 44px pill (36px in a header) on the shared fill with no border, an orange ring while typing, a search glyph and left-aligned 16px text so iOS no longer zooms on focus. The unused SearchField and SearchAssetField are removed.
- [CHANGE][all] One page header (`PageHeader`) for pushed pages: back, title, actions and close in a single 52px row with a plain 24px back chevron in ink (flow pages included, no longer in the flow's accent), replacing the large header with its grey bar on Settings sub-pages, transaction details, pending notes, import account and Guardian rotation; flow pages (send, contacts, processing, receipt) move their title into the same row, so content starts ~50px higher. The back button, and the rows of the Language list, also work again on a page opened a second time while it was still sliding away (Settings → Address Book → New contact, or Settings → Language).
- [FEATURE][all] Address book: saved contacts and your own accounts in separate sections under one search, a New contact page that checks the address as it is entered (including against contacts already saved), and a page per contact with its full address, network and date added, a Send button that opens the send flow with the contact filled in, and Edit to rename (or change a `0x` contact's network) or delete it. Its lists sit in grey groups with dividers that start after the avatar, under sentence-case section labels, and New contact is a grey secondary button.
- [CHANGE][all] Token detail page uses the shared hero, buttons, sections and cards. Its Token info card shows a short, copyable contract id instead of the full id in bold, and a View on Midenscan action.
- [CHANGE][all] Filters and segmented choices across the app share the tab bar's raised bubble and bouncy switch.

- [CHANGE][ci] **The telemetry promises are now tested against a real request leaving a real wallet, and doing so found one of them slightly overstated.** Every existing telemetry test was blind to the one thing that matters most: the unit tests assert against a stubbed transport with made-up data, and every end-to-end suite builds with no analytics key at all — which switches telemetry off at compile time, so none of them had ever seen a single request leave. A new suite builds the extension with its endpoint pointed at a local server that records whatever it is sent, then drives a real import, a real opt-in and a real opt-out. It checks that nothing whatsoever is sent before the user agrees, that what is sent afterwards carries only the fields it is supposed to, and — against a wallet holding an actual recovery phrase, password and address — that none of those values appear in any byte sent, in any encoding they could be disguised in. It cannot ride an existing suite: the endpoint is fixed when the app is built, and this is the only configuration where agreeing to share data is safe to test, because the only place it can reach is the local recorder. It then caught the thing it was written to look for. Turning sharing off drops everything pending and stops the crash reporter at once, and nothing that starts afterwards is ever sent — but an activity that was *already under way* when the switch was flipped could still report that it was abandoned, because the background worker learns of the change through a separate write that nothing sequences against it. The escaping event is one of the fixed eleven activity names with a "cancelled" outcome and a duration, and carries nothing about the person or their money; the settings screen now waits for that write rather than firing it off, which closes the part of the window belonging to the switch itself. Closing the rest means routing the change through the same channel the events use, which is not done here — so the limit is written down instead, along with the two things a local recorder cannot prove: that the real vendor accepts the message, and that the manifest permits reaching them (it does not name either analytics host, and production works only because both vendors accept cross-origin requests). The suite runs on merge to `main` and on any change to the telemetry code, and each of its assertions was verified to fail when the guarantee it covers is deliberately removed.
- [FIX][all] A dApp transaction built from its own request bytes is verified again before you approve it. The check that shows what such a transaction really moves could not run for an ordinary account, so the approval sheet fell back to what the site itself declared, and an account with a spending limit refused every one of these transactions rather than counting it.
- [CHORE][all] The spending-limit E2E suite covers dApp custom transactions: one within the cap is approved and counted, one over it is refused until the wallet's own authentication clears it. The challenge sheet is addressed by its own testid, so the iOS assertion can no longer read a different drawer.
- [CHANGE][all] The onboarding protection step names the mechanism it will actually use: "Face ID set up" on iOS and "Biometric set up" on every other platform, instead of one combined label (#497).
- [CHORE][all] Store listing copy, screenshots and upload assets for the App Store, Google Play and the Chrome Web Store are generated from checked-in manifests by `yarn store-listing:*`, with a validator that enforces the documented per-store counts, dimensions and copy limits (#497).
- [CHANGE][all] The encrypted wallet file now carries accounts imported from a private key, so restoring it brings them back with their keys instead of silently leaving them behind. A file written by an older version still restores, and names how many imported accounts it had to omit before you confirm. An imported account whose key the wallet cannot read is named rather than quietly dropped from the backup.
- [CHANGE][all] Bottom tab bar: new Home, Explore and Activity glyphs, icons only at 32px with no labels (each tab keeps its name for assistive tech), and one light-grey pill that slides between tabs on the shared pill spring.
- [CHANGE][all] The Overview segmented bar's pill and segment widths animate together on the pill spring instead of a width snap under a tween.
- [CHANGE][all] Passcode screens: Nunito throughout (subtitle, keypad, links), ExtraBold keypad digits with a press-in scale, and the unlock screen no longer selectable on long press. The home Assets heading steps down from Black to ExtraBold, and the Accounts drawer gets an ExtraBold title with Nunito Bold 16px rows.
- [CHANGE][all] The token search box under the home Assets heading is removed: the asset list follows the heading directly.
- [CHANGE][mobile] The bottom tab bar is docked to the bottom edge instead of floating as a pill: full width with a hairline top rule, its background running under the home indicator. It slides away while a tab page is scrolled down and returns as soon as the scroll stops or reverses. The extension and desktop keep the floating pill.
- [CHANGE][all] The balance card itself opens the account options on tap (keyboard: Enter or Space); the pencil button in its corner is removed. Copying the address no longer risks opening the options.
- [CHANGE][all] Typography pass: tab page titles are Nunito ExtraBold with -0.5px tracking, the unlock title is ExtraBold, the segmented action bar label is Nunito, Settings section headings are ExtraBold and their rows Bold, and the first Settings section sits 12px below the title rule.
- [CHANGE][all] Send flow: every step shares one layout, so the back button, title and large input stay in the same place from recipient to amount to route. Confirm sits just above the docked tab bar on the recipient step and drops to the bottom of the screen once later steps hide the bar; a long pasted address no longer pushes the Address Book pill into it.
- [FEATURE][mobile] Send flow: a Paste pill on the recipient step fills the address from the native clipboard, with the same validation as a scanned QR code. Mobile only: it is the one platform where a clipboard read works without a permission prompt, and elsewhere the field takes an ordinary paste.
- [CHANGE][all] Send flow: the amount step is redesigned. The amount is the large input under the title, with its dollar value; one card below holds the token (available balance, Max) and the recipient with its network; a missing-MIDEN fee shortfall is a notice with a Receive link instead of a red amount.
- [CHANGE][all] Send flow: networks show as chips with their logo (Miden's orange brand mark from miden.xyz) instead of solid orange pills, and a 0x recipient picks its destination network from chips on the recipient step instead of a separate sheet.
- [CHANGE][all] Send flow: entering or pasting a recipient animates instead of jumping. The address field grows smoothly as it wraps, the action pills swap as one set (the current pills fade out, then the next set, such as "Add to contacts?", fades in), the network chip lands just after, and network options, errors and recents fade and expand in and out. Reduced motion turns the animation off.
- [CHANGE][mobile] Send flow: Confirm slides up with the keyboard and back down when it closes, and when the tab bar hides or shows, instead of jumping. Layout still snaps (animating the keyboard inset reflows every frame); the button is eased from where it was drawn with a transform. The amount step's fee notice and Max pill fade in and out.
- [CHANGE][mobile] Step flows (send, swap, bridge deposit, encrypted file) slide the previous step in from the left on back, mirroring the forward slide; back used to swap steps with no transition.
- [CHANGE][mobile] Full-screen pages slide in from the right and back out on back by default (Review, token detail, pending notes, earn, guardian and bridge pages, developer settings), matching Settings and history details. The in-progress transaction screen keeps its fade, since Review replaces itself with it.
- [CHANGE][all] Send Review is redesigned on the send flow's layout: back and title where the other steps have them, the amount as the large text with its dollar value, one compact card of details (the full recipient address, network as a logo chip, max fee, and expiration with an orange Edit link), and Send Payment pinned where Confirm sits.
- [CHANGE][all] Processing screen: on the shared flow layout (close top right, title, Hide/Done pinned at the bottom) in the flow's accent, so a send is blue and a swap, earn or claim takes its own color. The hero is a soft accent circle with a round-capped ring spinner, and the active step uses the same spinner; both spin as a GPU transform, so they stay smooth at high refresh rates while the transaction pipeline keeps the main thread busy. The step list is a compact card, and the hero no longer squashes into a pill on short screens.
- [CHANGE][all] Transaction receipts (send, swap, earn, bridge, claim, guardian) are on the shared flow layout: close top right, a Success title, the same animated check as Processing (it used to stretch into a pill), a tighter heading, the details as a compact card, and the CTAs pinned at the bottom. The recipient address is shown in full, not shortened, and the explorer link takes the flow's accent.
- [CHANGE][all] Send flow: highlights use Send's sky blue (the back arrow, chevrons, Max, the Receive link, the address caret, selected network chips and route cards) while Confirm keeps the brand orange; the back button sits on the #F9F9F9 nav button surface. New design tokens: an accent and a tint per home action (Send, Receive, Earn, Swap) and a nav button surface.
- [FIX][all] Send flow: after backing out of Review, the amount step's back button returns to the recipient step with the address still filled in. It used to close the flow, and reopening Send landed on the amount step again with no way back to the address.
- [CHANGE][all] A disabled primary button is a faded version of the brand color with white text, instead of beige with grey text, so it reads as the same button waiting to be enabled.
- [CHANGE][all] Screen header back and close buttons share the nav button surface (#F9F9F9 in light mode) with a grey glyph; back is no longer an outlined orange arrow.
- [FIX][ios] With a hardware keyboard attached (the simulator's Mac keyboard, or a Bluetooth keyboard), focusing a field no longer lifts the page by a full on-screen keyboard that is not showing; the layout clears only the visible accessory bar.
- [CHANGE][all] Design system tokens for direction B (page, fill, ink, muted, accent tint, status text colors) with a unit test that holds every text pair to 4.5:1; nothing uses them yet.
- [FIX][all] Muted text (subtitles, labels, placeholders) is darker in light mode, #6B6B6B instead of #ABABAB, so it is readable at 4.5:1 on white and on grey groups.
- [CHANGE][all] Transaction statuses (pending, confirmed, failed) use one shared badge: the word alone, no dot, in soft sage, sand and clay tones from the activity icon palette, readable on cards. Activity amounts use the same sage and clay.
- [CHANGE][ci] Pull request CI cancels old runs, uses smaller 2 vCPU runners for Docker E2E jobs, skips Earn and Guardian systems for unrelated changes while keeping their required checks, and moves metadata-only jobs to `ubuntu-slim`.
- [CHANGE][ci] Pull request CI drops the local-e2e fast-blocks runner, runs Guardian stress and fault.smoke only on main, moves Swap and Earn E2E to main (required gates stay green on PRs), splits lint/unit from mock Playwright behind the `Test` gate, and runs the guardian-fault policy checks as Jest.
- [CHANGE][ci] Coverage Check shards the Jest run three ways and merges before the 95% gate. Local E2E runs on 8 vCPU, records Playwright traces only on failure, and runs the node at 500ms blocks.

### Features

- [FEATURE][all] Guardian private-key export and seed-less import carry the existing Miden hot and EVM keys together as `hot:evm` (two separate 64-character lowercase hex scalars). Authenticated reveal opens a QR first, with separate text fields available. Import supports native/extension camera scanning, local QR image decoding, and manual entry; both scalars are validated before storage changes. The supplied EVM key is encrypted in the vault and restores its address without deriving a new key or seed. Seed-based recovery stays separate; the export does not contain a cold recovery key.
- [FEATURE][all] Settings now has an optional seed phrase removal flow. It removes the phrase and Guardian recovery keys from local storage after backup verification. Daily signing keys remain. Guardian recovery actions accept temporary seed input without saving the phrase again. Account creation after removal is not yet supported.
- [FEATURE][all] Guardian rotation and everyday key replacement are now offered on every Guardian account, including one imported from an everyday key only. When the wallet holds no local cold key, the action prompts for the seed phrase, derives the cold key against the account's on-chain cold signer, and keeps it in memory only for that one transaction.
- [FEATURE][all] Show an in-wallet notification when a newer wallet version is detected, with the update summary and the action needed to apply it.
- [FEATURE][all] Settings → About has a Support link to the Miden support site.

### Fixes

- [FIX][all] An explicitly chosen Guardian is never silently replaced when its availability probe turns offline, and an account already using a custom Guardian no longer opens the switch flow with another operator preselected (#1083).
- [FIX][all] Switching between Home's Send, Receive, Earn and Swap no longer jumps back to Overview.
- [FIX][all] Settings → Recovery phrase opens its own page (warning first, then the phrase) instead of an overlay the tab bar covered.
- [FIX][all] **Usage data described the app, not the person using it.** The first build to reach a real device reported, for a session in which the user did exactly one swap, an abandoned send and a completed receive-address share — and no swap. Three separate causes, all now fixed. The home screen renders Overview, Send, Receive, Earn and Swap as one carousel and mounts all five at once, so any flow begun when its screen mounted began on every launch for screens nobody had looked at; those now begin when the user is actually on the route. Every swipe release navigates, so crossing the carousel emitted a full, matched, entirely meaningless activity for each pane passed over; a route now has to hold still for 600ms before anything is reported, which a crossing never does and a real visit always does. And swap, earn, dApp approvals and guardian rotation had no instrumentation at all, so the things people actually do reported nothing while the screens they passed through reported everything. Separately, the value sent in the analytics provider's "session" field was the per-activity identifier, chosen because it is the least linkable thing that could go there — with the result that every session held exactly one activity, every session duration read 0s, and no session ever described a visit. It now carries an identifier for one run of the app: still random, still in memory only, still gone when the app closes, renewed after thirty minutes of inactivity, and never joinable to another run. The activity identifier moved alongside the other fields, where it still pairs a start with its end. The privacy policy states the new bound directly, in the same place it stated the old one.
- [FIX][all] A transient RPC failure during the mandatory pre-send sync returns the transaction to the queue with a cooldown instead of marking it Failed before anything was built or submitted. (#822)
- [CHORE][all] Development guidance now identifies the shared React bottom navigation as the active extension, mobile, and desktop implementation, so navigation changes no longer target the retired native overlay (#808).
- [FIX][e2e] Balance helpers honor requested token symbols, and stress conservation tracks only the faucet deployed by the run so unrelated assets cannot create false loss or mask real loss (#770).
- [FIX][all] **Keyboard focus no longer drops to the page when the Funding screen takes over the faucet card.** Activating the card from the keyboard replaced the button you had just pressed, so focus fell back to the start of the page. Focus now stays on the card while the Funding screen shows, and returns to the card's action if it ends without the card going away (#923).
- [FIX][all] **Funding a new wallet now shows its full lifecycle instead of going silent for ~30s.** The faucet API acks a mint within seconds, but the minted note only becomes visible after chain inclusion and a client sync (~30-60s); the "Fund your wallet" card used to flash success on the ack and vanish, leaving the Home screen looking idle until the funds appeared. The card itself is now the tap target (no separate CTA button) and walks through a full sequence: a "Funding" hero (animated hourglass tile, indeterminate progress bar, "Your funds will be available shortly") for the whole wait, then a green "Funds deposited - You have $X ready" success beat once the funds land, then a hand-off to the pending-notes card - which now holds back while the hero is on stage so it can't push the success state off-screen (it sorts first in the carousel). The wait survives remounts and app restarts via a per-account persisted request marker (so one account's wait never surfaces on another), counts only notes beyond the request-time baseline as arrival (a pre-existing claimable note can't fake an instant success), and falls back to the actionable card 3 minutes after the request went out if the mint never lands; the faucet request itself times out after 60s so a hung request can't hold the loading state forever. The pending-notes card's "See Now" button was likewise folded into a whole-card tap. `PromptCard` gained an icon slot, a filled pill CTA, a corner dismiss that hides while a request is in flight, press-in feedback on tappable cards, and a reusable "hero" takeover mode; the Home "Assets" heading now uses the heading font, and the onboarding confirmation screen's typography was polished (bold Nunito heading, Nunito body copy, pill-styled daily-reminder note - fixing a stray `font-medium` attribute that was never applied).
- [FIX][all] **A faucet request that times out after it was sent no longer offers a retry that could mint twice, and one abandoned before it was sent no longer leaves Funding stuck.** The wallet now records that the token request is going out before sending it, and does not send it if that record cannot be saved. A timeout, a dropped connection, a server error or an unreadable reply after that point is an unknown outcome, since the faucet may already be minting, so the card keeps waiting for the funds or its 3-minute fallback instead of offering Fund again. A request abandoned before that point (an extension popup closed during the proof of work, an app killed) sent nothing, so its Funding screen ends once that request's own 60s timeout has passed instead of holding for 3 minutes. Until then every open surface waits, since the popup, side panel and tabs cannot see each other's requests. A surface that was already open when another one sent a request still offers Fund, but tapping it waits for that request instead of sending a second one (#919, #922).
- [FIX][all] **Dismissing pending notes no longer brings the card back for notes dismissed on another account, or in another extension surface.** A dismiss covered only the notes in view and replaced the stored list, which is kept for the whole wallet, so dismissing on one account dropped the ids dismissed on another, and in the extension the popup could drop ids the side panel had just stored. The dismissed ids are now merged, and the oldest are dropped once 200 are kept (#941).
- [FIX][extension] **The popup, side panel and tabs no longer overwrite each other's prompt statuses.** Dismissing or completing a prompt in one surface while another completed a different one, or set an account's faucet status, within one storage round trip lost one of the two changes: each surface applied its change to the record as it had read it. Every surface now takes the same turn to read and write that record (#937).
- [FIX][extension] **Two extension surfaces can no longer both send a faucet request for one account.** The popup, side panel and tabs now take turns checking and updating an account's faucet request record, so one that finds no request running cannot be overtaken by another surface's request before it records its own, and a surface ending a request it waited on can no longer delete a newer request's record another surface just saved (#935).
- [FIX][all] **Completing or dismissing Fund on one account no longer hides it on your other accounts.** The faucet prompt's status is now kept per account instead of once for the whole wallet, so a new or unfunded account is offered Fund even after another account was funded. A status saved by an earlier version is ignored, so an unfunded account may be offered Fund once more (#921).
- [FIX][extension] **The extension no longer treats the note list saved by an earlier session as live.** The service worker now stamps the note list it writes with its last successful sync, and the popup counts a list as live only once it sees a new successful sync for the current account after it opened. Until then the Fund card waits instead of taking its baseline from stale notes, and new-note toasts wait for the first live sync, as on mobile and desktop (#920).
- [FIX][ci] The dependency-integrity gate that runs before every production build no longer refuses a correct `node_modules` when the build itself runs with `NODE_ENV=production`. `yarn check --integrity` also compares the flags the install was made with, so a job that installs without that variable and builds with it was rejected; the check now verifies the whole dependency tree whatever the environment, which is what it is there to do. The v1.16.1 release's Chrome extension build failed on this.
- [FIX][all] Show the missing EVM key error when an imported key pair is incomplete. Update tests for seed phrase removal, key-pair import, and recovery signing.
- [FIX][all] Seed phrase removal now deletes and verifies the recovery secret stored under its commitment. Recovery actions exclude the seed input wait from transaction expiry.
- [FIX][all] Importing a wallet from an everyday (hot) key no longer fails on a fresh install. The key was parsed before any client had loaded the SDK's WASM module, so the parse threw inside the SDK and was reported as an invalid paste; the import now loads the module first.
- [FIX][all] Faucet requests in Activity use an icon that matches the other transaction types.
- [FIX][all] Activity's empty state sits right under the filters instead of below a large gap.

### Changes

- [CHANGE][extension] **Speculative pre-proving is removed.** The send form no longer asks the service worker to execute and prove a send before Confirm, and the `MIDEN_USE_SPECULATIVE_PROVING` flag, speculation manager, send-path cache check and speculate/invalidate messages are gone; on the default build the cache was already unreachable, and a guardian account's send cannot be proven before Confirm (#717).
- [CHANGE][all] **E2E suites can now be run against real infrastructure, and the live Epoch bridge-out spec runs again.** `yarn e2e:real --suite <name>` drives the bridge suites against the real hosted Epoch allocator/solver, the real AggLayer bridge and real Sepolia, and the in-protocol DEX swap suite against public Miden testnet instead of the local node it uses on a PR. Every external dependency is probed before anything is built - Miden RPC and faucet, Epoch health and a live quote from the solver, the Sepolia chain id and the three contracts the wallet hardcodes, the hosted guardian, and a supplied Sepolia key's gas - so a service that has stopped answering costs seconds rather than a quarter of an hour. The gasless relayer and the DEX quote service are reported alongside them but are advisory: no suite asks them anything, so neither can fail a run. `bridge-out-epoch.spec.ts` is no longer `describe.skip`: it is gated on `E2E_REAL_EPOCH`, which the runner sets. The `NO_QUOTE_AVAILABLE` refusal that disabled it on 2026-08-12 no longer reproduces - the allocator prices an intent from its mandate, whose `tokenIn` is the zero address for a Miden-sourced leg, so an arbitrary Miden faucet quotes the same as a registry one (#627).
- [CHANGE][all] **Test-token funding now mints only native MIDEN.** The Fund action no longer also mints IMIDEN from the forkchoice faucet — the two-source funding is removed, which also retires 1.15.20's per-source retry bookkeeping (there is no second source left to double-mint). One faucet request runs per account at a time no matter how often the card is tapped or remounted, bounded by a 60s timeout that aborts the underlying work, and a failed request shows the faucet's actual error message on the card. The two funding drawers earlier iterations had left unwired (`WalletFundingDrawer`, `FundWalletDrawer`) are deleted along with their orphaned translation keys. The network-discovered native asset now also appears first in the swap token list ahead of the existing DEX test assets.
- [CHANGE][all] Removed about 20 unused UI components (no visible change).
- [CHANGE][all] Animations across the wallet now follow the system Reduce Motion setting, and the design system's motion presets (fade, reveal, pop, sheet, page, press, indicator, shimmer) are available from `lib/animation`.
- [CHANGE][all] Buttons and toggles press with the design system's press motion and respond instantly when Reduce Motion is on.
- [CHANGE][all] Buttons follow the new design system: a 52px pill with a larger label, secondary and destructive buttons on the neutral fill, a 36px compact size, and a spinner that keeps the button's width while it loads.
- [CHANGE][all] Tab root headers (Activity, Explore) drop the grey rule under the title, and their header actions become bare icon buttons instead of grey circles; sheet titles are smaller and left-aligned, sheets get a bigger top radius and a smaller handle, and a sheet now opens and closes instantly under reduced motion.
- [CHANGE][all] Lists share one set of components: a grey 16px group, 64px rows (56px without a second line) with the title over a muted subtitle and a trailing value, check or chevron, and dividers that start after the row's avatar or icon; section labels are 13px bold and sentence case. The address book is the first screen on them.
- [CHANGE][all] Settings: each section is a sentence-case label over a grey group of rows with dividers and a chevron (Language shows its current value beside it), in place of the round section icons, large headings and bare rows; the Networks page lists networks the same way with a check on the active one.
- [CHANGE][all] Settings sections get their coloured icons back: each group heading again shows its round icon (security, preferences, about, developer) beside a larger title, over the grey row groups.
- [CHANGE][all] Send flow: the contact picker is titled Address Book and opens with a search over names and addresses, then your accounts and your contacts in separate sections. Contacts show their network (Miden, or a `0x` contact's destination such as Sepolia) and your accounts show Public or Private beside the shortened address, in place of the "External" label.
- [CHANGE][all] One `EmptyState` for every "nothing here" message: a `fill` card with a 56px icon circle, title and optional body, and an optional secondary action. Contacts' empty list, the pending-notes summary, the Activity list and the QR scanner's no-camera state now share it.
- [CHANGE][all] One `Skeleton` loading block for the wallet: the send review "you receive" row, the bridge route fee, the amount field, the EVM bridge deposit review and the home balance card now share it instead of five separate ad-hoc `animate-pulse` divs.
- [CHANGE][all] Detail cards (send review, the transaction receipt, a contact's address) share one look: a warm fill with hairline dividers between rows, and a row's inline action (Edit, Copy) is always a readable orange instead of sometimes taking the flow's own color, which was too faint to read as text.
- [CHANGE][all] History (transaction details, swap details, bridge claim, the failure/cancelled card) moves onto the same compact detail-card look as the rest of the wallet, kept under a small pill title where one is shown; value text no longer carries its own stray styling, and the failure card's message and its "Show full error" toggle read as one card body instead of two.
- [CHANGE][all] Send review, the transaction receipt and a contact's page share one centered layout: an avatar or status circle, then the amount or outcome, then a muted line; the receipt's outcome heading steps down to match the rest of the wallet's type scale.
- [CHANGE][all] One text field (`TextField`) for a `muted` label, a 52px pill or a 16px-radius multi-line box on `fill`, and an error that rings the field and announces itself to assistive tech; the new-contact page's address and name fields and the send flow's add-contact sheet now use it, with Paste and Scan now sitting inside the address field instead of below it.
- [CHANGE][all] `Pill` moves onto the design system's tokens (neutral on `fill`, selected in `accent-tint`, status pills as a dot and a word), replacing the app's Badge, the transaction history status pill, the seed-word chip and the Activity filter chips; onboarding's "Default" tag moves onto it too. The retired `Badge` and `Chip` components are removed.
- [CHANGE][all] A new `Avatar` component (round, 24/36/40/88px, image/initials/icon with an optional corner badge) backs the contact avatar and the token logo; a token's default fallback and its four known logos, and a couple of blue placeholder circles in the send amount step, move onto it too. The token detail page's large logo grows from 72px to the design system's 88px hero size to match.
- [CHANGE][all] A new `CopyButton` component (accent "Copy"/"Copied" text, or an icon swap via a render function, with an aria-live label so the state is announced) writes to the clipboard through `@capacitor/clipboard`, which has its own web implementation, so the same call works on desktop, the extension and mobile with no platform branch. The token detail page's contract-address copy button moves onto it, gaining "Copied" feedback it previously had none of. The dApp browser's "Copy link" action moves onto the same `@capacitor/clipboard` call (not the `CopyButton` component — the sheet closes immediately on tap, so there's no row left to show feedback in), replacing a `navigator.clipboard` call whose own comment noted only WKWebView guaranteed it.
- [FIX][all] Buttons: CTAs across send, swap, earn, contacts, address book, unlock's hardware-retry screen and the transaction/review/receipt flows no longer carry stray restyling (`rounded-full text-base font-semibold`, hand-painted fills, custom heights, inline font-size/padding styles) that fought the canonical Button's 52px/19px anatomy; small row and in-card actions (claim, restore hidden transfers, the empty-state secondary action, the seed-phrase show/copy row, the guardian-endpoint prompt) use the 36px `sm` size instead of one-off height and text-size overrides; History's cancel button is the real `Destructive` variant instead of a Primary painted red. A handful of non-CTA button shapes (swipe-action rows, a couple of legacy popups) are deliberately unchanged — they're a different pattern the design system doesn't define yet, not a missed override.
- [CHANGE][all] The dApp confirm page, the alert/confirmation modals, hot-key rotation, editing the Miden faucet id, importing a private key and restoring an encrypted wallet file all move their submit/cancel actions onto the canonical `Button`, replacing the atoms `FormSubmitButton` and `FormSecondaryButton` (both removed) and the stray padding, font-size and border overrides they carried.
- [CHANGE][all] Every loading indicator (the confirm page, page-level Suspense fallbacks, hot-key rotation, OpenSidePanel and onboarding's loading states, history's list and detail loading states, the earn withdraw status page, an EVM bridge deposit, exporting an encrypted wallet file) now renders the canonical `Spinner`, always the accent orange (a devnet build's spinner used to be slate, from the same build-specific palette as its buttons), replacing the atoms `ActivitySpinner`, `Spinner` and `CircularProgress` (all removed). The confirm page, page-level Suspense fallbacks, hot-key rotation, OpenSidePanel and onboarding's loading states sit closer to their text now too — the canonical `Spinner` carries no built-in margin, where `CircularProgress` had a fixed 20px; the list/detail loading placeholders keep their prior position via an explicit wrapper.
- [CHANGE][all] Token detail, the recovery phrase and encrypted-wallet-file screens move onto `PageHeader`, dropping the encrypted-file screen's stray divider under the title.
- [CHANGE][all] Reset-required, forgot-password and the recovery-seed prompt move onto `PageHeader`, and the now-unused `NavigationHeader` is removed.
- [CHANGE][all] The bridge deposit screens and developer settings move onto `PageHeader`.
- [CHANGE][all] The Earn vault, position and withdraw-review/status headers move onto `PageHeader`, gaining haptic feedback on back that the old hand-rolled headers never wired up; the now-unused `ScreenHeader` is removed.
- [CHANGE][all] A new `IconButton` component (a bare 24px ink glyph in a 44px hit area, or a 32/36px circle on `fill` with a `muted` glyph) backs the page header's back/close, the earn flow's back buttons, the drawer sheet's close, and the tab header's action icons, replacing several hand-rolled round buttons that had each drifted slightly (the drawer close was 24px on a flat grey fill; earn's back buttons were 40px with their own hover colors).
- [CHANGE][all] Confirmations and alerts (delete a contact, disconnect a dApp, decline a transfer, reset the wallet, error alerts) open as bottom sheets instead of centered pop-ups: the title, one sentence, the action over a Cancel button. They are announced as alert dialogs, focus Cancel first, close on Escape or Back, and no longer dismiss on a tap outside, and they sit above any open sheet.
- [CHANGE][all] The old pop-up modal and its styles are removed now that confirmations are sheets, and with reduced motion on, the extension no longer animates the app shrinking behind an open sheet: it moves there at once.
- [CHANGE][all] Confirmations name their action instead of "OK" and show it in the destructive style: Delete (a contact), Disconnect (a dApp), Decline (a transfer), Reset Wallet (developer settings) and Reset Extension.
- [CHANGE][all] Copying a connected app's account id, the receive screen's address, or the balance card's account label now shows a checkmark or "Copied" for a moment, instead of copying with no sign anything happened.
- [CHANGE][all] Addresses and transaction hashes shown in transaction history are now small tappable pills with a copy icon and "copied" feedback, in place of plain text that gave no indication it could be tapped at all.
- [CHANGE][all] Page motion comes from one place: a page that slides in and the page it covers move on the design system's `page` motion, a page that fades in uses the shared fade (a touch slower in the extension), and the steps inside a flow (send, swap, wallet file export, bridge deposit, onboarding) swap on the same curve as a page slide, a little quicker in onboarding. With reduced motion turned on, onboarding steps now change instantly instead of still fading.
- [CHANGE][all] The Processing screen and the success receipt settle on the design system's 64px hero status circle (down from 96px), and the EVM bridge-deposit and Smart Withdraw processing screens move onto the same hero instead of their own copy of it.
- [CHANGE][all] Swap review and the EVM bridge deposit review move their rows onto the shared detail card, with the amount(s) as a hero value instead of a bespoke 40px block; Guardian Settings' connection details (provider, endpoint, region, last sync) move onto the same detail card, and the new-contact page's avatar moves onto the same centered hero as the rest of the wallet.
- [CHANGE][all] Activity and Explore cards use one shared filled card, without borders.
- [FIX][all] Home/Explore/top-bar consistency pass: the prompt card and Explore app tiles use the 16px design-system radius, the segmented action bar's segments and pill are fully round instead of a fixed 22px corner, Activity's icon tiles are round instead of square, and the last raw hex colors on these screens (the balance delta pill, the balance card's divider rule, Activity's timestamp, the Explore tile border, the Accounts drawer's hover fill, and the bottom nav's notification dot) now point at design tokens. The Accounts drawer's Settings/Import Account rows no longer carry two conflicting border-radius classes, and Home's bottom scroll padding now matches Explore's.
- [FIX][all] Text fields show their whole focus and error outline; on the New contact page it was cut off at the left and right edges.
- [CHANGE][all] The last grey surfaces and text colors from before the design system move onto its tokens: inputs, cards, secondary buttons and chips sit on the same warm fill (a pressed or hovered one on its darker step), and headings and body text use one ink color instead of two slightly different greys.
- [CHANGE][all] The earn deposit amount and review screens use the shared page header (a 20px title beside a plain back chevron, with haptic feedback on back, and the "asset on network" label as a pill), the QR scanner shows a denied camera permission as the same empty-state card as a missing camera, and the tab screens fade in on the shared fade.
- [CHANGE][all] Bottom tab bar: same size and spacing as before, 24px icons in ink (muted when inactive), and the active tab's pill is a raised white bubble with a soft shadow that sinks when pressed. It slides between tabs on a bouncier spring with one small overshoot, the new tab's icon pops, a pressed tab dips, and every real tab switch buzzes once. Reduced motion moves the pill instantly with no pop or press scale.
- [CHANGE][all] Overview action bar: no grey band any more, the row sits on the page under a hairline rule, and the active pill is a raised white bubble with a soft shadow that sinks when pressed. The pill slides on the same bouncy spring as the bottom bar, the new segment's icon pops, a pressed segment dips, and swiping between the home pages now buzzes once per page change, like a tap on the bar.
- [CHANGE][all] The theme picker and the Developer settings pickers use the shared segmented control, and the old tab picker is removed.
- [CHANGE][all] The test network is named on a corner ribbon, a solid brand-colour sash with white TESTNET (or DEVNET, LOCALNET) lettering across the bottom tab bar's lower-right corner, instead of a banner across the top of every screen; nothing shows on mainnet. It sits over the bar without changing its size or taking space from the tabs, and tapping it opens the same explanation sheet. The banner is kept wherever the network matters for what you sign: the dApp confirmation window, and the seven screens that commit value (send review, swap review, bridge deposit and its EVM review, Earn deposit and withdraw review, and guardian rotation review). Two of those render inside the tab layout but hide the tab bar, so the ribbon would not have reached them either.

## 1.16.1 (2026-09-15)

### Changes

- [CHANGE][all] Upgrade `@miden-sdk/miden-sdk` to 0.16.1, which moves `miden-client` to 0.16.1, and rebuild the Android and iOS native provers against `miden-client` 0.16.1 so they stay in lockstep with the SDK.
- [CHANGE][mobile][desktop] The iOS, Android, desktop and Play Store app icons use the brand kit's Bread Logo exactly: the loaf mark sits slightly larger in its tile, with the kit's soft drop shadow. The Chrome extension icons are unchanged and still fill the toolbar frame.
- [CHANGE][mobile] On mobile and desktop the pending transfers list renders the last known list for the account immediately, instead of staying empty for several seconds while the first note read waits for the wallet lock. The list is stored per account and network endpoint, in a fixed newest-first order so a refresh never reshuffles the cards, and is replaced by the live read as soon as it lands. An entry that no live read has confirmed is shown but can never start a claim, and an account whose notes were all claimed stores an empty list, so claimed notes do not reappear on the next launch.
- [CHANGE][all] Activity redesign: every transaction row and pending transfer is its own bordered card with no divider lines. A pending transfer folds open from its header to show From, Amount and Received on, with Decline and Accept Transfer always visible as the card's bottom edge; a claimed transfer opens the same way with a Details button. The Pending filter chip lists only unclaimed transfers, with an Accept All button pinned to the bottom edge in place of the tab navbar that queues one consume per faucet, native asset first, and marks every card as claiming on tap on all platforms. Search moved into the header behind a search button and opens in place of the title, so the list does not move. The pending-claims carousel is removed, and under the Pending filter a Hidden transfers row counts declined transfers and restores them all. Bottom-tab changes swap instantly and each visited tab keeps its scroll position and state. Settings and transaction details slide in from the right over the page beneath, which stays mounted so going back restores it, and a slide page that pops back onto another slide page slides out instead of staying parked under it. Activity rows are Framer Motion layout elements: when a filter or search removes rows, the remaining rows and date groups slide into place like a native list, with reduced motion collapsing the move to an instant swap.

### Features

- [FEAT][all] The popup, full-page, side-panel, mobile and desktop wallet screens and the dApp confirmation popup show a Bread-branded banner naming the network the wallet is on (it follows Developer Settings overrides and hides on mainnet), with a sheet that explains the test environment. Onboarding shows a network notice before a wallet is created or restored; Receive, the EVM wallet connect step and the bridge amount form warn to use test funds only; and a shared address or QR image names the network (#875).
- [FEATURE][all] Add Settings to the bottom navigation, alongside Home, Explore and Activity (Explore remains hidden in the extension). Settings subpages keep their full-screen layout and back navigation, and returning restores the list's scroll position. Remove duplicate Settings shortcuts from tab headers, use a pencil for balance-card account options, localize tab labels, and fit four destinations on narrow screens. (#802)
- [FEATURE][all] Replace the Bread logo with the new loaf mark across every surface: the in-app logo (onboarding, unlock, network banner, receive QR), the brand SVGs, the extension toolbar and notification icons, the iOS and Android app icons and splash screens, and the desktop (Tauri) icon set.

### Fixes

- [FIX][all] Fix the Epoch (Fast) EVM→Miden deposit quote, which the allocator refused with "A quote isn't available for this intent": reverse-quote from the typed Miden output like the Epoch integration example, send only the SDK's canonical EVM→Miden witness fields, deliver into the example's testnet faucet, show the quoted EVM spend on review, and log the intent alongside a failed quote.
- [FEATURE][all] The AggLayer (Slow) route bridges any asset in both directions: the send flow no longer restricts Slow to the dedicated bridge faucet and the B2AGG note carries the selected token's faucet, and the EVM deposit screen offers Slow for USDC (ERC-20 approve, then `bridgeAsset`) as well as ETH. The Miden bridge account id is updated to `0x3b66e20b5088f25133b69216484652`.
- [FIX][all] Point AggLayer (Slow) EVM→Miden deposits at Miden Testnet rollup 86, the rollupID the AggLayer now assigns to Miden, instead of the retired rollup 78.
- [FIX][all] Show the token symbol, not the Sepolia USDC contract address, on an Epoch EVM→Miden deposit's activity row and detail hero: the pending bridge-in takes its source symbol from the tracking row instead of the allocator's token name, the received row keeps its own output symbol, and a stored symbol that is an EVM address is ignored on display.
- [FIX][all] Poll every pending bridge transaction (Epoch and AggLayer, both directions) from the app root every 8 seconds while the wallet is unlocked, instead of only while the Home, Explore or Activity page is open. An Epoch deposit started from the bridge screen is now tracked to settlement whichever screen is showing.
- [FIX][all] Update the IMIDEN, IETH, IBTC and IUSDT swap faucet IDs for Miden testnet 0.16. All four tokens use 8 decimals.
- [FIX][mobile] Opening search on Activity or Explore no longer jerks the page on iOS: the bottom navbar fades out over the keyboard slide instead of vanishing in one frame, and the keyboard inset moves the layout once rather than animating the body padding through every frame of the slide. The Settings tab also renders again after the bottom-nav merge; its pane was missing from the tab shell's render order.
- [FIX][e2e] Earn deposit, Earn withdrawal, faucet mint/balance and multi-account Chrome journeys now cover Guardian accounts as well as the cheaper offchain account shape. The Earn gate starts and checks a local Guardian before running the co-signed legs (#769).
- [FIX][all] The Guardian picker no longer lets an offline operator be chosen. A card marked Offline is disabled, a default or already-picked card that a later probe finds offline hands the selection to the first online operator (or to nothing in the rotation flow, so the user picks the replacement deliberately), and Continue is disabled while there is no online operator to submit. Before, the card said Offline but still took the tap, and account creation against it failed only after the seed backup and password steps.
- [FIX][all] An open wallet no longer leaks an IndexedDB connection and a WASM client on every consumability read. The read built a throwaway client each lap (every sync alarm on the extension, every 5 s claimable-notes poll on mobile and desktop) and relied on the SDK's `terminate()`, which releases nothing for an in-realm client; after two idle days the extension renderer died of out-of-memory. Each realm now keeps one reader and rebuilds it when the client is replaced or the RPC endpoint changes; the guardian swap request build, which leaked a client per swap the same way, uses that reader too.
- [FIX][all] A signed write (send, claim, swap, account creation) no longer strands a WASM client and an IndexedDB connection: each realm keeps one `MidenClientInterface` for every read and write (alongside the reader of the entry above), and installs its signer once instead of rebuilding the client to carry it (#878).
- [CHORE][all] Every production build now refuses a `node_modules` that does not match `yarn.lock` (`yarn check --integrity`, 30 ms), so a stale checkout can no longer bundle the wrong SDK.
- [FIX][all] The send screen waits for balances before warning about missing MIDEN for fees and clears that warning when funds arrive. Clearing an amount keeps the input red and Confirm disabled without showing “Invalid amount”.
- [FIX][ci] Every ubuntu job now drops Google's Chrome apt repository before touching apt. Nothing installs Chrome from apt, yet its CDN served a mismatched package index for over an hour and failed every Playwright, xvfb and local-node install with `Hash Sum mismatch`.
- [FIX][e2e] The testnet E2E harness now funds every new account from the public faucet before its first transaction, as it already did on devnet; testnet moved to the fee-charging 0.16 node and unfunded mints failed inside the kernel.
- [FIX][all] Onboarding registration failures now appear on the confirmation screen with a Retry action instead of leaving the screen idle. If registration succeeds but the wallet does not report Ready within five seconds, Retry checks readiness again without recreating the wallet. (#835)
- [FIX][all] **Notes the wallet auto-consumes no longer raise a claim prompt (#811, #459).** The home "You have Pending Notes" card, the received-note notification and the Activity red dot read the raw claimable list, so a native MIDEN note that auto-consume was already claiming still asked the user to act, and after a recovery that resurrected spent notes (#742) the card never cleared. All three now read `useManuallyClaimableNotes`, which leaves out only the native notes a consume already covers and, while auto-consume is on, the ones an auto-consumer is about to claim by the rule the service worker applies to its push: a native note worth too little to auto-claim, or a native swap note whose per-order auto-consume is off, still asks. The Pending Notes page deliberately keeps the full list so a failing auto-consume stays visible and retriable.
- [FIX][all] Pending Earn deposits and withdrawals keep checking with bounded backoff while the wallet stays open. Interrupted withdrawals recover their saved delivery requests without repeating the source withdrawal. Activity details recover from temporary database errors and update as status, receipts and token metadata change.
- [FIX][mobile] **The balance card shows a skeleton, not "$0.00", until the first balance read completes.** After a recovery the store has no balance entry for the new address, so the card summed a zero placeholder row and printed "$0.00" for the seconds it took the first read to win the WASM lock from the mount-time sync. The card already had a loading state; the home screen now drives it from `useAllBalances`' loading flag (#844).
- [FIX][e2e] The iOS dApp browser suite no longer checks the dApp slot's paint with the notification-permission alert over it: every iOS wallet screenshot now dismisses that alert first, not only the screen poll's, so the slot the #875 banner shortened no longer drops under the match bar.
- [FIX][mobile] A dApp minimized shortly after it opened reopens from its recents tile or peek card. Before, the restore could show it and park it again at once: parking waits for a snapshot while the dApp is still in the foreground, and a slot measurement landing in that wait marked it as already shown, so the next restore parked it before its slot was measured.

## 1.16.0 (2026-09-09)

### Features

- [FEATURE][all] **The wallet accounts for the network fee that protocol 0.16 charges every transaction.** Since 0.16 the fee is withdrawn from the acting account's own vault inside its auth procedure, before anything else in the transaction runs — so a balance the wallet presented as spendable was not, and an account holding only tokens could not even claim them. Five things change. The send and swap forms cap the native asset at balance minus a fee reserve, and all three of send, swap and earn deposit refuse outright when the account cannot cover a single base fee, so the shortfall is named on the form instead of surfacing as a failed transaction after biometric confirmation. Unattended claiming applies a value floor: a note (or, for a batch, the batch total) worth less than the transaction that would consume it is left alone rather than claimed at a loss, which also closes a cheap griefing vector where one fee buys an attacker a pile of dust notes. Claim ordering puts the native asset first, because a claim is itself a fee-paying transaction and an account with an empty vault has to credit the fee asset before it can consume anything else. Receipts and transaction details show the fee actually charged, read from the kernel's own fee note rather than estimated, and the fee is recorded on the transaction row. And two fee-related failures are no longer reported as a generic proving failure: a request that reached the kernel without the fee-conversion info a Guardian account must commit is named exactly, and the kernel's vault-shortfall assertion now names the fee as one of its two candidates without claiming which — it does not say the fee, because the same assertion also fires on stale local vault state, and telling a user holding plenty of MIDEN to receive some MIDEN talked them out of the resync that actually fixes it. Guardian accounts needed their own path throughout: the auth-arg slot belongs to the multisig, so the client cannot inject conversion info the way it does for a basic wallet, and every request the wallet proposes — sends, rotations, and the pre-built collateral and swap requests that four flows persist at queue time — now carries it explicitly. The fee is discovered from the chain (`fee_faucet_id` and `verification_base_fee` off the latest block header), cached per endpoint and re-discovered when the endpoint changes; a chain that charges nothing behaves exactly as before, and a fee the wallet could not read fails open rather than blocking transactions the node would have accepted. Every surface that spends a fee now names its ceiling BEFORE the tap: the send, swap and earn-deposit reviews, both claim buttons on the pending-notes screens, the bridge reclaim, the retry on a failed transaction, and the dApp's consume approval sheet. The claim and retry paths are the ones that matter, because they submit with no review step at all — there is no later screen on which the cost could appear. The figure is a bound rather than a guess: the kernel's multiple is `floor(log2(cycles)) + 1` and the VM caps execution at 2^29 cycles, so 30x the base fee is a ceiling no transaction of any kind can exceed. Where one tap submits SEVERAL transactions the screen says so rather than quoting one transaction's ceiling over a button that pays it many times — Claim All groups by faucet and so charges once per asset, and claiming notes one at a time charges once each where the group button charges once for all of them. Nothing is rendered at all when the base fee is undiscovered, which is a reachable state that latches a retry cooldown; a zero or a guessed quantity would be worse than silence. The wallet no longer builds the fee-conversion commitment itself: since guardian 0.17.0-rc.3 builds guarded accounts from the upstream auth component, miden-client can classify them, so every wallet-built request simply declares a fee conversion SALT and the client commits `hash(CONVERSION_INFO || SALT)` from the anchored block's fee faucet. That deletes the wallet's parallel implementation — the commitment helper and the auth arg threaded through the send, swap, bridge and collateral builders — and removes the last place the two SDKs could drift on a value that has to match byte for byte or the cosigned summary does not reproduce. A guarded account deployed BEFORE that guardian release still carries the old component and cannot be classified; a declared salt against one fails closed rather than paying a wrong fee.
- [FEATURE][all] **Swap activity now has a dedicated live receipt.** The detail screen mirrors the swap flow with an offered-to-requested amount hero, an accessible Framer Motion amount-progress bar, and fill rows showing the real received amount, consumed time, and note ID without a decorative status icon. Transfer details include the swap transaction ID, source account, and every settlement-consume transaction ID. A manually consumed, active order links to Pending Notes; auto-consumed and terminal orders omit that shortcut, and a filled order no longer shows Cancel. The receipt intentionally avoids predicting a total fill count, which the order lineage cannot know in advance.
- [FEATURE][all] **Guardian seed recovery now recovers pending notes in the background.** After the mandatory hot-key rotation lands (and never while a transaction is in flight or the wallet is locked), the wallet drains the private-note transport, imports notes embedded in pending consume proposals, backfills public notes by the recovered account tag from the account's creation block, and then performs a normal sync. A non-dismissible home card shows each recovery step live, including the public-backfill block progress. A pass that could not import everything it found keeps the account marked so a later session retries it. Transaction-history recovery is out of scope until a Guardian release exposes canonical delta history to clients. (OpenZeppelin/guardian#357)

### Changes

- [CHANGE][all] **Every Miden dependency moves from the 0.16 release candidates to the 0.16.0 finals, which is what makes the wallet work on testnet.** The node validates the client's `accept: application/vnd.miden; version=…` header and requires the pre-release label to match its own, so the stable 0.16.0 testnet node rejects every rc client at onboarding with `accept header validation failed … (client version: 0.16.0-rc.4)`. `@miden-sdk/miden-sdk`, `@miden-sdk/react` and both wallet adapters go to 0.16.0; the native prover pins `miden-client =0.16.0` and its iOS and Android binaries are rebuilt against the transaction kernel the SDK's lockfile resolves (protocol 0.16.1, Miden VM 0.29.4); the E2E stack moves to the rust-sdk `v0.16.0` node and the crates.io `miden-client-cli` 0.16.0 with no git override. The OpenZeppelin guardian and multisig clients move to 0.17.0, the first stable guardian release on the Miden 0.16 line (it pins the same SDK); the E2E stack runs the matching guardian server image, since an account created by a release-candidate guardian fails every read with `No signer commitment found in account storage` under the stable SDK.
- [CHANGE][extension] **Local proving is about 16% faster on machines with more than six cores.** The offscreen proving document spawned one worker thread per logical core, which is counter-productive: the pool competes with the document's own main thread and the browser compositor, and on Apple Silicon `navigator.hardwareConcurrency` counts efficiency cores that are two to three times slower than the performance ones. Work is split evenly across the pool, so a chunk landing on an efficiency core becomes the critical path the whole proof waits on. The pool is now capped at six threads. Measured with the web-sdk proving benchmark on a 4-performance / 6-efficiency-core machine (so it reported ten cores), across three sweeps: 7280ms at two threads, 5367-5530ms at four, 5386-5524ms at six, 5742-5860ms at eight, 6424ms at ten. Four and six are indistinguishable, eight is consistently worse with no overlap, and ten is about 19% worse than six — scaling saturates at the performance-core count and goes negative beyond it. Six rather than four because the error is asymmetric: too many threads measurably hurts while too few costs nothing here, and six leaves headroom on machines with more fast cores.
- [CHANGE][ci] **The fee-accounting spec waits for the sender's native balance to settle before asserting on it.** `vaultBalance` is a single read of the wallet's Zustand store, and that store is fed by AutoSync on an interval — it does not update synchronously when a send completes. Assertion 4 read it immediately after the send, so on a slow devnet moment the pre-fee balance was still there, the delta came back as exactly 0, and the spec failed claiming no fee had been charged. It now waits for the balance the recorded fee implies. A fee that is genuinely never charged, or charged in the wrong amount, still fails — the wait times out and reports expected against actual — so the race is bounded without masking the bug the assertion exists to catch.
- [CHANGE][extension] **A note being claimed keeps a button, and gets it back when the claim fails.** The extension decided whether a note was "being claimed" from its own approximation — a `NoteClaimStarted` broadcast into a Zustand set, cleared only when the note stopped being consumable, with a 120s safety expiry. A consume that FAILED left the note consumable, so the clear never fired; the expiry's clock restarted whenever the account object identity changed (every `StateUpdated`, and every service-worker recycle broadcasts two), so in practice it could be deferred indefinitely. Two further latches — the batch set in `useClaimNotes` and each row's own `isLoading` — were deliberately never reset on extension, so even once the gate cleared the button stayed gone until the row was unmounted and remounted. The gate is now the fact the broadcast was approximating: a live consume row in the shared Dexie, which is exactly what mobile and desktop already read. A failed consume leaves `Queued`/`GeneratingTransaction`, so the note becomes claimable again on the next 3s tick. While a claim is genuinely in flight the row now shows a "Claiming…" control instead of unmounting the button to a blank spacer, and that control opens the consume's own progress screen.
- [CHANGE][ci] **Devnet E2E funds fee accounts from the public faucet, never from stale localnet funder wallets.** `sendNativeFundingNote` picked its funding source by asking whether `wallet_N.mac` genesis funder files existed on disk. Those files are written by a _localnet_ genesis and carry no network tag, so a devnet run on any machine that had previously run localnet found them and spent them as though they were devnet accounts — every fund failed with `transaction execution failed`, because those accounts do not exist there. The failure looked like devnet being broken and was entirely local state. A chain that publishes its own faucet is now the authority on its native asset; genesis funders are the fallback for a chain that has none, which is the only kind that ever had them.
- [CHANGE][all] **Send says the account cannot pay a fee before you type an amount.** The check ran only once an amount existed, so someone holding tokens but no MIDEN picked a recipient, picked a token and typed a figure before learning nothing was sendable — a verdict that never depended on the amount. Swap and earn deposit already said so on mount; send now does too. The German translation of the per-asset fee line is also corrected (`beansprchter` → `beanspruchter`).
- [CHANGE][ci] **All four devnet E2E suites gate on push again — #797 is closed.** Mobile E2E was the last one out, and its remaining failure was not about fees at all: the iOS balance wait summed every asset, so on a fee-charging chain it went positive on the native funding balance and the spec opened its send before the test token existed. With the wait scoped to its token, a devnet run passes 7/7 iOS specs. No gate carves out a neither-ran shape now, so a devnet suite that fails to run is a real bug rather than an expected silence.
- [CHANGE][ci] **The iOS balance wait gates on the token the spec is about to use.** `getBalance` took a token symbol and ignored it, summing every asset instead. That was unambiguous while a test wallet held only the test token — but on a fee-charging chain it also holds the native asset it was funded with, and that lands FIRST because claiming it is what pays for everything else. So the wait went positive on the funding balance, the spec opened its send before the test token existed, and it failed on a missing `send-token-TST` row. The symbol is now honoured, and every iOS balance wait names TST.
- [CHANGE][ci] **Three of the four devnet E2E suites gate on push again (#797).** They were quarantined for a week because devnet's fee-enabled genesis was untransactable by any released tooling. Chrome E2E, Chrome Guardian and Mobile Guardian now pass against the real devnet and their gates no longer carve out the neither-ran shape, so a suite that fails to run there is a bug again rather than an expected silence. Mobile E2E stays off the push arm on its own merits: fee tooling works for it — a run completes mint-and-balance, multi-account, multi-claim and send-private with zero funding failures and zero vault-shortfall assertions — but send-public.ios fails on a UI selector, which is a different problem and shouldn't hold the other three hostage.
- [CHANGE][ci] **The group-claim spec claims its fee funding before minting the notes it groups.** Funding draws a note on the NATIVE faucet, and the spec asserts the pending list collapses into exactly one asset row — so a funding note still pending at that point is a second row, and the assertion fails on a true statement about a state the spec never intended. It passed only where that note happened to be claimed in time. Claiming it before the mints is deterministic; claiming after would drain the two notes the spec exists to group.
- [CHANGE][ci] **Fee funding is asserted, not assumed, and recipient-only wallets get it too.** Funding sends a NOTE, so the vault stays empty until it is claimed — and `claimAllNotes` returns after two empty pending reads, so where note discovery is not instant it drains nothing and reports success. The guardian recovery-stress specs funded and claimed in one breath, and on devnet the claim ran before the note appeared: the account then failed its rotation on an unpayable fee, reporting the kernel's vault-shortfall assertion, which names nothing about funding. Those sites now retry until the native balance is actually non-zero. Separately, the iOS ports of send-public, send-private and guardian-send-consume claim the incoming note on wallet B, which the Chrome ports leave pending — and B is never a mint target, so nothing had ever funded it, while the note it claims carries the test token rather than the native asset. All three now fund B before it claims.
- [CHANGE][ci] **The E2E harness can fund its accounts on a public fee-charging chain.** It could only take the native fee asset from local-stack genesis wallets, which by construction cannot exist on devnet — so since devnet enabled fees every devnet suite died before its first transaction, a fresh account's vault being empty while `pay_fee` withdraws from that vault inside `auth_tx`. Devnet's public faucet IS the chain's native fee-asset faucet (its bech32 id decodes to the `fee_parameters.native_asset_id` the block header reports), so a grant from it is spendable on fees. Both funding sites now go through one source-picking helper: genesis funders where they exist, the public faucet otherwise. The faucet path only sends the note where the recipient is a browser wallet, which claims it itself, and consumes it where the account is CLI-owned — for an undeployed faucet that consumption is also its deploy. The funded-vault check that gates this no longer keys off the token SYMBOL: which identifier the CLI prints in the faucet column depends on whether it knows that faucet's metadata, so on a public chain it prints the raw faucet id and the symbol match reported "not funded" forever against a vault that had just been funded.
- [CHANGE][all] **The kernel's fee note can no longer be picked by accident.** Since 0.16 the fee is paid by emitting a TX_FEE output note, so a transaction that produced one note on a fee-free chain produces two — and any code selecting a note by POSITION can take the wrong one. The signature never changed, so nothing broke and nothing forced a second look; six sites across five layers were quietly wrong on any fee-charging chain while staying green at fee 0. Fixed here: the dApp-facing `waitForTransaction` handed third-party sites the unsplit list (so a site doing `outputNotes[0]` could get the fee note, and every site reading `.length` counted one too many), the confirm sheet totalled the fee as an unexplained outgoing asset and counted a note the user never created on both the executed and summary paths, and the earn and swap completion readers indexed blind. Taking the fee out of the transfer total is only half of it: the dApp approval sheet now shows it as its own "Network Fee" row — on the extension, in the text rows mobile and desktop render, and on the simulated-effects list that is the whole of the off-extension custom-transaction sheet — because a cost the user pays that appears nowhere is worse than one shown in the wrong place. Identifying a note as the fee is itself now a security boundary rather than tidiness, because being identified REMOVES the note from the sheet's totals: `0xfee` is a plain u32 and a dApp's request reaches the wallet verbatim, so a site could otherwise hand over a tagged note and have the transfer erased from the screen the user is approving. Three conditions must hold — the chain actually charges (at a base fee of zero the kernel emits no fee note at all, which made a single tagged note unopposed on testnet), the note holds exactly one asset drawn on the chain's native faucet, and exactly one such candidate exists — and an undiscovered base fee or faucet id now fails CLOSED, counting the note as user value rather than trusting the tag. That reverses an earlier fallback whose reasoning ("nothing in that window is attacker-selected") stopped being true once this code began serving the approval sheet, and it is why bootstrap now discovers the base fee alongside the faucet id instead of leaving it to the first caller that happens to await it. The two decode paths were reconciled to make that safe, since the summary path derives its total from the account delta (fee already inside) and the executed path from the output notes (fee never inside), so the same request would otherwise have rendered a transfer-plus-fee row for a Guardian account and a transfer-only row for a single-sig one, through one renderer under one "verified" label. Rather than patch the sites, the class is closed: `lib/miden/activity/fee-notes.ts` is now the only module permitted to read `executedTransaction().outputNotes()`, enforced by a `no-restricted-syntax` rule that fails `yarn lint`; `partitionFeeNote` returns a branded `UserOutputNote[]`, so handing a raw `OutputNote[]` to anything that picks or counts notes is a compile error; and `fee.test.ts` gains fee-note-FIRST fixtures, since every previous fixture put the fee note last — precisely the assumption the whole class rested on. The primitives live in a leaf module because the accessor has to be cheap to import from every completion path: routing it through `fee.ts` pulled in `lib/shared/format` and broke eight unrelated suites at module load.
- [CHANGE][ci] **The E2E suite now proves that a transaction fee is charged to the right account, in the right asset, for exactly the amount recorded — and the per-PR chain charges one.** Nothing in the suite asserted anything about a fee before this: every money assertion is scoped to a non-native symbol (`TST`, `SWPA`, `COLLATERAL`) while Miden charges the fee in the native asset, so the fee was out of frame by construction, and two helpers had additionally been loosened from `==` to `>=` with "a fee may also leave the account". A fee of zero, a fee of 100x, and a fee charged to the recipient all passed. `fee-accounting.spec.ts` closes that: it reads the chain's `verification_base_fee` as the wallet itself discovered it, then asserts the completed send recorded a `feeAmount` of at least one base fee, that the sender's native balance fell by _exactly_ that amount, that the sender's transferred-token balance fell by _exactly_ the amount sent, and that the recipient's native balance did not move at all. The leverage is that the harness's faucet mints a non-native token while the fee is native — two different assets, so neither assertion needs to tolerate the other and both can be equalities. The `run-local-node` action takes a `verification-base-fee` input (default `10000`, devnet's value), fails the job if genesis did not come up at the value requested, and **every** E2E suite now charges — blockchain, swap, earn, bridge-guardian, resilience, guardian and stress all run at `10000`, because a suite pinned to `'0'` skips the kernel's fee branch entirely and proves nothing about fees. The node build cache is keyed per base fee: its path holds `data/genesis-config`, so a cache warmed at one fee would otherwise restore a genesis contradicting the fee the suite asked for. A zero-fee chain is still a legitimate configuration (testnet runs one), so the spec asserts the zero case rather than skipping, and annotates its result `fee-assertions-exercised` or `fee-assertions-NOT-exercised` with the base fee — a bare green on a fee-free chain proves nothing about fees, and "the fee suite is green" is exactly the sentence that gets repeated. The four send journeys' sender-side check is tightened from "debited by at least the amount sent" to exactly it, which the asset split makes safe and which the sibling guardian specs already did.
- [CHANGE][all] **A failing transaction keeps the cause that explains it.** `resolveTransactionErrorMessage` deliberately replaces technical failures with non-technical copy, but it discarded the original — so an unrecognised-procedure failure reached the user, the logs and the E2E dump as "the app and its prover are out of sync" with nothing naming _which_ procedure. The raw cause is now logged whenever a mapping replaces it, `formatRawTransactionError` walks the `cause` chain (several wrappers in this pipeline say "see the cause chain" in their own message while every consumer printed only the outermost frame), and the harness's failure dump prints the `rawError` the row already persisted. A receiver-side wait for a note that never arrives can also be given the sender's page, so it reports the sender's failed rows instead of "nothing showed up" — the fault in that shape is almost always the sender's, which the receiver cannot see.
- [CHANGE][ci] **The blockchain E2E harness funds the accounts it creates, so the suite runs against a fee-charging chain.** Since protocol 0.16 the fee is paid from inside the account's auth procedure, which withdraws from the account's own vault before anything else in the transaction runs — so on a chain whose `verification_base_fee` is non-zero, an account with an empty vault cannot transact at all, including to deploy itself. The harness now creates its faucet undeployed, funds it from a genesis wallet, and lets consumption of that funding note be its first transaction: note credit lands in the vault before `pay_fee` withdraws from it, so that transaction settles its own fee. Note recipients are funded the same way, because a wallet that is only ever sent tokens can never claim them — `pay_fee` takes the native asset, which a token note does not credit. The faucet is composed with `basic-wallet` as well as `basic-fungible-faucet`: the faucet component exports `mint_and_send` and `receive_and_burn` but not `receive_asset`, so a plain faucet cannot consume a P2ID note and has no way to be funded (genesis cannot seed one either — `[[fungible_faucet]]` takes no `assets`, only `[[wallet]]` does). Whether the chain charges is discovered from the first deployment that fails for want of a fee rather than configured, so a chain that starts charging is handled without a harness change and one that does not never takes the funding path.
- [CHANGE][ci] **The local E2E node is built against the same protocol rev as the wallet's SDK.** A `Protocol PR:` marker reached the SDK but not the node: `start-test-node.sh` cargo-installs the node binaries from crates.io, where they resolve the protocol crates their own manifest pins, so the SDK ran 0.16.0-rc.8 against an rc.4 node and prover. That is invisible for a single-sig account and fatal for a guarded one, which links `miden::standards::fee` DYNAMICALLY — the host supplies the MASM at execution time, and a host on the other rev rejects the account's own procedure root ("procedure with root digest ... could not be found") on the prover, then again on the node, while the local and earn suites stay green on the identical stack. `run-local-node` now takes the rev `inject-linked-web-sdk-pr` already resolved (handed over through `$GITHUB_ENV`, so there is no second pin to drift), clones the node source and **rewrites its exact `=0.16.0-rc.N` protocol requirements to the version the patch provides** before emitting a `[patch.crates-io]` for the whole protocol workspace into a private `CARGO_HOME`. That rewrite is the part without which none of the rest does anything: cargo substitutes a patch only where the patched version SATISFIES the requirement, and an exact pin is unsatisfiable by any other version, so the patch is recorded "not used in the crate graph" — a warning, exit 0. The binaries are therefore installed from that clone rather than from crates.io. Four things then have to be loud, because every failure mode here is a warning with exit 0: an unused patch is a hard error; both lockfiles (the node's, and miden-client's, which builds gen-genesis) are asserted to carry every protocol crate exactly once at the pinned rev; the dep-info of the binaries that actually start is asserted to carry no `registry/src` protocol crate; and the node cache key carries the rev, so a stock build cannot be restored over a patched one. Running on a `pull_request` with no injection ahead of it is an error rather than a silent stock node.
- [CHANGE][all] **In-browser local proving works again — `@miden-sdk/miden-sdk` and `@miden-sdk/react` move to `0.16.0-rc.4`.** The only change in that SDK release is the Miden VM moving `0.29.1` → `0.29.4` (same `miden-client 0.16.0-rc.2`, same protocol layer `0.16.0-rc.6`), carrying [miden-vm#3722](https://github.com/0xMiden/miden-vm/pull/3722): `miden-processor` now attempts the hasher-chiplet trace-builder thread with a fallible spawn and builds the trace serially when the target refuses. On wasm32 the refused spawn used to trap ~2ms into every local prove, and the trapped `wasm-bindgen` future never settled — which is why local proving could not complete in a browser anywhere on the 0.16 line. The `send-public-local-prove` E2E spec is un-quarantined and runs in the local suite again. The native prover's VM crates take the same `0.29.4` move and its binaries are rebuilt in lockstep (the `miden-client` pin stays `=0.16.0-rc.2`; `miden-protocol`/`miden-tx` stay `0.16.0-rc.6`).
- [CHANGE][all] **The wallet moves to the Miden 0.16 protocol line.** `@miden-sdk/miden-sdk` and `@miden-sdk/react` go to `0.16.0-rc.3` (from the `0.16.0-alpha.2` this branch carried), which bundles `miden-client 0.16.0-rc.2` with the protocol layer at `0.16.0-rc.6` and Miden VM at `0.29`. No wallet call site needed adapting: the breaking changes across these releases (`AccountComponent.createNetworkAuth` → `createNetworkAuthComponents`, `TransactionSummary.salt()` → `userParams()`, the `basic_wallet::add_assets_to_account` rename, and network-note fee pricing) all land on surfaces the wallet does not use, and what `0.16.0-rc.3` adds on top of `rc.2` is additive — chain-anchored execution (`captureAnchor`, the `anchor` option, `useChainAnchor`/`usePreview`) plus two `TransactionSummary` accessors, none of which the wallet calls. The local E2E stack follows the same line: the test node is built from `0xMiden/miden-client` at `v0.16.0-rc.2`, the `miden-client-cli` pin moves to `0.16.0-rc.2`, and note-transport-service moves to `v0.5.0-rc.1` — still the newest NTS release, now one prerelease behind the SDK's protocol layer rather than one ahead.
- [CHANGE][all] **Guardian accounts work on the 0.16 protocol line again.** `@openzeppelin/guardian-client` and `@openzeppelin/miden-multisig-client` move to `0.17.0-rc.1` — the guardian's own version line is independent of Miden's, and this is its first release built against Miden 0.16 (it declares `@miden-sdk/miden-sdk: 0.16.0-rc.3`, matching the wallet's pin, and its `guardian_ecdsa` MASM was rewritten for the new kernel). Until now every published guardian build targeted Miden 0.15, so the wallet's `resolutions` forced a 0.15-era client onto a 0.16 SDK and guardian account creation failed at runtime on divergent MASM procedure roots; the guardian E2E suites were quarantined for it (#522) and are re-enabled here, as required checks rather than jobs that tolerate `skipped`. Three call sites needed adapting: proposals now carry a `ChainAnchor` (see below), `createP2idProposal` takes its options as the fourth argument rather than behind a `nonce` slot that no longer exists, and `MultisigConfig.guardianEnabled` is gone because the account builder now rejects a config without a guardian commitment outright — the wallet only ever passed `true`, so behavior is unchanged. The account's storage slots also moved namespace in this release, as the multisig auth component graduated into `miden-standards` (`openzeppelin::multisig::signer_public_keys` → `miden::standards::auth::multisig::approver_public_keys`, and likewise for the guardian key). The wallet had those names inlined as local constants, so every read returned nothing and a freshly-created Guardian account looked like it had no signers at all — each proposal failed with "No signer commitment found in account storage". The readers now go through `AccountInspector`, the package's supported accessor, which is versioned with the layout; the package deliberately does not export the slot names for this reason. A fourth change closes a real hole rather than tracking an API: new Guardian accounts now also pin `update_procedure_threshold` to 2. Requiring both device keys to change the guardian only means something if lowering that requirement costs the same, and it did not — `update_procedure_threshold` sat at the account threshold of 1, so either key alone could have dropped the guardian override back to 1 and then switched the guardian by itself. The 0.17 builder rejects that shape outright, matching the check the Rust side already enforced. Devnet points at the OpenZeppelin staging guardian, which already runs `0.17.0-rc.1`; the local E2E stack's guardian image moves to `v0.17.0-rc.1` in lockstep with the client, since server and client have to agree on the procedure roots. The local patch that made the client's P2ID builder pick the vault slot that could fund the transfer is dropped with this bump: it existed because on Miden 0.15 an asset's callback flag lived in its vault key, so one faucet could occupy a slot per variant and the builder picked by vault order. On 0.16 the flag is a property of the faucet account id — it cannot vary per asset — so there is no longer a choice to get wrong, and the client no longer consults the vault at all.
- [CHANGE][all] **A replace-hot-key proposal records the block it was built against.** Since protocol 0.16 a signed transaction summary binds the reference block commitment, so it only reproduces when re-executed at that same block. The wallet builds this one proposal's summary by hand (the rest come from the multisig client's own builders, which already do this), and it now serializes the `ChainAnchor` that execution returns onto the proposal alongside the summary. Without it a cosigner or the executor re-executes at whatever height it happens to be synced to, derives a different summary, and the collected signatures no longer verify — the multisig client refuses to execute an anchor-less proposal rather than let that happen silently, so any such proposal still pending from an older build has to be recreated.
- [CHANGE][extension] **Speculative pre-proving is disabled on Chrome while its cache is unreachable.** With the Miden client running in the offscreen document (the default), the send that would claim a speculative proof executes in that realm, while the cache holding it lives in the service worker — so the pre-prove was produced and thrown away, and Confirm paid the full prove cost anyway. Rather than keep starting a prove per debounced edit of the send form (and closing the offscreen document mid-read to abort superseded ones, which caps the CPU actually burned at roughly one prove per editing session), the wallet now stops speculating when it cannot consume the result. No send gets slower, whatever its reclaim setting: on this configuration the send already ran inside the offscreen document, and the cache is only ever consulted on the branch that dispatches a prove _to_ that document — unreachable from inside it — so no send was claiming a speculation to begin with. (The feature bought little in the other configurations either: there the cache is skipped whenever a reclaim height is set, and the review screen seeds every same-chain send with a 7-day reclaim unless the user edits it to "Never".) The flag-off and non-Chrome paths are unchanged.
- [CHANGE][extension] **Listing claimable notes no longer spins up a Web Worker per call.** The short-lived client this read uses left the SDK's worker default in place. That was free in the service worker, where workers do not exist, but the read now runs in the offscreen document, where it meant a fresh worker and a second copy of the WASM module on every sync tick, claimable-notes refresh and dApp note query.
- [CHANGE][mobile] **The native transaction prover is rebuilt against `miden-client 0.16.0-rc.2`.** The prover ships as committed binaries, so a pin that lags the SDK is not caught at build time — it surfaces on-device as the prover rejecting valid transactions with `procedure with root digest … could not be found`. The Android JNI libraries (`arm64-v8a`, `x86_64`) and the iOS `MidenMobileProver.xcframework` (device + simulator) are all rebuilt on the new pin, and `prover.prove()` is now called synchronously (it stopped returning a future in 0.16). Moving from the SDK's `0.16.0-rc.2` to `0.16.0-rc.3` took the transaction kernel with it (`miden-protocol`/`miden-tx` `0.16.0-rc.4` → `0.16.0-rc.6`), which is a procedure-root-affecting move rather than a client-only one, so every artifact was rebuilt again rather than exempted. The 0.16 kernel is materially larger — the Android library grows from ~5.8 MB to ~16 MB — but the iOS libraries are now built with the same link-time-optimized profile the Android bridge already used, which takes them from ~97 MB to ~27 MB despite the bigger kernel. A repeatable `packages/native-prover/scripts/build-ios.sh` replaces the previously undocumented manual steps; its post-build check that the archive exports `_miden_prove_transaction` no longer fails on a good archive, which it did once the toolchain started emitting LLVM bitcode newer than Xcode's `nm` can read — `nm` exits non-zero after listing every native member, and under `pipefail` that sank the check regardless of the symbol being there.
- [CHANGE][all] **Proving a transaction in the browser is unavailable on the 0.16 line, and its end-to-end spec is quarantined.** `miden-processor` overlaps hasher-chiplet trace building onto a second thread via `std::thread::scope`, gated only on `#[cfg(feature = "std")]`. wasm32 has `std` but cannot spawn threads, so the spawn returns `Unsupported` and the module traps about two milliseconds into the prove. Trapping inside a `wasm-bindgen` future is what makes this hard to see: the JS promise is never rejected, it simply never settles, so the prove neither completes nor errors and holds the offscreen client lock until the write watchdog tears the realm down — a 35-minute silent stall on a laptop, from a crash that happened immediately. It arrived in miden-vm v0.28.0 (miden-vm#3407) and reached the SDK at `0.16.0-rc.2`; `0.16.0-alpha.2` and earlier prove locally in seconds. Only the local fallback is affected. Delegated proving is the default on every platform and is untouched, because that proof is produced natively by the remote prover, and mobile routes local proves to the native Rust prover — so the exposure is an extension or desktop user who turns delegated proving off, or a delegated prove that falls back to local, either of which now fails at the write deadline instead of proving in-browser. `send-public-local-prove.spec.ts` is skipped until the SDK ships a `miden-processor` that excludes wasm from the threaded path, and the stress suite's opt-in `STRESS_LOCAL_PROVING` flag carries the same warning; CI never sets it.
- [CHANGE][ci] **A release's build artifacts are named after its tag rather than `package.json`.** A prerelease cannot put its own version in the tree: `package.json` is stamped verbatim into the extension manifest, which Chrome requires to be dotted integers, and into the Tauri bundle, whose Windows MSI rejects a prerelease as well — so the tree stays at the final version for the whole candidate series. Every artifact was named from it, which meant `v1.16.0-rc.0`, a later `v1.16.0-rc.1` and `v1.16.0` itself all produced a file called `MidenWallet.1.16.0.Testnet.zip`. Nothing was ever overwritten, since uploads are scoped to their own release, but a file already downloaded no longer said which build it came from. Chrome, Android, iOS, macOS and Windows artifacts now take the tag; a manual dispatch has no tag and keeps reading `package.json`.
- [CHANGE][all] **Settings sub-screens open as full-screen pages instead of bottom-sheet drawers, and Settings plus the Guardian screens are restyled.** General, Address Book, Language, Keys, Guardian, Advanced, and Authorized dApps now navigate to routed `/settings/…` pages sharing one prominent header (left-aligned Nunito title, circle back arrow, bar divider — also applied to Pending Notes, Transaction details, and the Rotate Guardian flow); the Settings root gets grouped headings with per-group icon chips and icon-less rows; the Guardian settings, rotation review (new hero illustration), and a new switch-guardian success screen follow the one-page design references; non-Latin language names no longer render as missing-glyph boxes on iOS.
- [CHANGE][ci] **The browser-crash E2E no longer fails on its own screenshots.** The harness screenshots the app whenever it signals a screen change, which the app does by calling a function the test runner exposes into the page. That capture is diagnostics, but it is not passive: answering one means real calls back into the browser. The spec that kills the whole browser on purpose, to prove the wallet survives a crash, could land that kill while a capture's call was still outstanding — and the reply then named a browser object the test runner had already discarded, failing inside Playwright's own bookkeeping as “Object with guid handle@… was not bound in the connection”. Playwright charges that to whichever test is running, so a spec whose body had passed was reported as failed, with no stack and nothing in it to point at. Error handling around the capture could not have helped: the throw happens while the reply is being validated, not in the code that made the call, which is also why it arrived bare. The trace confirms it rather than merely fitting — the run's last open action is the capture's own wait, with the browser close logged right after it, and the recorded error names the guid CI reported. The harness now switches capture off and waits out anything already running before it destroys a page or a browser, at every point that does so, including the per-test teardown that closes both wallets. That teardown is where the previous version of this bug lived (an unguarded context close), which is the reason for the wider net. No app behaviour change.
- [CHANGE][ci] **The double-payment guard is now tested against a real database and a real chain, not against mocks.** The bug where cancelling a send that was already on its way could pay the recipient twice passed the entire test suite, because every test around that code replaced the database with a hand-written stand-in and exercised one piece of it at a time — so nothing ever ran the pieces together, and the stand-ins did not reproduce the one database behaviour the bug turned on. A new test runs the real code against the real database schema and replays the whole sequence, with a companion case that deliberately omits the fix to prove the test can still fail. A new end-to-end spec then does it against a live chain and a live guardian, where the assertion is the recipient's actual balance — the only place "the network rejected the duplicate" can genuinely be observed. Writing the first of those immediately turned up the retry bug listed above. Test-only, plus a test hook on the retry button.
- [CHANGE][ci] **A guardian E2E spec no longer fails on its own instrumentation after the browser it deliberately killed goes away.** One spec kills the whole browser on purpose, as a deterministic stand-in for the Chromium crash CI produces intermittently, so the wallet's crash-recovery relaunch is exercised on every run. Two pieces of harness weren't expecting to be pointed at a browser that had already gone. The network-capture instrumentation attaches listeners to the extension's service worker and re-attaches whenever MV3 recycles it, from an async event handler — so a worker announced and then destroyed made the attach throw, and with nothing awaiting it that became an unhandled error charged to whichever test was running. Separately, the fixture teardown closed each wallet's browser context unguarded, unlike every other close in the harness, so a relaunch that didn't complete produced a second failure from teardown that buried the first and could fail a test whose body had passed. Both now tolerate a browser that is already gone: losing capture on a worker that no longer exists costs nothing, and teardown can no longer invent a failure. Test harness only — no wallet code is touched and no assertion is weakened.
- [CHANGE][ci] **CI no longer loses an hour to a stalled package mirror.** The Ubuntu package mirror the runners use intermittently stops responding; when that happened, jobs sat inside the package step until they hit their one-hour limit and were killed without running a single test. Package installs now prefer the canonical mirror, time out and retry individual downloads, and every install step has its own cap — so a bad mirror costs a few minutes and a retry instead of a whole job. The browser-download step, which has hung for over two hours, is capped the same way.
- [CHANGE][ci] **E2E screenshots now tell the test's story instead of tracking every UI twitch.** Capture was "one screenshot per screen-key change", so the gallery filled with transient drawers and intermediate cards unrelated to what the test was doing. It's now one frame per named test step (captioned by the step name) plus a few explicit beats inside the send/swap/claim flows (review → generating → receipt), written in test order — so each spec's filmstrip reads as its narrative. No app behaviour change; the old app-side screen-key signal is left dormant.
- [CHANGE][ci] **The mobile E2E screenshots no longer have the OS "Allow notifications" dialog covering them.** The wallet requests notification permission when its home screen mounts, and the native prompt then sat over the centre of nearly every mobile screenshot. Android now pre-grants `POST_NOTIFICATIONS` before launch, and iOS taps "Allow" on the SpringBoard alert via `idb` (the CDP harness can't reach a native alert). No app behaviour changes — the real permission code path still runs; the alert is just answered. Best-effort, so a missing `idb` degrades gracefully instead of failing the run.
- [CHANGE][ci] **Two guardian E2E specs no longer fail their own setup on a healthy run.** The shared claim helper stops as soon as it twice reads an empty list of claimable notes — which is also true in the moment before a freshly minted note has synced into the wallet. The guardian recovery and forgot-password specs claimed immediately after minting, so they could report "drained" having consumed nothing; the note then arrived and sat unclaimed, and the exact-balance check that followed failed on a run where nothing was actually broken. Both specs now wait for the note to be discovered before claiming, which is what the guardian switch specs already do. No app behaviour change.
- [CHANGE][mobile] **The dApp browser now has an end-to-end test suite on both iOS and Android.** It drives the whole journey a user can take — opening a dApp from the curated grid or by typing a URL, minimizing it to a tile, maximizing it again, stacking several dApps and switching between them, leaving the browser tab and coming back, and closing one — and checks what is actually on screen at each step rather than only what the app believes. The dApps under test are served locally, so the suite is deterministic and needs no network or third-party site to stay up. It runs on every push to `main`, and its screenshots are published to the E2E gallery alongside the other suites. Getting them there needed one app-side change: the entire dApp browser lives on a single route, so the signal that drives screenshot capture saw no change across opening, minimizing, maximizing or switching dApps — the suite whose failures are purely visual would have contributed a filmstrip with none of its transitions in it. The app now names which dApp is on screen (and the card switcher) in that signal, using the same label the capsule bar shows, once the dApp is actually up rather than while it is still loading. Gated on `MIDEN_E2E_TEST` and tree-shaken out of production builds; no user-facing change.
- [CHANGE][ci] **Two E2E suites stop reporting failures that were the test harness's own, not the product's.** The dApp-browser suite's staleness check reads the 96px generation marker the fixture page paints in its top-left corner, mapping the wallet's slot coordinates onto a device screenshot — but those two coordinate spaces only share an origin when the webview runs edge-to-edge, which is true on iOS and false on Android, where the screenshot includes the system status bar above the WebView. The check therefore sampled ~40px above the marker on Android, read a blend of marker and wallet chrome, and failed as "the dApp is showing a STALE frame" on a perfectly correct app. It now measures that offset from the dApp's own painted top edge instead of assuming it is zero (verified against the failing run's artefacts: 40px on the Android emulator, 0 on iOS). Separately, the Chrome E2E config carried two `use` blocks, so the later one silently won and the bounded action/navigation timeouts added in 1.15.x never applied to a single CI run — leaving stuck actions to hang for the whole five-minute test budget and fail as "Target page, context or browser has been closed", naming neither the step nor the element. The blocks are merged, and the root `playwright*.config.ts` files (previously checked by neither `yarn lint`, which only walks `src`, nor `tsc`, whose `include` named just one of the sixteen) are now type-checked, which makes a duplicate key a hard error rather than a silent one. No app behaviour change.
- [CHANGE][ci] **A single hiccup from the npm registry no longer fails a whole CI job.** Every `yarn install` in CI ran once, unguarded, so one bad HTTP response while fetching one package took the job down with it — a `502 Bad Gateway` on a single `@babel/core` tarball failed Build Production on `main` while every other workflow on that commit was green, and the deploy itself had done nothing wrong. yarn v1 retries some socket-level errors but not an HTTP error response, so a 502 is terminal and the retry has to wrap the install. All five install steps (across `ci.yml`, `cd.yml` and `pr.yml`) now share one composite action that retries with linear backoff and hard-caps each attempt, so a stalled registry costs a couple of minutes and a retry rather than the job — the same treatment the Ubuntu package mirror and browser download already got. The per-attempt cap is enforced inside the action rather than with `timeout-minutes`, which GitHub does not reliably apply to a step that calls a composite action. Installs stay lockfile-tolerant, since two of the five run after the linked-web-sdk injection has rewritten `package.json`. No app behaviour change.
- [CHANGE][ci] **The double-payment E2E now reproduces a cancel the user can actually retry, so it can pass at all.** The spec builds its scenario by rewriting a landed send into the row a mid-flight cancel leaves, then clicking Retry and watching the recipient's balance. It wrote the _user's_ cancel reason — and the details screen derives "this was cancelled" from that string alone and drops the Retry button entirely when it matches, a guard that predates the double-payment work by many releases. So the spec constructed the one state in which the control it must click cannot exist, and its sixty-second wait for that button could only ever time out. It has failed on every run since it landed. The two cancel routes differ in exactly that string: the stuck-row reaper, which ages out a send that outran its processing limit, fails rows through the same `cancelWhilePipelineMayStillRun` but writes its own reason, leaving Retry on screen — and that is the route the guard under test actually has to defend, so that is what the spec now plants. No product behaviour changes, and the assertion is untouched: a retried send that may already have broadcast must credit the recipient exactly once. Whether a hand-cancelled send _should_ be retryable is a separate product question this does not answer. The planted row is now also documented for what it is — a deliberate composite carrying the reaper's reason at the inline pipeline's stage, rather than a transcript of one production row — including why the stage must stay where it is: move it to the one a real reaper cancel would more often leave and the spec keeps passing while quietly losing the ability to fail, because the cached bytes it exists to test would then survive whether or not the defence under test is present. Three unit tests now pin the distinction the E2E silently depends on, one of them against the real predicate rather than the test file's mock of it, so the next regression costs a jest run rather than a chain, a guardian and a twelve-minute job.
- [CHANGE][ci] **The linked-web-sdk gate no longer mails a failure every fifteen minutes, and its verdict can no longer get stuck.** The gate published its verdict as a commit status and re-published it on a fifteen-minute cron. GitHub caps each (commit SHA, status context) pair at 1000 statuses, so a PR whose head SHA sits still — an open PR waiting on a web-sdk release is exactly that — burned the cap in about ten days, after which every write returned `422 Validation Failed`. That had two costs, both of which wallet#510 was paying: the step failed on the 422, so the scheduled workflow went red every fifteen minutes and mailed everyone watching the repo about a "failure" that was the gate's own bookkeeping rather than anything about the PR; and since no further status could be written, the pending→success flip due when web-sdk finally released would have been silently dropped, leaving the PR blocked on a stale `pending` with no way to clear it short of a force-push. The verdict is now a Check Run, which has no per-SHA cap: one run is created per head SHA and updated in place on each later evaluation, so nothing accumulates and the verdict always reflects current state. `pending` maps to `in_progress` so a ruleset can still require the check by name and block merge. The `statuses: write` permission is dropped, since nothing writes statuses now. This is the arrangement web-sdk's equivalent gate already uses; the wallet's copy predated that fix. Fixed alongside it: the gate job's condition tested `github.event_name == 'pull_request'` while the workflow triggers on `pull_request_target`, so both arms were false on a PR event and the gate skipped entirely — every verdict came from the cron, up to fifteen minutes late. One consequence of the migration is worth knowing: a PR that already carries a capped, stale `pending` status keeps it until its head SHA changes, because the old status can no longer be overwritten. A push clears it. No app behaviour change.
- [CHANGE][ci] **E2E screenshots are now browsable as a GitHub Pages gallery.** The per-screen-change screenshots each E2E job captures were only reachable by downloading a run's artifact zip and digging through `screens/` folders (and macOS Finder silently drops files on those long paths). A new Pages deploy aggregates the latest `main` run of every E2E job into one browsable gallery at `/e2e/` — grouped by job and spec, with a click-to-zoom filmstrip — while the existing docs stay at `/`. No user-facing change.

### Fixes

- [CHANGE][all] **Devnet E2E is quarantined (#797) and the SDK stack moves to the newest 0.16 releases.** Devnet's 2026-08-27 re-genesis (miden-node v0.16.0-rc.2) enabled transaction fees, and no released tooling can transact against it — fee conversion info is opt-in on the client, and neither the E2E `miden-client` CLI (verified: even `new-account --deploy` for the test faucet aborts with the same kernel assertion on the rc.2-era and rc.3 CLIs), the web-sdk, nor the multisig-client thread it yet — so all four devnet suites skip their push arm and the gates carve that shape out loudly instead of leaving `next` permanently red; `workflow_dispatch` still runs them, and #797 tracks the per-suite exit criteria. Alongside: `@miden-sdk/miden-sdk` + `@miden-sdk/react` move to `0.16.0-rc.5` (bundling `miden-client 0.16.0-rc.3`; protocol stays `0.16.0-rc.6`, the VM stays `0.29.4`, so the pinned native prover binaries remain proof-compatible without a rebuild) and the E2E CLI pin moves to `0.16.0-rc.3` on the renamed `0xMiden/rust-sdk` repo — the right pins for the moment fee support ships.
- [CHORE][all] Added a conformance run that checks every dApp provider — extension, mobile and desktop — against the published `MidenWallet` interface, using a suite the adapter itself exports. Nothing previously compared the real interface to the real providers: the adapter's tests injected a fake `window.midenWallet` and this repo mocked the adapter package, so each side asserted against a shape it invented. The run records the two providers' known gaps exactly, so both opening a new one and closing an existing one are visible. Making it able to run at all took two jest changes: the adapter ships an ESM-only bundle that had to be added to the transform whitelist, and its root manual mock had to be bypassed for this one suite — without either, the suite caught its own import error and skipped permanently while reporting the reason as a missing export.
- [CHORE][all] Moved the dApp adapter off the abandoned `@demox-labs` npm scope onto `@miden-sdk/miden-wallet-adapter-{base,miden}@0.16.0-rc.5`, which Miden maintains. The old packages were last published 2025-11-26, are owned entirely by Demox Labs accounts, and were pinned at `0.10.0` on this branch — so the wallet's dApp-facing surface depended on packages nobody at Miden could publish, five minor versions behind the interface the adapter now ships.
- [FIX][all] **An abandoned WASM operation can no longer borrow the client out from under its successor, and dead-lettered notes get a way back.** The three follow-ups #788's review deferred land together. First, every user-initiated write pipeline now re-checks lock ownership between a parking await and its next WASM call, per the contract that review wrote into CLAUDE.md: the offscreen document's dispatch mechanism hands every reducer its hold structurally (retiring the publish-consume convention only three of twenty-two reducers used), and the dApp reads, vault onboarding and restore chains, the custom-transaction dry run, the guardian proposal builds and the swap request build all refuse to continue once the watchdog has handed the mutex to a successor — an evicted flow's next call would be a second borrow of a client somebody else is inside. Submit and everything after it is deliberately never guarded — completing beats aborting once a transaction may have been broadcast — and each such site now says so where a future guard sweep would otherwise "fix" it; the pinning tests assert both directions, eviction refusing the pre-submit call and eviction NOT stopping the post-submit one. Second, a provably permanent HTTP rejection — tonic's "mapped from HTTP status code 400/404" fallback for a gateway answering gRPC-web with a bare HTTP error — no longer rides the note-import queue's 24-hour transient budget (~288 lock-held retries against an answer that cannot change): it takes the bounded three-attempt path and dead-letters labeled `rejected`. The set is deliberately just 400 and 404: a 401 or 403 can heal with the note's bytes unchanged (a WAF bot-challenge answers 403 for a minute, an expired gateway credential 401 until refresh), so treating them as permanent would have collapsed a day's budget to about fifteen seconds behind a challenging WAF; those, 408/429/5xx and number-free failures all keep the transient verdict, and the connectivity banner and seed-restore guard keep the deliberately broad classifier untouched. Third, the dead-letter store's contract always assumed a manual drain existed, and now one does: the Activity screen surfaces "Incoming notes need a retry" with the count whenever the store is non-empty, and its Try again requeues every dead-lettered note with fresh budgets — each note queued before it is removed, so bytes that may be the only copy of the funds are never absent from both stores — grants the pass one probe through a lit note-import fuse (the evidence stands, so a still-parked call re-fuses on its next eviction), and kicks an import pass in the realm that owns it so the outcome is visible now rather than on the next transaction-loop lap. (#788 follow-up)
- [FIX][all] **A chain sync that never answers is bounded, and a rate-limiting node is backed off instead of hammered.** On wasm32 the SDK's sync RPCs carry no transport deadline (the wasm client drops the timeout it is handed), so a response that never arrived parked `syncState` forever while holding the process-wide WASM client lock — on mobile, where the client runs on the main thread and the 3s idle loop is the wallet's only sync driver, that wedge is #777's permanently frozen wallet. Every pure-sync lock hold on that path — the idle loop and the transaction pipeline's pre-flight and landed-verification syncs; the service worker's own hold keeps its 30s timeout when the WASM is in the offscreen realm, and takes the ceiling instead when it is not — on the inline path (Firefox, or the flag off) that timeout's rejection released the mutex while the abandoned sync was still inside this realm's client, handing it to the read path behind it — now carries its own two-minute watchdog ceiling, and crossing it is the #775 eviction — named error, replaced client for the next tick — rather than the five-minute last resort; the mobile/desktop loop also gains the service worker's exponential backoff curve (30s→5min with jitter, a banner Retry or app foreground probes straight through) and is stricter with it — one probe per window, where the service worker still allows a fresh failure streak between windows, so an idle wallet stops feeding a rate limiter the burst that precedes the freeze — one sync tick fires SyncChainMmr, SyncNotes and SyncTransactions back-to-back, which is exactly the three-429s-in-20ms signature the incident recorded. Driving the trigger at an idle simulator wallet showed a 429 response itself already errors cleanly (both with and without a gRPC content-type); the park needs a request that never answers, and after an eviction the SDK's module-level sync coalescing still joins every later sync onto the abandoned call — so what the ceiling recovers is the mutex, not the sync, which is still the whole difference between a frozen wallet and one whose balances, sends and claims work, while full sync recovery additionally needs the upstream miden-client fix arming the transport deadline it currently ignores. The eviction is also bounded in the other direction: the per-hold ceiling is clamped so a caller can only ever tighten the five-minute backstop, never widen it past the bound #775 established. An eviction also stops freeing the client out from under the flow it evicted: that callback is abandoned rather than cancelled, so it may still be inside a WASM call, and the instance is marked poisoned in place — flipping the same flag its `isDisposed` guards already read, so the next acquirer still gets a fresh client — with outright termination reserved for a trap that leaves no holder behind. Marking is bounded rather than a permanent leak: the abandoned flow's own promise is the signal that it has stopped touching the client, so the poisoned instance is freed once that settles — and once every other flow still holding a reference to it (a sibling suspended mid-yield, the successor that resolved the same instance) has settled too, since any of them may still be inside a WASM call. An operation that never settles at all is the case that reclaims nothing, and it is the #777 case: a fetch the node accepted and will never answer holds its client for the realm's lifetime, which is what the fuse — not the deferred free — bounds, by cutting how often such an eviction can happen from once per two minutes to once per thirty. Because an eviction is abandoned rather than cancelled, the breaker counts it only once — the abandoned sync settling late can no longer zero the failure streak from a hold the loop had already given up on, which would have kept a node slow enough to be evicted every tick on the 3s cadence forever; and since the eviction's message is closed wallet-authored text that no transport heuristic matches, it now explicitly raises the connectivity banner, so the hang is not the one failure that backs off with no Retry offered. And because that same coalescing means a fresh client joins the parked sync rather than escaping it, repeated evictions cannot fix the wedge they are evicting: after four consecutive watchdog evictions the loop drops from the 3s cadence to one probe every 30 minutes — on mobile each eviction also leaks the WASM instance, since terminating a worker that does not exist is a no-op, so the breaker's few-minute ceiling would keep paying that price indefinitely. Deliberately a stretch and not a stop: a parked fetch can still be torn down or answered late and nothing reports that moment, so the loop has to keep looking, and a wallet that had given up must not depend on a Retry banner the user can dismiss or a foreground hook that only exists on mobile. Any successful sync restores the normal cadence, and a Retry or foreground still probes immediately — a user's own Retry is exempt from both the fuse's evidence and the breaker's escalation, so tapping it against a down node cannot walk the curve it is trying to escape (a failed tap does re-arm the current window from now, which is what keeps a dead node off the 3s cadence). The conclusion also survives an idle auto-lock and unlock, since what it describes is the realm's parked sync rather than one screen's lifetime — the user coming back is worth one probe, not a return to the fast cadence. The fuse's evidence is a fact about the realm's client rather than about one loop, so every automatic probe on that client keeps its own entry in one shared ledger: the guardian accounts' sync — a hold on the same client, at the same ceiling, driven from the same tick, whose failures are swallowed per account — now feeds it too, where before its evictions were structurally invisible to the only counter that existed and a guardian wallet (the default type) could park and poison the client every two minutes forever with the fuse reading zero. Per probe rather than one count for all of them, because a single count was defeated by ordering alone: the loop reports its own healthy sync before firing the guardian's, so on a healthy node with a parked guardian the count oscillated between zero and one and the threshold was unreachable — the same every-two-minutes park the sharing was meant to end, now with the sharing as its cause. Evidence is withdrawn only by a success on the same probe, since one probe reaching the node says nothing about another probe's parked call — and "probe" means the narrowest thing that can park independently, so the guardian accounts are keyed one entry per account rather than one for all of them: they talk to separate endpoints, the loop over them is sequential, and a shared key let a healthy sibling erase the parked account's evidence inside the same lap, which is the unreachable threshold above one level down. That gate lives inside the guardian sync itself rather than at its caller, so it also holds for the extension's post-sync trigger, and a wallet with one parked guardian account keeps syncing its healthy ones. A lit fuse also has to be consulted by probes that have no cadence of their own to stretch — the fired-and-forgotten guardian sync, and the two polls below — or it bought nothing: each simply started a fresh two-minute park on the next tick. The service worker gets a fuse of its own on the same rule, since on the inline path (Firefox, or the flag off) it pays the same leak per eviction. Two remaining unbounded holds on the idle path are converted too — the claimable-notes poll (both of its holds, the note read and the swap-lineage classification that follows it on the same tick) and the balance read, which is the one hold that runs precisely because the mutex was just released, so after an eviction it is the flow that rebuilds the client against the node that just parked. Bounding them capped each park at two minutes but not the rate, so both now feed and consult the fuse as well: fused, the notes poll pauses (leaving the notes already on screen alone, where an empty result would have read as "they're gone") and the balance read skips exactly as it already does when the mutex is busy; the notes poll books its success only once BOTH of its holds are through, since a success booked between them cleared the very evidence the second one was accumulating. The swap-settlement tick joins them on the same terms — a 3s timer that reads the same consumable notes through the same proxy, and so the same probe — where it had been left on the five-minute backstop with nothing watching it at all, and it withdraws that shared evidence on its own success rather than only ever adding to it. The note-import queue's pass is fused too: bounded but unwatched, driven from the transaction loop, it parked the realm's only mutex and leaked a client once per lap, and the sync loop's counter could not see it because it counts only the sync loop's own holds — skipping is safe there because the queue is durable, so the notes keep their attempts and their backoff and a consume waiting on one fails recoverably. Only a pass that actually started an import withdraws that evidence, which is what makes the fuse reachable at all: from the second failure onward a carried note's backoff exceeds the transaction loop's own cadence, so a pass with nothing yet eligible always lands between two evictions, and booking it as a success held the count at zero forever. Two clocks are corrected on the same principle that the fuse and the breaker already follow: the guardian's rate-limit cooldown becomes a monotonic deadline, so a backward correction cannot park an account for the size of the correction, and a note is only given up as a transport failure once it has spent both the 24-hour budget and a plausible number of attempts — the budget alone is one wall-clock subtraction, and a forward correction satisfied it on a note's first failure and dead-lettered bytes that can be the only copy of the funds. The guardian loop also stops letting one account's storage fault end the lap: resolving an account's endpoint was the single await outside the per-account guard, so a failure there skipped every account behind it. The guardian service build is bounded for the probe that runs on a cadence and only for it, since after an eviction the client generation moves and that build is no longer the warm cache read it looks like — it is a fresh genesis fetch at the node that just parked — while the ten transaction-pipeline callers keep the five-minute backstop a user waiting on a write needs. The guardian service's own initializer takes the same post-build ownership check as the probes, being reachable from that unattended loop. A guardian probe's evidence is keyed by its endpoint as well as its account, so repointing an account at a different guardian cannot be silenced for half an hour by a conclusion reached about the node it no longer uses, and a guardian 429 is reported like any other non-eviction failure rather than slipping past the ledger on its way to the rate-limit cooldown — below the threshold it breaks the run of consecutive evictions, and above it a 429 is not the success the fuse is waiting for. The longest unguarded stretch of WASM work on any of these paths is the swap-lineage classification, one round trip per open order, so the loop itself re-checks ownership before each one rather than trusting a check made at its caller's boundary: its length is the user's order count, not a constant. On the service worker's inline path the sync's downstream note-and-vault read is bounded as well, since there it is not the warm re-read it looks like — and after an eviction of the sync itself that read is skipped outright rather than sent to rebuild against the node that just parked, with a liveness check before every one of its WASM calls, not merely after its client build: the note read and the account read are themselves round trips an eviction can land in, and past any of them an abandoned callback's next call would run outside the mutex, borrowing a client somebody else now holds. The delivery sweep behind that read is skipped on the same lap for the same reason — its own holds would each rebuild against the node that just parked. The balance read and the manual note import take the same check at their own mid-flight transitions; for the import that also means the bytes are queued for background retry rather than followed by an unmutexed sync. None of this evidence outlives the node it was collected against: repointing the wallet at a different RPC discards every fuse conclusion and the breaker's window in both realms, because otherwise a fused wallet stayed near-silent for up to another half hour and withheld the successful sync that is the fuse's only way out. Because an eviction leaves it unknown whether an operation landed, it now routes like the transport failures it resembles rather than falling off the classifier's transport-token test: a note import abandoned that way is queued for background retry instead of dropped (for a private note handed over by a dApp, those bytes can be the only surviving copy), and an abandoned transaction no longer shows "No funds moved — please try again" on the very row whose Retry then refuses because it may already have been submitted. Where the abandonment provably landed before the row could build a write, though, the hedged copy is withheld in turn and the row says plainly that nothing was submitted: telling a user to go and check their activity is false there, and it talks them out of the retry the same finding has just made safe. Every bounded hold is also named in its eviction record, since a dozen sites now share one two-minute ceiling and the log could not say which of them parked. Two more sync holds on that path are converted rather than left on the five-minute backstop: the guardian accounts' own sync, which the idle loop no longer awaits either (a hung guardian endpoint held the loop's cadence hostage with its failures swallowed, so the breaker, the fuse and the banner never saw the freeze — and guardian is the wallet's default account type), and the note-import queue's trailing sync, which also moves out of the import's lock, since it followed the queue's own commit and had no business holding the mutex across a 2s sleep. The import itself is bounded the same way and for the same reason, and an eviction of that hold now banks the note's attempt before it propagates: an eviction abandons its callback, so nothing else spends one, and a note that costs nothing to retry is a note the queue can never drain — it would re-enter the same parked hold every lap forever. That queue is durable in its own right now, which it had to become once a note could outlive many failed passes: a private note's bytes can be the only copy of the funds it carries, and the pass used to clear the whole queue on any failure. Each note now retries on its own wall-clock budget — and on a budget matched to WHY it failed, so a note that spent attempts on an outage or on watchdog evictions still gets the misclassification grace the unparseable-note cap exists to provide, rather than being declared malformed on the first error the classifier did not recognise — and one the wallet has genuinely given up on — a sustained outage past a day, or bytes that cannot be deserialized — moves to a dead-letter store the UI can offer back, with the queue refusing to stop carrying it unless that store confirms the write, so a full store or a failed one costs a slow retry rather than the note. Because an eviction abandons a pass rather than stopping it, the commit reconciles what the pass actually imported against the queue as it stands rather than by position, and a pass that has been superseded is refused outright — a zombie settling late can no longer delete notes enqueued after it. A failed import pass still lets the transaction lap proceed rather than aborting it: the queued consume waiting on that note is marked Failed, which is recoverable (nothing was submitted, the note stays claimable, a later auto-consume re-initiates it), whereas aborting the lap stops every send and claim in the wallet and reaps that same consume half an hour later anyway. The behaviour is pinned by unit suites that fail against the unfixed code; a resilience spec drives sustained 429s and a hung sync RPC at the real client in CI, but against the service-worker realm this fix does not change — it is a characterisation test of that path, not #777 protection, and says so. (#777)
- [FIX][all] **A guardian transaction executes at the block its co-signatures were bound to.** Since protocol 0.16 the co-signed transaction summary pins the reference block commitment, but both copies of the wallet's execute pipeline — the in-process one (mobile, desktop, and the extension with the offscreen client off) and the offscreen document — executed at whatever height the client happened to be synced to, so every guardian write raced the chain: a block landing during the guardian's co-sign round-trip made the kernel reject the transaction as unauthorized. A 300-op devnet stress run measured the race directly — 4.5% of sends failed with a ~0.7–0.9s round-trip, 35% once sustained load stretched it past ~1.7s. The proposal already ships the `ChainAnchor` of the block its summary was built at (validated against the signed summary during signing); both pipelines now decode it and pin execution to it, so the summary reproduces however far the chain has advanced. No anchor-age bound is needed on top, and not merely because anchors are short-lived: an anchor tracks the creation blocks of its own request's authenticated inputs, so ordinary chain advancement cannot invalidate it, and every retry path rebuilds the proposal from scratch rather than replaying a stored one. An anchor that cannot be decoded now fails the write outright instead of quietly falling back to the unanchored execute this fixes. Every anchor the wallet captures or decodes is also released once it is done with — it owns a partial blockchain on the WASM heap, and the two places that previously left one to the finalizer were the replace-hot-key proposal and the dApp custom-transaction dry run, both on the guardian path, so they accumulated one per rotation and one per confirm dialog. That release can never mask the failure it runs alongside: it is the guardian error's identity, not just its text, that decides whether a co-signature is retracted. (#784)
- [FIX][all] **A WebAssembly trap can no longer freeze the wallet forever behind a wedged WASM lock.** A trap aborts the wasm-bindgen future without ever settling its JS promise, so the process-wide client lock's holder never released it and every later WASM operation — sync, balance reads, sends, claims — queued forever: a crash two milliseconds into a prove presented as a 35-minute frozen wallet with no error anywhere (the extension's only backstop was the 90-second offscreen teardown; mobile and desktop had none). The lock now recovers on two independent paths: an uncaught WASM-trap-shaped error on the realm evicts the holder within milliseconds, and a five-minute watchdog bounds any silent wedge — a deliberately generous last resort, because several healthy holds (a cold restore's on-chain account probes, guardian round-trips, any hold that continues into other work) are not tightly bounded — the pure-sync holds were on that list too until the #777 entry above gave them their own two-minute ceiling — and pausing entirely, like the offscreen write deadline already does, for sign round-trips and local prove attempts, which may legitimately run unbounded (the guardian pipeline's local proves included). Recovery rejects the wedged operation with a named `WasmClientPoisonedError` and disposes the poisoned client, so the next operation constructs a fresh one instead of inheriting a dead module. Because an evicted operation is abandoned rather than cancelled, the eviction rides the existing outside-kill rails: a killed consume is node-adjudicated before being failed, a killed send records the may-have-submitted crossing so Retry refuses to mint a second payment, evictions are suppressed for a cooldown after a recovery so a corpse's deferred trap cannot cascade into innocent holders, and an evicted flow can no longer corrupt the mutex through the lock-yield used around offscreen proves. Deferring the free is the rule for every path that discards a client, not just for a trap: the routine options refresh and an endpoint-change reset also mark-and-defer while a flow is suspended mid-yield, since a reset takes the mutex that flow is not holding and so could otherwise terminate a client it still had in hand. The locked-vault deferral is narrowed to match: a write whose sign reported a locked vault but whose failure was an abandonment — a watchdog eviction, or an offscreen op killed by a realm teardown — is now Failed with the may-have-submitted crossing recorded (so Retry asks for acknowledgement) rather than being returned to the queue as a fresh write, because the deferral's "a locked vault is strictly pre-submit" argument is exactly what an abandonment breaks.
- [FIX][extension] **A Guardian transaction delegates its proof again instead of hanging until the watchdog kills it.** Guardian accounts are the wallet's default, and on Chrome their proving runs inside the offscreen document — but that copy of the execute/prove/submit pipeline asked the SDK to prove without naming a prover. The empty form selects the SDK's own default-prover fallback, which needs a fully initialized client and so never dispatches from the offscreen realm: the remote prover received no request at all and the wait never ended. Nor was there a ceiling on it, unlike the two paths already fixed, so nothing could turn that silence into the refusal the local-proving fallback needs — the transaction simply held the realm's client lock until the write watchdog tore the whole document down about three minutes later, marked the claim Failed and left it to be retried into the same wall. Claiming a received note was the visible symptom, and it stalled on every attempt. The prove now names the configured remote prover and is bounded exactly as the other two are, so a Guardian claim or send delegates in seconds and a genuinely unreachable prover falls back to proving locally. Alongside it, the offscreen realm now narrates each step of a write it is running — which call it is inside, whether it is waiting on the client lock, and which prover a delegated prove chose — so a write that stops making progress names where it stopped instead of leaving nothing behind; this is recorded only in end-to-end test builds.
- [FIX][all] **A remote prover that stops answering no longer parks a transaction forever.** Delegated proving awaited the remote prover on a call carrying no deadline of its own, so a prover that accepted a proof and then went quiet left that await pending indefinitely — and it does so while holding the client lock, which means every later write queues behind a transaction that can never finish. There was a fallback to local proving, but only a _rejection_ could reach it; silence could not, which is the difference between a prover that fails and one that stalls. A delegated prove now has a client-side ceiling and treats crossing it exactly as it treats a refusal, re-proving locally. For a guardian transaction that is safe in the strongest sense — the wallet drives execute, prove and submit itself, so the ceiling provably expires before anything is broadcast. Claiming a note is bounded too, and is the only other write that can be: it goes through one opaque call that proves and submits together, so a re-run may repeat a submission, but it re-consumes the same notes, and a first attempt that did reach the chain makes the second fail on the spent nullifier rather than move funds twice — the same property that already let this one write retry where others must not. Local proving stays unbounded, since it is legitimately slow on a weak machine and is itself the fallback. This surfaced as claiming a note hanging in the local end-to-end stack (#718): that stack runs a node, sequencer, ntx-builder, prover, guardian, note-transport and two browsers on a two-core runner, its prover aborts any proof exceeding a one-minute ceiling, and it accepts only a single in-flight request — which the ntx-builder is also competing for, since both point at the same prover. So a wallet consume was routinely aborted server-side, and the wallet then waited on a prover that had already given up. The stack's prover now gets a ceiling and a queue sized for a machine it shares with everything else, and the offscreen watchdog's own ceiling becomes settable by the build and is raised for that stack alone; every shipping build keeps the existing one, since the same consume claims three notes inside a 41-second run against devnet on ordinary hardware.
- [FIX][all] **A private send that never reached the note transport service no longer reports itself as delivered.** The recipient of a private note can only ever get it through that hand-over — the chain carries a commitment, not the note — so a failed relay strands the funds, and the wallet reported it as a clean "Sent". It had no record that a relay was owed: durability was delegated entirely to the SDK's retry outbox, which the client writes from _inside_ the relay call and only after it has resolved the transport endpoint, so everything that fails before that point queues nothing while failing exactly like a mid-transport timeout that did queue. The wallet now records the debt before attempting the relay and the outcome after, so an interrupted relay is distinguishable from a completed one, and a send whose note did not get through says so in its history entry with guidance instead of hiding it in a console line. Four ways a note could go missing in silence are closed with it: the relay was dispatched with a 15-second read deadline that could expire while it was still queued behind other work — before it had made a single request — and is now treated as the value-moving operation it is; the decision to relay trusted a wallet-side field that could disagree with the note the transaction actually produced, and now defers to the note; a multi-note transaction waited for chain confirmation between each relay, delaying later notes for no benefit; and a private send on a network with no transport service configured — which includes mainnet — is refused up front instead of landing on chain and reaching nobody. Beyond that, a private note is no longer considered delivered just because the transport accepted it. The acknowledgement is an empty message, so it is not evidence the note was stored — and a note that was acknowledged but never stored is invisible to everyone, with no error anywhere for the wallet to react to. The wallet now re-hands the note to the transport a few times over the following hour unless it can prove delivery, which both detects and repairs that case: a note the service does not hold is accepted and stored at a fresh position, ahead of any cursor the recipient has already passed. A re-push the service rejects means it already holds the note — worth knowing, since it proves the first relay arrived, but not itself a delivery, and not a repair. The sweep stops early when the note is spent on chain, which is the only proof a sender can have that a private note arrived. Sends confirmed that way now say so in their history entry.
- [FIX][ci] **End-to-end network capture no longer misses note-transport traffic.** Requests were classified by host, and the transport is the one endpoint configured as `127.0.0.1` rather than `localhost` — so on the local stack every transport request fell through to `other` and was dropped, silently, from the timeline that exists to explain a note that went missing. Transport is now recognised by its gRPC service path, which also follows a build-time `MIDEN_NOTE_TRANSPORT_URL` override to any host or port, and a captured `SendNote` records which note it carried.
- [FIX][all] **A transaction that fails after it has been submitted is never sent a second time.** The local-prover fallback re-ran the whole write — execute, prove, submit and apply — whenever the delegated (remote) prover path threw, including when the throw came from `submit` itself or from the local store update that follows it. For a send that meant a second broadcast with a freshly built note, so the user was debited twice for one transfer; and because the retry's error replaced the original, the "already accepted by the node" classification stopped firing, the row was marked Failed, and a user-initiated Retry could send a third time. Each write now declares its point of no return and the fallback only ever re-proves an attempt that provably never reached submit.
- [FIX][all] **A guardian transaction whose co-signature is rejected at execution is retried rather than failed.** The guardian co-signs a transaction summary bound to the account state and reference block it saw. If that state moves before the wallet's own execute recomputes the summary, the account's auth procedure rejects the signature and the write dies with "transaction is unauthorized" — not a refusal of the transfer, and it clears the moment a fresh proposal is signed against current state. (This is the backstop, not the cure. The cure is to pin execution to the proposal's own chain anchor, so the signed summary reproduces at the height it was signed for and a block landing mid-flow cannot invalidate it — the SDK accepts that anchor on execute and the proposal already carries one, so it costs no extra round trip. That work is in review as #786; this arm is what recovers the send until it lands, and it should shrink rather than stay once it has.) Nothing reaches the chain when it happens, because execution is where it stops and both leaves execute before they prove and submit, but the row was being marked terminally Failed and left for the user to retry by hand. Over a 300-send devnet run against a guardian under sustained load that cost 47 sends, 14% of them, with the rate tracking how long the guardian took to answer: 4.5% while the proposal-to-execution window stayed under a second, 35% past 1.7s, climbing through the run as guardian round-trips grew from 650ms to 1.37s. Sends, swaps, claims and generic executes now return to the queue on a jittered 15-54s cooldown and keep retrying for three minutes from the first such failure — extended by any unrelated backoff the row waits out along the way, since a single rate limit from the same overloaded guardian can park it for longer than the whole budget — after which the row fails carrying the reason it actually got rather than ageing out as "expired" half an hour later; the spread is wide because the trigger is a guardian already under load, and marching every affected wallet back in lockstep would help hold it there. The transaction stays in progress for that window instead of failing within seconds. An earn deposit is deliberately not among them: its caller reads results back off the finished row, so requeueing would leave that caller waiting on a row that will not finish this cycle, and its collateral note is bound to an allocator mandate rather than being a transfer that can simply be rebuilt — failing it, and letting the user re-initiate, is the honest outcome. The gate is the error text rather than the row's `stage`, unlike the neighbouring rate-limit arm, and that difference is what keeps it alive: on the shipping path the leaf runs in the offscreen document, whose stage stamps are replayed as timing-only so they never author `stage`, leaving a row that died mid-execute still reading `'sending'` — a stage-gated arm would not have fired once in production. A lock-recovery eviction is excluded even when its cause chain carries this exact reason, because the property the retry rests on — execution stopped the write short of submit — is the one thing an eviction does not give you: it rejects the caller while the pipeline it abandoned runs on and can still submit. The failures were invisible to the stress suite for a related reason, since its driver counts a send when the UI says "initiated" and never revisits the outcome; it now reconciles each wallet's own transaction table at the end of a run and asserts that each wallet's balance moved by exactly what the other wallet's completed sends account for, allowing only what that wallet's own possibly-submitted failures could explain — and capping how many of those a run may accumulate — so a total that conserves while value sits on the wrong side no longer passes.
- [FIX][all] **An AggLayer (Slow) bridge claim is bound to the bridge it is claiming for.** The lookup took only the destination address and returned the newest ready deposit, so with two bridge-outs to one L1 address the wallet claimed the wrong one — the smaller amount was claimed on L1 while the larger row was marked claimed and its funds left unclaimed. It also ignored the deposit's own claim hash, so an already-claimed deposit stayed selected forever and every further claim reverted. The deposit is now matched by the row's Miden transaction id, already-claimed deposits are excluded, and a row that cannot be told apart from a sibling is left on "Claim Pending" rather than claiming someone else's deposit.
- [FIX][all] **A claim the user just approved is no longer swallowed by auto-consume's retry backoff.** After one failed attempt, a dApp consume the user then approved — and the "Reclaim funds" button on a failed bridge — queued nothing for up to a day and answered with the previous attempt's failed transaction id, so the dApp reported the old error for a request that was never made. Both paths are now treated as the deliberate user actions they are, exactly like the wallet's own Claim and Retry buttons.
- [FIX][all] **A dApp can no longer have the wallet sign with an account it is not connected to.** `signBytes` authorized the request against one field and chose the signing key from another, and nothing compared them — so a page holding a live session for one account could pass a different account's key and receive a signature made with it, including the signature form that authorizes a transaction. No approval surface could contradict it: the extension's signature screen shows no account, and the mobile/desktop sheet's account row is the very field being substituted, so it rendered the attacker's account and read as consistent. The signing key is now bound to the connected account.
- [FIX][all] **Private-note disclosures are limited to the connected account.** A dApp granted private-note access received every private note in the wallet — id, nullifier, sender, state and amounts — including accounts the user never connected, while the approval sheet promised "for account <the connected one>" (and with automatic permission there is no sheet at all). The read is now scoped to the connected account.
- [FIX][all] **A consume approval shows the note the wallet will actually consume.** The sheet rendered the faucet, amount and note type the requesting page declared, none of which can influence execution — only the note id is used — so a page could show a large, familiar-looking claim over a note carrying something else entirely. The rows are now derived from the resolved note, and a request whose note the wallet cannot resolve is refused instead of prompted.
- [FIX][all] **The verified asset view is back on dApp custom transactions.** On the 0.16 SDK the dry run that produces it now errors for any account that is already fully authorized — that is, every ordinary single-sig account — so the confirm screen silently fell back to the page's own declared numbers and reported the loss as a temporary hiccup. It falls back to a local execution and shows what the transaction really moves.
- [FIX][mobile][desktop] **Choosing biometric protection can no longer create a wallet with no password at all.** If the hardware check flipped to unavailable between the onboarding screen and wallet creation, the vault key was encrypted under an empty password: every secret it holds would be recoverable from a storage dump with no secret, and the user could never unlock again because no password was ever chosen. Wallet creation now fails with an explicit "password is required" error instead, the same way the import-from-client path already did.
- [FIX][desktop] **Guardian accounts work on the desktop app.** The desktop backend adapter implemented 16 of the 27 in-process request types and silently dropped the other 11 — the entire guardian surface — so guardian sync, the device-key rotation gate and "Reveal guardian keys" failed with an error that read as a wrong password. Both in-process adapters now dispatch through one shared handler, and an unhandled request type reports itself by name.
- [FIX][all] **An abandoned Epoch earn deposit no longer mints an orphan collateral note on standard accounts.** Once the caller gives up on a deposit, its queued row stayed pickable, so the collateral note was still submitted to the allocator with no live intent behind it — locking the funds until the note's reclaim height while the activity row read "Deposited to lending". The Guardian path already refused this; both paths now do.
- [FIX][all] **`yarn build:devnet` and `yarn build:desktop:devnet` work again.** Both still called webpack and an undefined cache-clearing script after the migration to Vite, so the documented devnet command deleted `dist/` and then failed. (`yarn build-all` is broken independently and still is: `run-s build:*` reaches `build:storybook` first, and there has been no `.storybook/` config since the Vite migration — build the platforms you need explicitly.) They now wrap the Vite extension/desktop builds, matching the mobile devnet script.
- [FIX][extension] **Developer-settings custom networks work again on Chrome.** The extension runs its Miden client inside an offscreen document, a separate JS realm that never loaded the saved endpoint override — so it built its client against the build-default node and, because the network ID also decides the address prefix, encoded account and faucet ids under a different bech32 prefix than the service worker did. A wallet configured on a custom network therefore did all of its real work (sends, syncs, note delivery) against the default node, and native (MIDEN) notes never auto-consumed because the two realms disagreed about the faucet's address. The offscreen realm now loads the override before it can create a client or encode an id, and saving an override re-hydrates it there too instead of only in the service worker.
- [FIX][all] **Every transaction step shows a duration again, on every account type.** Value-moving writes run inside the offscreen document by default on the extension, which had no way to report the execute → prove → submit boundaries back to the transaction row — so the progress screen lost its per-step durations, on standard sends and on Guardian accounts alike (Guardian being the default, this was the common case). Stage stamps now ride the same op-scoped reverse channel the mid-op signing callback uses, so the timings are identical whether the write runs offscreen or inline. A stamp is telemetry and can no longer fail a transaction on any path.
- [FIX][all] **A completed transaction keeps its step durations, and a retry times its own attempt.** Completing a transaction could hand back a copy of the row captured before the run started, wiping every boundary recorded during it; and retrying a failed transaction kept the failed attempt's boundaries, so the new attempt displayed the old one's durations.
- [FIX][extension] **The connectivity banner stops showing the wrong state.** The banner's snapshot lives in one shared storage key but was written wholesale by two separate JS realms — the service worker (node/network, plus the prover mark from a requeued Guardian send) and the offscreen document (prover, around every write it proves) — neither of which had seen the other's issues, so the last writer replaced the whole picture and a successful offscreen prove could clear a real "node unreachable" banner. The offscreen realm now reports each observation to the service worker, which applies it one category at a time and stays the only writer; and the service worker seeds its snapshot from the stored one at startup, so a banner raised before a restart is still cleared by the next successful sync or prove instead of latching on screen.
- [FIX][all] Restored three translations that were blank: "Rotate Guardian" in Portuguese and Russian, and "Custom Guardian" in Ukrainian.
- [FIX][all] **Retrying a failed Fast (Epoch) bridge no longer mints a second, orphaned collateral note.** An Epoch Miden→EVM send is a recallable P2IDE note to the allocator whose only meaning comes from an Epoch intent that is already abandoned once the row fails — so re-queueing it re-ran just the Miden leg, locking a second transfer at the allocator until its reclaim height and then reporting "Bridged to EVM". Failed Epoch bridged sends are now non-retryable (the user re-initiates from the bridge flow); Slow (Agglayer) bridges, which carry a self-contained request, still retry.
- [FIX][all] **Retrying a transaction the node reports as already landed now reconciles it instead of erroring.** The "already landed → complete the row" guard wrote through a helper that refuses to touch a finalized row, so it threw on every execution: the user saw "Something went wrong" and the row stayed Failed forever even though the funds had left the account.
- [FIX][all] **A post-submit failure on a Fast bridge no longer hangs the bridge and shows a false success.** A `bridged-send` marked Completed with no transaction result left the Epoch note builder waiting forever — its intent never submitted, the collateral note stranded — while the activity row read "Bridged to EVM". Such rows now fail like earn deposits do, and the wait itself can no longer hang on a result-less row.
- [FIX][all] **Apply-after-submit failures are detected again on the 0.16 SDK.** The wallet classified them by an `errorCode` property the SDK never sets, so a transfer that reached the chain but failed its local store update was marked Failed; the user would re-send and pay twice. Classification now matches the SDK's actual error text (and still honours a code when one is present).
- [FIX][all] **The "returns to sender by" warning on recallable notes works again.** Miden 0.16 moved the reclaim height inside the P2IDE note's storage, so the wallet read an unrelated field and silently hid the deadline on every recallable note.
- [FIX][all] **dApp transactions can only execute as the account the site is connected to.** A connected page could name any of the user's accounts as the sender of a send or custom transaction and the wallet would sign with it, while the approval screen — which never showed a sender — looked exactly like the connected account's own transfer. The executing account is now checked against the session and shown on the approval screen.
- [FIX][desktop] **Pages in the desktop dApp browser can no longer forge wallet requests.** Every remote origin loaded there was granted Tauri IPC, so any visited site could emit a wallet request tagged with another dApp's origin (matching that site's stored session and raising a genuine-looking approval) or close the wallet window. The injected bridge needs no IPC — it uses the navigation intercept — so the grant is removed.
- [FIX][all] **Locking the wallet now cuts off dApp access to private notes and balances.** The unlocked-state assertion only checked that the wallet had been initialized, which a locked wallet still satisfies, so a site holding automatic private-data permission kept receiving notes and balances after Lock.
- [FIX][desktop] **Desktop wallet creation and seed import work.** The desktop backend adapter dropped the wallet type and shifted every remaining argument by one, so onboarding wiped storage and then failed — and the same call on the forgot-password route destroyed an existing wallet before failing.
- [FIX][all] **Forgot-password recovery no longer assumes a Guardian wallet.** Recovering a non-guardian wallet wiped local storage and then failed with "No Guardian accounts found", because only the Guardian key namespace was searched. The flow now asks which recovery method to use, like onboarding does.
- [FIX][mobile][desktop] **`signBytes`, private-note import, and on-request data disclosures now prompt instead of hanging.** These dApp requests had no non-extension path, so on iOS, Android and desktop they silently never settled until the page's own five-minute timeout.
- [FIX][mobile][desktop] **A transaction interrupted by an app kill is recovered at next launch.** With no service worker, nothing reaped or resumed an orphaned row, so it sat "in progress" indefinitely with its inputs reserved until the user happened to start another transaction.
- [FIX][all] **A Slow (Agglayer) bridge that reached the chain no longer loses its L1 claim button.** A post-submit failure on the local store update marked the row Failed, and the activity detail hides the whole claim panel — the deposit tracker, Connect wallet and Claim Asset — on a failed row, so the only in-wallet path to claim the bridged funds on L1 disappeared while the tokens sat in the bridge; Retry could not help either, because it re-submits the same pre-built note the node has already accepted. The rule that fails such a row exists for the Fast (Epoch) route, whose caller waits on the row's result, and it now applies only to that route.
- [FIX][all] **A Slow (Agglayer) bridge shows its real token again.** The activity row recorded the bridged faucet in the SDK's hex form while every other row (and every reader) uses the bech32 form, so the entry rendered as "Unknown" with a fallback decimal count, and the token's own history omitted the bridge entirely.
- [FIX][all] **Fast (Epoch) bridge and Earn deposit rows show their token on every network.** Both recorded the faucet under a hardcoded testnet address prefix, so on devnet, mainnet, the local E2E network, or with a developer endpoint override the row read "Unknown" and vanished from that token's history. The on-chain note was never affected.
- [FIX][all] **A multi-note custom transaction reports the amount it actually moved.** The activity row kept only the last note's total instead of the sum, and counted one faucet per note — so a two-note transfer recorded half the value and lost its send/claim label.
- [FIX][mobile][desktop] **The Guardian `update_guardian` hardening completes in the session that starts it.** The self-heal enqueued the transaction and then nudged only the extension service worker, so off-extension the row sat pending in Activity, never progressed, and left the account un-hardened until the next app launch.
- [FIX][all] **The private-data approval no longer tells the user the permission cannot be revoked.** Settings → Authorized DApps removes the session, and the stored permission with it; the old wording discouraged the one remedy that exists.
- [FIX][all] **A send or swap that may already be on chain can no longer be re-sent by Retry.** The node-verified double-send guard was keyed on the transaction id, which only the success path ever records — so every row failed by a route that kills a transaction from outside its own write (the stuck reaper, the cold-start sweep, an offscreen deadline kill, a Cancel pressed mid-flight) reached Retry with nothing to check, and re-queueing it built and submitted a second, fully valid transfer. Retry is now refused for those rows, on both the affordance and the model, and the reason is surfaced instead of silently sending again.
- [FIX][all] **The wallet locking mid-signature no longer stalls every queued transaction for half an hour.** The deferral that keeps a locked-out claim from failing left its row marked in-progress, and the processing loop skips a cycle whenever any row is in that state — so nothing the user queued afterwards ran until the stuck reaper failed the deferred row (30 minutes on desktop, 2 on mobile), which is the terminal note-claim failure the deferral exists to prevent. The row is now genuinely re-queued and picked up after unlock.
- [FIX][all] **Every language ships its current translations again.** The flat locale bundles the UI renders from are produced from the translated files by a script that nothing called, so they were frozen at whenever someone last ran it by hand: Spanish carried 469 of 924 keys and rendered "Close" as "Cerca" (nearby), the "reset permissions for {site}?" prompt never named the site, and the whole Earn and bridge surface fell back to English in all 12 non-English languages. The flatten now runs as part of the translation job, the bundles are regenerated, and the parity gate checks the file the UI actually reads. A key whose English text changed since its last translation falls back to English rather than shipping a translation of superseded copy.
- [FIX][mobile] **dApp signature, note-import and on-request data prompts appear again.** They were filed under a session slot the mobile browser never renders, so no sheet appeared and the request never settled — and because one queue serializes every dApp request across every site, the first such request blocked all dApp activity until the app was restarted. The prompts now route to the session that raised them, and a prompt nothing answers auto-declines after two minutes instead of wedging the queue.
- [FIX][mobile][desktop] **The "DApps Interaction" switch actually disables dApps.** It was enforced only on the extension's content-script bridge; the in-app browser and the desktop dApp window dispatch straight into the wallet, so turning it off changed nothing there. The check now sits at the single dispatcher every transport funnels through.
- [FIX][mobile] **Revealing a private key, Guardian cold key or hot key blocks screenshots and screen recording.** The recovery-phrase screen was guarded; these screens — carrying material of the same sensitivity — rendered into an ordinary text field, so a screenshot, the task-switcher thumbnail, or any live recording or screen-share captured the key.
- [FIX][desktop] **A dApp session no longer covers every other port on the same host.** The desktop browser derived the requesting origin without the port, so a site approved at `http://localhost:5173` was reconnected with no prompt — and its automatic balance and private-note access honoured — for anything else served from `localhost`. Origins now include the port, and a page with no meaningful origin (`file:`, `data:`) is refused rather than bucketed together.
- [FIX][extension] **An open wallet tab keeps receiving updates after the service worker restarts.** The tab's subscription was bound to the port that died with the worker and nothing re-attached it, while requests kept working — so the page looked healthy but never saw another lock, sync-complete or note-claim event. Locking from the popup left an open tab rendering the unlocked wallet until it was reloaded.
- [FIX][mobile][desktop] **A transaction orphaned by an app kill is cleared at launch instead of 30 minutes later.** Startup recovery only aged rows out at the stuck-transaction threshold, and the processing loop skips a cycle while any row is in progress — so a crash mid-send froze every later send, claim and swap at Queued for the rest of that window. A fresh app process is a cold start, so the sweep is now unconditional, matching the extension.
- [FIX][all] **An Earn deposit's activity detail shows From and To the right way round.** The transfer direction was keyed on `send` alone, so a deposit — which moves collateral out to the Epoch allocator — rendered as if the allocator had paid the user. A user-cancelled bridge was reversed for the same reason.
- [FIX][all] **Retry can no longer send a transfer twice after a failure the wallet could not classify.** The guard that hides Retry on a send or swap whose submit may already have landed matched a fixed list of failure reasons, all of them written by routes that kill a transaction from outside its own pipeline. A write that failed from _inside_ its own pipeline after the submit had landed — on Chrome, the offscreen document going away right after a multi-second proof, so the reply never came back — produced a reason on no list, and the row was offered as retryable with the recipient's note already on chain and the account already debited. The guard no longer looks at the reason at all: a send or swap that left the queue without a transaction id to ask the node about is not replayable, and the user re-initiates from the send or swap flow instead. A lost reply is also now reported as the same kind of failure as the document closing, which it is.
- [FIX][all] **A claim that already reached the node is no longer reported as interrupted.** Two of the note states the client writes the moment a consuming transaction is submitted and applied — "processing", awaiting its block — were read as "not consumed", so the stuck-transaction sweep failed the claim, the note disappeared from the claimable list because it _was_ claimed, and the phantom failure counted toward the auto-claim backoff for that account. Most visible on Guardian accounts, where the claim pipeline releases its lock and syncs for several seconds inside exactly that window. Such a row is now left alone until the note settles.
- [FIX][desktop] **A site in the dApp browser can no longer choose what the address bar says.** The browser chrome was drawn inside the visited page's own document, so one line of page script could relabel it with any hostname — and nothing else on screen showed the real one. The address is gone from the page-drawn toolbar (which keeps only its navigation buttons) and the window's title bar now shows the origin the wallet itself computed, updated on every page load.
- [FIX][mobile] **A restored dApp bubble is authorized as the page it actually loads.** A session recorded the URL it was opened with but the origin of the last page it navigated to, and a cold restore loaded the former while authorizing requests as the latter — so a site could hand its own page another dApp's approved session, including automatic access to private notes and balances. The two now move together on every navigation, a restore derives the origin from the page it is opening, and requests from a load the wallet has not yet confirmed the origin of are refused rather than attributed to the previous page.
- [FIX][extension] **The connect prompt no longer promises automatic private-data access it will not grant.** A request for automatic permission with no data categories selected — the default when a site names none — was shown as "Automatic" above an empty list, while the wallet in fact prompts for every such request. The popup now derives that copy from the same helper the mobile and desktop prompts use.
- [FIX][mobile][desktop] **A claim of a note somebody else took is no longer reported as "Received".** The stuck-transaction sweep accepted "the note is spent on chain" as proof the claim landed, but that state is also what a recallable note looks like after its SENDER recalls it, or a public note after another wallet consumes it first — so the claim's history row settled as "Received <amount>" for funds that never arrived, with the balance unchanged. Only a note the wallet can prove its own transaction consumed completes the row now; anything else fails it after the usual grace window, which is safe (a re-claim collides on the spent note and the next sync reconciles). This sweep is the only claim reconciler on iOS, Android and desktop — the stricter rule it now shares already governed the extension.
- [FIX][desktop] **A page in the dApp browser can no longer call the wallet's own native commands.** Tauri skips the capability check for an app's own commands unless the app ships a permission manifest, and this one did not — so every command was reachable from any site the user visited, including the one that deletes the Secure Enclave key wrapping the vault. On a Touch-ID-only wallet that key is the only way in, so one line of page script, with no biometric prompt, could make the mnemonic and every account key permanently unrecoverable; the hardware encrypt/decrypt pair was likewise callable on attacker-supplied input. The commands now carry a permission manifest scoped to the wallet's own window, and each additionally refuses any request that did not come from it.
- [FIX][desktop] **A dApp can no longer approve its own connections and transactions.** The approval prompt was injected into the requesting page's own document, so the page owned the dialog: a few lines of script could tick the private-data acknowledgement and click Approve the instant it appeared, granting standing access to private notes and balances and executing a transfer to an attacker's address with no user interaction beyond having the page open. The verdict also travelled back over a channel the page could drive itself. The prompt now renders in the wallet's own window — the same component mobile has always used — and the wallet window is raised so it cannot be missed.
- [FIX][desktop] **An embedded third-party frame can no longer act as the dApp that embeds it.** Wallet requests arrived over a navigation the webview reports for every frame, but were attributed to the top-level page's origin — so any cross-origin iframe on a connected dApp (an ad slot, a chat widget, a compromised CDN script) could send a transaction as the connected account, with the approval naming the innocent host site. The bridge now carries a per-window secret that only the top-level document ever receives, and requests without it are dropped.
- [FIX][desktop] **The macOS and Linux desktop app can talk to the Miden node again.** Those builds render in WebKit, so the SDK picks its classic worker, which fetches the WASM module from a fixed relative path the desktop bundle never emitted. The fetch 404'd and every SDK call rejected for the whole session: no sync, empty balances, and every send or claim failing behind a "cannot reach the Miden node" banner. The desktop build now emits the module where that worker looks for it, as the mobile build already did. Windows was unaffected (its webview takes the other worker path), which is why no Chrome-based test caught it.
- [FEATURE][all] **A "Guardian unreachable" connectivity banner appears when the guardian stops responding.** After a threshold of consecutive sync failures caused by the server being down (connection refused / timeout / 5xx — never a 401 or 429, which prove it's up), the home connectivity banner — the same surface as the node-sync-failed banner, slotted between node and prover in its priority — offers a Switch Guardian action that routes to Rotate Guardian; the direct on-chain switch fallback makes that rotation work while the operator stays down. Any response from the guardian clears it, and a dismiss lasts for the session.
- [FIX][all] **Switching to a custom-URL guardian no longer raises a false "Guardian changed" prompt.** The drift check only recognized built-in operators, so a deliberate switch to a custom endpoint — whose completion had already persisted the correct URL — was flagged as an unidentified out-of-band switch every time. The check now verifies the account's stored endpoint against the on-chain guardian commitment before asking the user for a URL it already holds.
- [FIX][all] **Guardian rotation no longer requires the outgoing guardian to be reachable.** The switch flow used the current guardian as a coordination mailbox (proposal storage + signature collection), so a dead or unreachable operator blocked the very rotation meant to escape it — even though the on-chain switch only needs the wallet's two on-device keys. When the outgoing guardian is unreachable — or answers that it cannot co-sign for this account at all, which is what a half-finished earlier rotation leaves behind and what the settings page surfaces as "Needs attention" — the wallet now builds the update-guardian transaction locally, signs it with the hot and cold keys, submits it on-chain (execution pinned to the summary's chain anchor), and registers the post-switch state on the new guardian directly. A guardian that answers with an ordinary rejection (an auth failure, or a conflict with a still-pending proposal) keeps the normal coordinated flow, and so does any failure once the co-signature is already back — at that point a transaction may be in flight, and a second one is never worth the risk.
- [FIX][all] **The Guardian settings page's status pill now reflects reachability.** It rendered a green "Online" whenever an endpoint was configured — through an entire outage. It now subscribes to the sync loop's guardian-outage flag (the signal behind the home connectivity banner) and flips to a red "Offline" pill while the operator is down. "Online" additionally requires positive evidence rather than merely the absence of an outage: the flag arms only after several consecutive failed ticks, so a pill driven by it alone still claimed "Online" for the first seconds against an operator that was already dead — and because both the flag and the last-sync stamp are per-realm, that window reopened on every popup reopen. Until a guardian sync actually lands the pill reads "Checking", and the "Last sync" row agrees with it instead of reporting a wallet-wide sync the guardian had no part in.
- [CHANGE][ci] **The infra-resilience E2E suite now covers a fully offline outgoing guardian.** A new spec refuses every connection to the old operator for the entire rotation and asserts the switch still reaches Completed — with the old guardian hard-down the proposal flow cannot succeed, so a completed switch plus a fired fault proves the direct on-chain fallback ran. The network-fault seam gains the `networkFaultHits()` false-green guard the guardian seam already had, so a spec can prove an armed whole-infra outage actually bit.
- [FEATURE][all] **The guardian picker marks operators that are offline.** The choose-guardian screen (onboarding, recovery, and Rotate Guardian) now pings every built-in operator's unauthenticated `/pubkey` endpoint in parallel and shows an "Offline" banner on any card whose operator doesn't answer within the deadline — so the user doesn't create or switch onto a guardian that can't co-sign right now. The verdict is advisory: an offline card stays selectable, since the outage may be transient.
- [FIX][all] Delegated proving no longer wedges a transaction on a slow machine. The wallet now names its own remote prover — and its gRPC deadline — instead of relying on the SDK default, whose ~10s deadline a transaction proof can exceed under load; missing it failed the prove and re-proved on the unbounded local prover while holding the WASM lock, so claims and sends could stall indefinitely rather than finishing seconds later. (#718)
- [FIX][all] Guardian operator cards on Choose/Rotate Guardian now shrink to fit narrow viewports instead of being clipped at the screen edges. (#744)
- [FIX][all] Bridge transaction details: hero amounts now use the adaptive 2-decimal formatter (also for quoted output amounts), and the page can no longer scroll horizontally when a value is long. (#752)
- [FIX][all] Import Wallet seed-phrase screen: consistent vertical gap between the word grid and the Continue button on short viewports. (#741)
- [FEATURE][all] Transaction details Notes card now shows the note type (Private/Public) for sent and consumed notes, and lists the note ids a claim consumed. (#732)
- [FEATURE][all] Batch claims that consume several tokens now show every asset's summed total ("+20 A, +10 B") on the activity row, the generating-transaction pill and the details hero, instead of only the first token.
- [FIX][all] **Exporting a wallet file no longer reports success while writing nothing.** The export converted exactly two named fields out of the transaction row, so any other value JSON cannot represent — a swap's requested amount or order id, and now a batch claim's per-asset totals — threw before anything was written, while the export screen still showed "Wallet File Exported!". The user believed they held a backup they did not hold. Such values are now walked and tagged wherever they sit in the row rather than listed field by field, and a failed export says so on the screen instead of claiming success. The matching import path was hardened the same way and now replaces the transaction table in a single database transaction, so a malformed file leaves existing history intact rather than deleting it first and failing halfway; no screen calls it yet, so this is groundwork for restore rather than a behaviour change. Its decoder was also tightened: a byte field with a gap in it, or a value outside 0-255, used to be reshaped into a different blob without a word — a wallet file that restored something other than what it stored — and is now rejected; and an object in the file that happens to look like the wallet's own marker for a large number is now preserved as itself instead of being read as a number, which for a non-numeric one aborted the whole import.
- [FIX][all] **A token the wallet has never held no longer shows a made-up amount, and can no longer be sent by one.** Without the token's metadata there is no scale to convert its raw units by, and the placeholder used previously assumes 6 decimals — which renders an 18-decimal token a trillion times too large. Such an asset is now named without a quantity everywhere a quantity was shown: the home token list, the portfolio total and its 24h change, the token detail screen, the activity list and transaction details, a swap's requested and filled amounts, an earn withdrawal, the in-progress pill and the success receipts. Its fiat estimate is withheld rather than derived from the invented quantity. **The send flow now refuses such a token outright** rather than converting the typed amount by the guess — an amount entered against placeholder decimals is a different quantity from the one on screen, and it leaves the wallet irreversibly; the review screen refuses too, since it is reachable directly by URL. The transaction a dApp asks you to approve shows `?` in place of an amount it cannot scale, rather than a confident wrong number on the one screen where being wrong costs money. The unknown scale is recorded on the metadata itself, so it survives the hops between the service worker and the page that previously rewrote the placeholder into a record that read as fact — including for wallets that had already cached one. A record that has no scale is also no longer treated as an answer: a lookup that fails because the network was briefly unavailable used to be cached as though the token were unresolvable, which stopped the wallet ever asking again, and a cached guess now yields to real metadata the moment it arrives. An unresolved token is still listed with its balance withheld rather than dropped from the list. The pending-notes screen still shows these amounts and is not covered by this change.
- [FIX][all] The "Reclaim funds" button on a failed fast bridge is reachable again — the reclaim block height never reached the detail view, so the button could not render for anyone.
- [FIX][all] **A restored wallet file can no longer make the wallet act on its behalf.** A backup is a record of what happened, but the transaction queue, the Retry button, the bridge and earn reconcilers, the swap-order list, the claim de-duplicator and the Recent-recipients list would all have treated a restored row as live work — a tampered file could have the wallet sign a transfer nobody authorised, poll a stranger's addresses, or present an attacker's address as one the user had sent to before. Restored rows are now marked at import and refused by everything that signs or queues. Display stays faithful to the file with one exception: a row the backup left mid-flight is shown as failed rather than as still in progress, because nothing can carry it further. No screen restores a file today, so this closes the hole ahead of the feature rather than in it.
- [FIX][all] Activity history no longer stops after the first page. Paging read the page size as an end position rather than a count, so every scroll past the first batch came back empty and the list appeared to end there.
- [FIX][all] A negative amount now formats as a number again ('-0.00005' rather than '0.000-5'), which affected reclaimed and reversed amounts wherever they are shown.
- [FIX][all] The Import Wallet screen now explains which words it does not recognise, instead of showing the untranslated string 'importSeedPhraseError'.
- [FIX][all] A transaction whose completion time is missing or unreadable no longer takes the whole activity list down with it; the row is grouped under today instead.
- [FIX][e2e] `killBrowser()` specs no longer fail with "Object with guid handle@… was not bound in the connection": the screen-change capture handler polls a handle-free boolean instead of `waitForFunction`, `killBrowser()` closes the page before the browser, and the async page-network capture listener is guarded.


### Fixes

- [FIX][e2e] The mobile screen-capture poll no longer turns a slow WebView into a failed spec. It read the app's screen key on a fixed 250ms interval without waiting for the previous read, and on iOS each read is a CDP `execute_script` against the app's single JS main thread — so whenever a read outlasted the interval, ticks enqueued faster than the WebView could drain them and the spec's own evals timed out behind the backlog as "WebView/RWI wedged". Observed on a `Mobile E2E (devnet)` failure that left 1047 unanswered calls outstanding against one wallet while its sibling stayed under 155. The poll now waits for each read to finish before scheduling the next, so at most one is ever in flight and a slow WebView is reported as itself.
- [FIX][extension] **The "already your current guardian" error on Rotate Guardian is no longer cut through by a horizontal line.** The message was rendered outside the screen's scroll container and pulled up over its bottom edge; it now sits inside the screen, directly above the Continue button. (#745)
- [FIX][mobile] **A dApp could open, report itself as ready, and never appear on screen.** The dApp browser hands the native webview the rectangle it should draw into, measured from the slot the React screen reserves for it. Measurements taken while the screen is still sliding in are deliberately thrown away, because they read the mid-slide position and would place the webview about 32pt to the right before it snapped back. But nothing guaranteed a later measurement ever happened: the observer that watches the slot only fires when the slot changes _size_, and sliding does not change size, so once three fixed timers had come and gone the last word was whichever of them was thrown away. The slide-in also holds its starting offset from the moment the screen mounts rather than from when the animation begins, so on a busy device — where the animation can start late — all three timers could land inside it. When that happened the wallet never sent a rectangle at all, and since it only makes the webview visible once it has one, the dApp stayed in the foreground as far as the app was concerned while showing nothing, with no way to recover short of leaving and coming back. The wallet now keeps re-checking until the slide settles instead of giving up after the third try. That re-checking is bounded, and when the bound is reached it measures anyway rather than giving up: a rectangle that may be ~32pt off is strictly better than none, because it puts the dApp on screen and restores the leave-and-come-back recovery, which was itself unavailable for exactly as long as no rectangle had been reported. This is also what has been failing the Android dApp-browser E2E on `main`, in two of its last three runs: the driver waits for a dApp that is foreground, loaded _and_ has a slot rectangle, and timed out with everything true except the rectangle.
- [FIX][all] **Sending a callback-enabled asset (e.g. a bridged token from a transfer-policy faucet) no longer fails with "failed to remove the fungible asset from the vault".** The send paths rebuilt the outgoing asset from faucet id + amount, which always carries the default Disabled callback flag; that flag is part of the asset's vault key, so the transaction addressed an empty vault slot and the kernel rejected it. Every send (guardian recallable, offscreen-prover, and the high-level path) now builds its request through one shared builder that derives the asset from the sender's actual vault key, flag included. Retrying a previously failed send also rebuilds its request instead of replaying the frozen bad one — except where the failed attempt could already have reached the chain, since rebuilding draws a new note serial and the original note id is the only thing that makes the network reject a duplicate payment. That exception now also covers a send that was cancelled while it was still running: cancelling (or the stale-transaction sweeper) marks the row failed but does not stop the work already in flight, so the transaction could still go out afterwards while the row's record of how far it got stayed frozen at the moment of the cancel. Retrying such a send would have read it as never sent and paid the recipient a second time; each send now records for itself that it is about to broadcast, independently of that frozen record. A send is also now refused outright if the requesting site omits the note type or supplies one the wallet does not recognize, rather than quietly falling back to a public note, and an amount too large to represent is rejected instead of silently wrapping to a smaller one.
- [FIX][all] **A connected site can no longer spend from an account you did not connect to it, or set a recall window other than the one shown.** A site's session authorizes exactly one account, but the account to debit was taken from the request itself and never checked against it — so a site connected to one account could name another as the sender and spend from that instead. The approval sheet shows the amount, recipient, token and note type but not the sender, so there was nothing on screen to give it away. Separately, the "recallable after" window was passed through to the note unchecked, and because it is stored on chain as a 32-bit block height, values that do not fit are truncated rather than refused: a window just past that limit wrapped around to zero, making a note the sheet described as recallable in years reclaimable by the sender immediately — a recipient who did not claim it in time could lose the funds — while a negative window wrapped the other way and put the sender's own recall billions of blocks out of reach. Both are now rejected before you are asked to approve anything. Also fixed: for a token that has no decimal places, the amount on the approval sheet was rendered a hundred times smaller than the amount being sent.
- [FIX][all] **A connected site could still spend from an account you never connected to it, by a second route, and cancelling a send mid-flight could still pay twice.** Three gaps left by the previous round's fixes. The sender check added last round guarded only one of the two ways a site can ask for a send; the general-purpose transaction request reaches the identical send flow while checking only that the site is connected at all, so naming a different account as the sender still worked — and since the approval sheet does not show the sender, nothing on screen gave it away. The check now sits in the shared step both routes go through, next to the other two checks that were already placed correctly. Separately, the reclaim window was validated only for sends coming from a site, while the wallet's own send screen turns a date you pick into the same value and passed it through unchecked, so a distant enough date still wrapped around into a note reclaimable immediately; that validation moved to the one place every send passes through. Finally, recording that a send is about to go out only happened once the transaction reached the network — but cancelling it minutes earlier, while it is being proved, does not stop the work, so retrying in that window still rebuilt the transaction and paid a second time. Cancelling a send that is already under way now records on its own that the transaction can no longer be assumed unsent, and the retry re-reads that record at the moment it decides rather than from a snapshot taken before its network check. Two more ways that record could be missed are closed with it. On phones it was measured against the wrong clock — against real time elapsed, while the limit it was checked against counts only time the app spent in the foreground, since a backgrounded phone freezes the work rather than continuing it. Cancelling a send, backgrounding the app for a few minutes and coming back therefore looked like far more than enough time for the transaction to have finished, when in fact it had barely started and was still on its way; retrying then paid twice. And the sweeper that gives up on a transaction taking too long recorded nothing at all, on the reasoning that anything it gives up on must already be finished — but giving up does not stop the work, and on a phone there is no time limit on it whatsoever, so this was the widest version of the same window.
- [FIX][all] **Bridging, earning, and Guardian sends of a callback-enabled asset no longer fail the same way ordinary sends did.** The fix above covered every path shaped like a send, but three others also move an asset out of your vault and each rebuilt it from faucet id + amount, so the same kernel rejection ("failed to remove the fungible asset from the vault") stopped them at the first note they mint: bridging to Ethereum, opening an Earn position, and — in a narrower form — a Guardian send with no recall window. The first two now take the asset from the slot it is actually held in, like every send does. The third already did, but chose between slots by whichever came first: because a token issuing callback-enabled assets occupied a separate slot per variant, a wallet holding both could have the transfer built against the one without enough in it and rejected for insufficient funds while the other slot covered it comfortably. That last case was carried as a local patch against the guardian client and has since been resolved upstream — see the guardian `0.17.0-rc.1` entry above.
- [FIX][all] **A send that cannot be verified either way is no longer a dead end.** When the wallet genuinely cannot tell whether a send reached the network — no record of the transaction and nothing to identify it by — it refuses to retry rather than risk paying twice. But there was no way out of that state: the Retry button stayed on screen and failed with the same message every time, and the only remaining option was to send again by hand, which is exactly the double payment being guarded against. The wallet now explains that it cannot confirm the transaction, asks you to check your balance, and offers a separate "Retry anyway" for when you can see it did not arrive.
- [FIX][all] **Offering a callback-enabled asset in a swap no longer fails the same way ordinary sends did.** Creating a swap order locks the offered asset out of your vault, so it is subject to the same rule as everything else this release fixes: the asset has to be taken from the slot it is actually held in. The swap builder does not accept an asset at all — only a token and an amount — and builds the wrong variant internally, so unlike every other path there was no better argument to pass. The wallet now lets it build the order and then re-issues it with the asset corrected, leaving everything that identifies the order untouched, so an order created this way is tracked, cancelled and settled exactly as before. Not fixed, because it cannot be reached from here: the wallet only creates orders, and filling someone else's order in a token that issues callback-enabled assets has the same defect on the paying side, inside the library.
- [FIX][mobile][desktop] **Approving a site's custom transaction now shows what it will actually do.** A site can ask the wallet to run a fully-formed transaction of its own construction, which can move anything the account holds. On the extension, that request is dry-run before you are asked and the approval screen lists the assets it moves. On mobile and desktop the same approval sheet had no way to run that dry run, so all it could say was "please ensure you know the details of the transaction before proceeding" and a recipient address — consent to an unnamed transfer of an unnamed amount. It now runs the same dry run and lists what leaves the account, what enters it, how many notes are consumed and created, and whether the account's stored data changes. A transaction that moves nothing says so, rather than leaving a silence that reads the same way; and a dry run that could not be produced says that too, with the reason, instead of appearing as a transaction with no effects.
- [FIX][all] **A private note from a site's custom transaction that could not be delivered no longer reports a clean success.** A private note only exists for its recipient once the wallet hands it over — nothing on chain carries what the recipient needs to see or consume it. The recipient is supplied by the requesting site and is optional, so a request that simply omits it produced a note the wallet could not deliver and would never retry, while the history row reported the transaction as completed with no indication that anything had gone wrong. The row now says a private note could not be delivered, and says how many. It stays completed rather than failed, because the transaction did land and the assets did leave: failing it would be untrue and would offer a Retry that spends them again.
- [FIX][all] **Amounts you type are now sent exactly as shown.** The conversion from what you type to the amount actually sent went through a decimal number that cannot hold enough digits for tokens with many decimal places, so the last few digits drifted: on an 18-decimal token, entering 1.000001 sent slightly less than the review screen said. The conversion is now exact at any precision, and digits finer than the token can represent are dropped rather than rounded up, so a send can never exceed the amount displayed.
- [FIX][all] **A connected site can no longer sign with a key belonging to another of your accounts, or run an arbitrary transaction against one.** Two more routes past the same boundary, both wider than the send routes fixed above. A request to sign data names the account whose permission is checked and the key to sign with in two separate fields, and only the first was ever compared against the session — while the key was looked up on its own, with nothing tying it to an account. A site connected to one account could therefore name its own account for the permission check and any other account's key for the signing, and receive a signature it was never granted; the approval sheet shows what is being signed but not whose key will sign it. Separately, the general-purpose transaction request also accepts a fully-formed transaction to run as-is, and the account to run it against was taken from the request and never checked either — the broadest case of all, since such a transaction can do anything the named account can, and the sheet describes the transaction without naming the account it will be charged to. Both are now bound to the account the session actually authorized.
- [FIX][all] **Retrying a send that had already reached the network could still pay twice on the default extension path.** Before retrying, the wallet asks the network whether the original transaction landed. On the code path that ships enabled in the extension's background worker, that question was never actually asked: the answer was hardcoded to "no record of it", which the caller reads as "cannot prove it landed" and retries through. The check now runs where it was supposed to, and a failure to reach it is reported rather than answered with a fabricated result.
- [FIX][all] **A send interrupted by the wallet's own watchdog is no longer offered as a one-tap retry that could pay twice.** Long transactions run in a separate context that the wallet tears down if it stops responding, and the whole operation — building, proving, sending and recording — is a single unit there, so a teardown says nothing about whether the transaction had already gone out, and the record that would have identified it is lost with it. A plain send in that state has neither of the two things that make a retry safe: nothing to identify to the network, and nothing that would make the network reject a duplicate. Retrying built a fresh transaction the network had no reason to refuse. Such a send is now refused with an explanation rather than sent again. This is deliberately narrow — an ordinary failure, including the one this release is mostly about, still rebuilds and retries as before.
- [FIX][all] **Cancelling a send that then simply failed no longer leaves it unable to be retried at all.** The protection against paying twice works by keeping the original transaction's details, so the network can reject a duplicate — but the previous round applied it to any send cancelled while it was running, permanently and with nothing to lift it. A send cancelled while being proved that then failed for an ordinary reason had its details pinned for good, so every retry replayed the identical failing request instead of rebuilding it — including the callback-asset case this release exists to fix — and the stale-transaction sweeper did the same to every send it swept. The wallet now distinguishes "this transaction went out" from "we do not yet know whether it will": the first is permanent, and the second is resolved as soon as the work stops, so a genuine failure rebuilds while a real crossing stays protected. A second route to the same dead end is closed with it: an ordinary send keeps no copy of its request, and the protection has nothing to hold for one — but retrying it still recorded that it might have gone out, so Retry worked exactly once and then refused for good, again including the callback-asset case this release is mostly about. That is now recorded only where there is something to protect, and an unresolved "do not know yet" is no longer quietly turned into a permanent "it went out" by passing through a retry. The same over-recording made a Guardian send with no recall window un-retryable from its very first failure, because such a send keeps no copy of its request either, yet was marked as possibly-sent before every attempt; and a cancel that arrived just as the send failed on its own could leave a marker nothing would ever clear, blocking Retry for minutes for no reason.
- [FIX][all] **Retrying a transaction that had in fact already gone through no longer fails with an internal error and leaves it marked failed for good.** The wallet checks with the network before retrying, precisely so that a transaction which really did land is completed rather than sent a second time. That check worked, but the step that acted on it did not: marking the transaction complete went through the same guard that stops a late error from overwriting a finished transaction, and a transaction being retried is by definition already marked failed — so the guard rejected it every time. The retry ended in "Transaction already in a finalized state" shown as the error, the transaction stayed marked failed despite having succeeded, and there was no way to reconcile it. Confirming a transaction the network vouches for is now a deliberate, separate step, so the protection against double-sending finally does what it was written to do. No funds were ever at risk from this — the failure was on the safe side.
- [FIX][all] **A public send from a Guardian account is no longer emitted as a private note the recipient never receives.** Guardian sends without a recall window were built private no matter what the review screen said. Because the wallet's own record still said public, the step that hands a private note to its recipient was skipped as well — so the note existed on chain, the recipient was never given the file needed to see or claim it, and a plain send has no reclaim window for the sender to undo it with. The note type the user approved is now the one that gets built.
- [FIX][extension] **The browser console is no longer flooded with wallet errors and warnings.** Two unrelated causes: the wallet reconnected to its background service worker once a second forever whenever that connection was unavailable — logging a "could not establish connection" error each time, in every open tab — and the extension pages each declared a preload for every code chunk they might need, which the browser warned about for each chunk it did not end up using. Reconnection now backs off and gives up when the page can never reach the extension again (after an update or reload), and the unused preload hints are gone.
- [FIX][mobile] **Flicking between the home screens no longer stutters just after your finger leaves.** The carousel started its snap from Framer Motion's `onDragEnd`, which the library defers to the end of the frame — so for one frame after release nothing moved the track at all, and at ~30px/frame that dead beat read as a freeze followed by a catch-up jump. Measured on device it hit 22 of 24 flicks, on frames that were otherwise a clean 60Hz with no dropped frames, which is why it never showed up as a performance problem. The snap now starts synchronously as the finger lifts and runs on the compositor rather than the main thread, so it also can't be disturbed by the re-render the route change triggers. Two related improvements: the release continues the finger's actual velocity instead of restarting from a standstill, and it settles in ~340ms rather than trailing off for over a second. Note that `requestAnimationFrame` inside `WKWebView` is capped at 60Hz by WebKit regardless of the display (WebKit bug 294338), so moving off the main thread is the only route to a higher rate here. Touching the screen while a transition is still running also no longer leaves the carousel stranded between two pages, showing both at once: a touch that only stops the motion, without going on to drag, now settles the track onto the page it was already committed to, and doesn't focus an amount field it happened to land on while that field was still moving.
- [FIX][all] **Recovering a guardian account from its seed phrase no longer intermittently fails to rotate its device key.** Recovery adopts the account state the guardian serves, and it adopts the same account more than once — the account matches at more than one HD index — with each copy unconditionally overwriting the last. While both copies describe the account's current state that is harmless, which is why this usually worked; when one of them was the account's creation-time state instead, whichever arrived last decided the outcome. Losing that race left the wallet believing a long-established account had never been on chain, so the device-key rotation that follows recovery was built as if it were creating the account, and the network rejected it outright ("initial account commitment 0x0000…0000 does not match the current commitment"), leaving the user stuck at the rotation step with an error they could do nothing about. The wallet now refuses to replace an account's state with an older copy of it — ordering can no longer decide the result. Account state only ever moves forward, which the client's own storage layer already required one level down; the wallet now upholds it before writing rather than discovering it afterwards. This also hardens the same write against a misbehaving or hostile guardian, which could previously roll a local account backwards, alongside the existing check that rejects a snapshot for the wrong account.
- [FIX][mobile] **Swiping between the home screens now works when your finger starts on an amount field.** On the Swap screen the "You Pay" and "You Receive" fields are full-width and about 64px tall, so most of that screen silently refused to swipe — the gesture did nothing at all. Framer Motion deliberately declines to start a drag when a gesture begins on a form control, so that dragging inside one selects text instead of dragging the whole page; on a full-screen carousel on touch that trade costs far more than it protects, since iOS selects text by long-press and double-tap. Touch gestures on those fields now page the carousel, while tapping a field still focuses it and opens the keyboard, and mouse and pen keep the select-text behaviour. The same applies to any other field inside the home carousel, such as the Send recipient address.


### Fixes

- [FIX][all] **Epoch Earn deposits and Fast (Epoch) bridge sends work again against the updated allocator, and earn deposits get an earn-specific receipt.** The Epoch allocator now validates the Miden collateral note on-chain and rejects any note not cryptographically bound to its intent (smallocator PR #38 — "Miden note is not bound to the intent mandate"), which broke opening Earn positions and Epoch sends. The wallet now follows the new integrator contract: the intents SDK is upgraded to 1.0.38, the collateral P2IDE note is minted with the SDK-supplied mandate-binding attachment and the SDK-supplied reclaim window (previously hardcoded wallet constants), and the same pre-built note request is used across the standard, guardian, and retry paths. On the UI side, a completed earn deposit no longer falls through to the generic send receipt — it shows a "You're Earning!" receipt with the deposited amount, market, and transaction id ("View Details" goes to the positions list) — and the in-progress badge stops hardcoding "AAVE-USDC", deriving the market name from the deposit's `marketUid` (e.g. "DUMMY-LENDING").

### Known issues

- **Guardian is expected to fail on this line until guardian packages ship for 0.16.** The guardian's version line is independent of Miden's: the newest published `@openzeppelin/miden-multisig-client` (`0.16.2`) still declares `@miden-sdk/miden-sdk: ^0.15.8`, so every released guardian build targets the Miden 0.15 protocol. The wallet's dependency resolutions force it onto the wallet's own SDK, so installs succeed and the mismatch surfaces at runtime as divergent MASM procedure roots. Tracked in #522.

## 1.15.21 (2026-08-17)

### Fixes

- [FIX][all] **QR-scanning a localnet, devnet, or mainnet address no longer fails as "invalid".** The QR scanner used a second, stale copy of the Miden-address validator (`lib/qr/format.ts`) that only accepted `mtst1` and a wrong `m1` prefix — so a `mlcl1…` address created under a dev-settings localhost override (and any `mdev1…` devnet or real `mm1…` mainnet address) was rejected. The QR validator now delegates its prefix check to the canonical `utils/miden` validator so it can't drift again; the paste path already accepted these.

- [FIX][all] **The transaction explorer link respects the dev-settings endpoint override.** The "Transaction ID" arrow in transaction details (plus the from/to account links there and the account link in dApp settings) were hardcoded to `testnet.midenscan.com`, so a custom explorer URL set via the hidden developer endpoint settings was ignored — and even a plain devnet build opened the testnet explorer. These links now resolve through the override-aware effective explorer URL (via `getExplorerTxUrl`/`getExplorerAccountUrl`), falling back to the effective network's default, and hide the arrow when no explorer is configured.
- [FIX][all] **Production (minified) builds no longer crash on load with "React is not defined."** The SVG→React (SVGR) transform in the vite configs generated components with the automatic JSX runtime (importing nothing from `react`), but the bundler recompiles those `moduleType: 'jsx'` modules with the _classic_ runtime (tsconfig `jsx: "react"`, which neither the `esbuild` nor `oxc` JSX options override in rolldown-vite) — emitting `React.createElement` calls against an undefined `React`. Harmless in unminified dev builds (a stray `React` leaked into the chunk) but every minified production build crashed on first render. SVGR now emits the classic runtime so each generated component imports `React`, matching the compile. Applied to the extension, mobile, and desktop configs.

- [FIX][all] **Private-note delivery no longer silently fails on fast chains.** The wallet relayed a private note's transport hint _after_ waiting for the transaction to commit, so the hint (the client's sync height) could already be at or past the note's commitment block — the recipient scans forward from the hint and would never find the note. The note is now relayed _before_ the commit wait (the SDK's prompt-relay flow), so the hint stays below the commitment block and the recipient picks the note up when it lands; the commit wait is preserved so `Completed` status still requires on-chain confirmation. Latent on testnet (slow blocks kept the sync height ≈ the commitment block at relay time); reproduced deterministically on a fast devnet.
- [FIX][all] **The send progress screen now shows a duration for every step.** Per-step timings were derived by observing live stage transitions, which a Dexie `liveQuery` coalesces — so a rapid transition dropped a step's start/end and left "Proof generated"/"Submitting" blank while the last step showed a meaningless "0 sec". Durations are now computed from per-stage timestamps persisted on the transaction, so no step is lost to coalescing and no fabricated zero is shown. The step list also adapts to the account: standard sends show proof + submit; Guardian sends additionally show the co-sign and guardian-sync steps. Standard sends now run execute → prove → submit as distinct stages so their proof/submit steps are timed on every platform.
- [FIX][all] Renamed the "Tx ID" field on the transaction details screen to "Transaction ID".
- [FIX][extension] **Developer endpoint settings no longer show "No response" for reachable endpoints.** The reachability probe used a `no-cors` fetch, which an MV3 extension page can never resolve — Chrome routes every extension fetch through the host_permissions/CORS path, so `no-cors` throws `Failed to fetch` and every gRPC field (RPC, prover, note-transport, faucet, explorer, guardian) reported unreachable even when the host was up. The probe now leads with a default-mode fetch (which the extension's `*.miden.io` host permissions and permissive-CORS hosts satisfy) and falls back to `no-cors` for non-extension webviews (Capacitor/Tauri).
- [FIX][extension] Centered the Save / Reset action block at the bottom of the developer settings screen (the fixed-width buttons were left-aligned on the wide full-page view).
- [FIX][extension] Developer-settings endpoint overrides now actually take effect. Saving an override only wrote to the frontend's cache/storage — the service worker runs in a separate JS realm with its own override cache (loaded once at boot) and a create-once Miden client singleton with endpoints baked in at creation time, so it kept hitting the build-default RPC until a full reload. Saving now nudges the service worker to reload its override cache and rebuild its Miden client, so the override takes effect immediately.
- [FIX][all] **Native (MIDEN) notes now auto-consume on a custom network, and balances show the correct native token.** The native faucet id discovered from the node's block header was cached per _base network name_ (e.g. `devnet`), so a custom dev-settings network sharing that name reused the real network's faucet id, and the cache was never invalidated when the effective endpoint changed. The auto-consume filter then compared incoming native notes against the wrong faucet id and never matched, so native deposits sat unclaimed. The cache is now keyed by the effective RPC URL (the node that defines the genesis and its fee faucet) and self-invalidates in memory when the endpoint switches, so each distinct node gets the right native faucet id. Additionally, the frontend primed native-asset discovery in a separate effect that didn't wait for the endpoint override to load, so on a warm-WASM page (e.g. the handed-off side panel) it captured the build-default node and rendered the native asset under the wrong faucet id; priming now runs only after overrides load, mirroring the service worker.
- [FIX][all] **A custom dev-settings network now survives wallet creation.** Creating a wallet calls a soft storage reset (`clearStorage`) that did a blanket key-value `clear()`, wiping the just-configured `endpoint_overrides` — so the wallet silently reverted to the build-default network while the account was already minted on the custom one. The result: the account address was on the custom network but balances/faucet/native-token resolved against the default node, showing a _second, wrong_ "MIDEN" row and breaking in-wallet funding. The reset now preserves the endpoint override (the dedicated dev-settings "Reset to defaults" still clears it explicitly). Also: the "Fund" prompt ran the hardcoded forkchoice faucet and the Miden faucet under `Promise.all`, so on a custom network the irrelevant forkchoice call sank the whole fund — funding now succeeds/fails on the (authoritative) Miden faucet alone, with forkchoice best-effort.
- [FIX][all] **`Localhost` networks now render addresses under the correct `mlcl1` prefix, and changing the dev-settings Network ID re-encodes the native token.** `getNetworkId()` had no localnet branch, so selecting Network ID = Localhost fell through to `NetworkId.testnet()` and encoded every account/faucet id under the wrong `mtst1` (testnet) prefix; it now uses the localnet `mlcl` HRP. Separately, the native-asset cache keyed only on the RPC URL, so changing just the Network ID (same node) kept serving a native faucet id encoded under the old prefix while freshly-synced note faucet ids used the new one — the balances placeholder mismatched the arrived token. The cache now keys on both the RPC URL and the network name, so a Network-ID change re-discovers and re-encodes.
- [FIX][all] **Small non-zero balances and monetary amounts no longer display as zero.** Amount formatting keeps each surface's normal precision for ordinary values, but expands past leading fractional zeros to show the first two significant fractional places (for example, `0.001234` renders as `0.0012` instead of `0.00`). The adaptive rule is shared by asset balances, token details, send/review screens, activity/history, bridge and Earn amounts, and fiat values.
- [FIX][mobile] **Bottom CTAs and drawers now track the iOS soft keyboard.** The keyboard height mirrored by the Capacitor listener is consumed again by the mobile layout, send-flow Confirm/Continue buttons collapse their navbar cushion to sit snug against the keyboard, and bottom-sheet drawers pad their content above it.

### Changes

- [CHANGE][ci] **E2E harnesses now capture a screenshot on every screen change (route/card/overlay), and four PR-only suites now also run on `main` with those screenshots uploaded as artifacts on green runs.** Capture is gated on `MIDEN_E2E_TEST` and tree-shaken out of production builds; no user-facing change.
- [CHANGE][extension] **The Chrome extension download is about a third of the size it was.** Two separate problems each inflated it. The released build was being produced in development mode by mistake, so every install shipped unminified code plus a complete set of source maps — around 60 MB of debugging files users have no use for. On top of that, two parts of the build each wrote their own copy of the same 19.5 MB cryptography engine, so identical files were shipped twice. The release now builds in production mode and the engine is emitted once, taking the package from about 30 MB to about 10 MB. No functional change; the same build was smoke-tested end to end first.
- [CHANGE][ci] **Translations are now generated by DeepL instead of the free Google Translate web endpoint.** The CI translation job called an unofficial, unkeyed Google endpoint whose output was poor enough that the generator carried ~250 lines of hand-maintained corrections — a per-language dictionary of known mistranslations to undo — just to keep "Seed Phrase", "Faucet" and "Note" in English. It now uses DeepL (via the `deepl-node` SDK) with a `DEEPL_API_KEY` secret; DeepL's native tag handling keeps those terms in English, so that whole correction dictionary is gone. Each locale is mapped to the correct DeepL target so regional variants stop silently degrading: `zh_TW` gets Traditional Chinese (bare `zh` was producing Simplified), `en_GB` gets British English, and `pt` gets Brazilian Portuguese to match the existing corpus. Fork PRs without the secret skip generation cleanly rather than failing.
- [CHANGE][all] **The Send recipient step now suggests recent recipients and can save a new one as a contact.** With the address field empty, a "Recent" list shows up to five of the account's most recent distinct send recipients — named when they match a wallet account or address-book entry, each with a Miden badge or destination-network dot and its truncated address — and tapping one fills the recipient. When a valid address isn't saved yet, the "Address Book" pill becomes "Add to contacts?" and opens a bottom sheet with the address book's add-contact form pre-filled with that address.
- [CHANGE][all] **The Activity page now surfaces pending notes above the history list.** When claimable notes exist, a "Consume your notes" card with the note count links to the claim screen, and an (i) button opens a bottom-sheet explainer describing what pending notes are; the header's pending-notes icon button is gone.
- [CHANGE][all] **Pending-note rows are redesigned around the amount, sender, and note privacy.** Each claimable note now leads with the incoming amount in green, followed by a "from ‹sender› · Private/Public" line, replacing the generic receive-icon tile.

## 1.15.20 (2026-08-17)


### Fixes

- [FIX][all] **The portfolio total no longer shows a fabricated dollar figure while prices are unavailable.** Every token defaults to $1 until real prices load, so before the price feed responds (or while it's down) the "USD total" on the home screen was effectively the raw token count dressed up as dollars. It now shows "$—" until real prices are available — stale-but-real prices still count — and switches to the true total once they load. (UX-review: a dash is the conservative choice; a skeleton or explicit "prices unavailable" affordance is a possible refinement.)
- [FIX][extension] **A transaction that fails in the background now tells you, instead of failing silently.** A send/consume/swap the wallet was processing while its window was closed could go to Failed with nothing to alert you — the mirror image of the "you received a note" notification never existed for failures. A genuine background failure now fires a native notification (never for a transaction you cancelled yourself, and never when a wallet window is open, since you'd already see it there). Extension only for now, matching the received-note notification.
- [FIX][all] **A failure to load your earn positions no longer looks like "you have none".** When the positions service was unreachable or errored, the "My positions" screen rendered an empty list and a $0 summary — indistinguishable from genuinely having no deposits, on the one screen where that difference matters. It now shows a distinct, retryable "couldn't load your positions" state on a failed load, while keeping the last-known positions visible during a transient error rather than hiding real balances.
- [FIX][all] **A sustained network outage is now backed off exponentially instead of probed every 30 seconds.** When syncing keeps failing, the sync circuit breaker used to wait a fixed 30s and try again — so a long outage meant hammering the node (and the wasm client) on a constant interval. The wait now roughly doubles per consecutive trip (capped at 5 minutes) with a little jitter, so a persistent outage is retried ever less aggressively and many wallets don't all probe in lockstep; a successful sync resets it to the base interval.
- [FIX][all] **A silent AggLayer bridge indexer can no longer hang the bridge claim/poll.** The two bridge-status requests (deposit lookup + merkle proof) had no timeout, so an indexer that accepted the connection then went quiet left the claim flow — and the bridge tracker's poll — hung indefinitely. Each request is now bounded by a timeout and aborts cleanly on expiry, so a transient outage fails the current poll tick and retries on the next rather than wedging on "Claim Pending". (The larger reconcile path for a permanently-stalled delivery is tracked separately.)
- [FIX][all] **Retrying test-token funding no longer double-mints the source that already worked.** The faucet funds from two independent sources (forkchoice IMIDEN + the official MIDEN faucet); if one paid out and the other failed, the funding screen showed an error, and tapping Retry re-ran BOTH — minting the already-funded source a second time. Retry now re-mints only the source that actually failed, and a genuine later re-fund still funds both. Each faucet request is also now bounded by a timeout, honors a `429 Retry-After` back-off, and the proof-of-work solve is capped by a deadline so a malformed challenge (e.g. an impossible `target=0`) can't spin the CPU forever.
- [FIX][all] **An incoming private note is no longer silently lost to a brief network blip.** A queued note import was given up after only three loop ticks — a matter of seconds — and then dropped with nothing but a console line, even though a private note's bytes can be the only copy of the funds it carries. Transient (network) failures are now retried on a 24-hour wall-clock budget with backoff, so a short outage can never exhaust them; a note that truly can't be imported (a sustained outage, or bytes that won't deserialise) is moved to a durable dead-letter store instead of being discarded, so it stays recoverable. dApp and manual imports now feed a failed note into that same retry queue rather than losing it on one blip.
- [FIX][all] **Retrying a failed send or swap can no longer double-spend your funds.** A transaction whose submit actually landed on-chain can still be marked Failed by an ambiguous abort; tapping Retry re-queued it blindly and broadcast a second send. Retry now asks the node first — if the transaction is already committed or in the mempool it is completed rather than resent, so your money moves exactly once.
- [FIX][all] **A network hiccup at startup no longer bricks the wallet until you reload.** The shared client was memoised as a promise that, if its first construction failed (e.g. the node was briefly unreachable), was handed to every later caller forever — a transient blip poisoned the whole session. The next call now re-attempts construction and self-heals.
- [FIX][all] **A wedged node can no longer hang the wallet indefinitely on a one-shot read.** Direct reads for the native token id, the chain head (reclaim-height), and faucet metadata had no timeout, so a node that accepted the connection then went silent left the awaiting screen hung forever. Each read is now bounded by a timeout with one retry and fails cleanly instead.
- [FIX][all] **The "Try Again" button on the error screen now works.** It had no click handler at all — clicking it did nothing. It now resets the error boundary and re-mounts the app; the offline hint on that screen also updates live if you reconnect.
- [FIX][all] **Adding an account from a seed while the network is down no longer creates an empty wallet.** The add-account path treated an import that FAILED because the node was unreachable the same as "this seed has no account", so a connection blip silently created a brand-new empty account and your real one didn't appear — the same fund-loss shape #629 fixed for restore, now closed for add-account too. It stops with "Could not reach the Miden network… please try again" and creates nothing; a seed genuinely not on chain still creates a new account as before.
- [FIX][all] **The wallet no longer ignores the Guardian asking it to slow down.** The background sync runs every few seconds and is by far the Guardian's most frequent caller, yet it was the one caller that treated "too many requests" as an ordinary error — logging it and coming straight back, which kept the limit tripped. Sends and claims already backed off on that signal; the sync now does too, pausing for as long as the Guardian asks and leaving the budget for the transaction that is actually trying to go through.

- [FIX][all] **The rotated-out-device fix now actually triggers.** The check shipped last round compared this device's key against its own local copy of the account — but Guardian accounts are private, so their state never travels on chain and that copy stays frozen at whatever this device last knew. Every device therefore still concluded it was the rightful signer and kept taking authorisation back. The device now asks the Guardian for the current state first, which it can still do because its recovery key is accepted even while its device key is being rejected.

- [FIX][all] **The send review screen no longer promises that an unclaimed payment comes back on its own.** It said the amount "returns to your wallet automatically" once the expiration passed. That is only true for the network's own token: everything else — every token you actually send — waits in your pending list until you claim it. The screen now says you can claim it back, which is what the wallet does. Found while writing the recall end-to-end test.

- [FIX][all] **A device-key rotation can no longer leave the wallet locked out of its own guardian.** After rotating, the wallet re-registers the new signer set so the guardian will accept requests from the new key — but that step ran once, and only its final push had retries. If any of the reads feeding it hiccuped while the rotation was still settling, the registration never landed and every following action failed with "Your session has expired" until a background repair eventually noticed. The whole step is now retried.
- [FIX][all] **Recovering your account onto a new device no longer gets broken by the old one.** Recovery moves the account's device key to the new device, so the old device stops being authorised and starts getting rejected by the Guardian. It reacted by re-registering itself — which is account-wide, so it took authorisation straight back off the device you had just recovered onto. That device then did the same, and the two swapped it back and forth about once a minute, indefinitely: on whichever device was currently locked out, every claim and every send failed with "Your session has expired". A device that is no longer the account's on-chain signer now leaves it alone.

- [FIX][all] **"From" and "To" are no longer swapped on a pending or cancelled send.** The transaction detail page worked out which way the money moved from the row's display label, and that label only reads "Sent" once the send has completed — it is "Sending" while queued and is overwritten with "Failed" when you cancel. So opening a send you had just cancelled showed "From: <the person you were paying>" and "To: <your own account>", on the one screen a worried user opens to check what happened. Direction now comes from the transaction type, which does not change underneath it.

- [FIX][all] **A failed wallet recovery now tells you what went wrong instead of dropping you on an empty wallet.** Forgot Password wipes the local wallet before re-registering, but a failure during that re-registration was only written to the console and the app navigated on regardless — so you landed on a wiped wallet with no explanation, which looks exactly like losing your funds. The reason is now shown on the confirmation screen with a Retry, and the flow stays put instead of navigating away. (#630)

- [FIX][all] **Restoring from a seed phrase while the network is down no longer creates an empty wallet.** The restore looks your account up on chain, and a lookup that FAILED because the node was unreachable was treated the same as "this seed has no account" — so a connection problem mid-restore silently created a brand-new empty wallet and your real account simply didn't appear. Restore now stops with "Could not reach the Miden network to look up your account… please try again" and creates nothing. A seed that genuinely has no account still creates a new wallet as before. (#629)
- [FIX][all] **A rate-limited Guardian no longer kills your transaction.** When the Guardian answers `429 Too Many Requests` before a transaction is submitted, the send/swap/claim now returns to the queue and retries automatically — honouring the delay the Guardian asks for, within sane bounds — instead of failing outright and making you start over. Rate limits hit after submission still fail loudly, since the transaction may already be on chain, and the 30-minute queue cap remains the terminal limit. (#617)
- [FIX][extension] **Recurring stuck "Consuming" notes are fixed by moving the wallet's WASM engine off the extension service-worker thread.** Transaction execution, proving, syncing, and reads now run in a dedicated `chrome.offscreen` document, so a wedged operation can no longer freeze the service worker and strand notes mid-consume; each operation is bounded by its own deadline and a wedged one is torn down and safely retried. Behind `MIDEN_USE_OFFSCREEN_CLIENT` (extension only; mobile/desktop unaffected). (#260)

### Changes

- [CHANGE][ci] **The Android test set-up no longer waits forever for software that isn't installed.** After the test devices were switched to a plain Android image, start-up still waited for a Google Play Services process to settle — on an image that has no Play Services, so it could never appear. Every run then spent its whole start-up budget waiting and failed with "did not stabilize". That wait is now skipped when the device has no Play Services, which is also the case where the problem it guards against cannot happen.

- [CHANGE][ci] **Android test devices go back to four gigabytes each.** Giving them five made things worse rather than better — with two devices on one machine the host ran short and the devices stopped starting up reliably at all. The memory freed by shutting down the leftover build process stays, since that gives the machine room without taking it from anywhere.

- [CHANGE][ci] **Android test devices get enough memory to keep the wallet alive.** Each device was given four gigabytes, a figure chosen on a developer Mac; on the CI machine that is too little and Android was killing the wallet itself to free memory, which is why the run kept failing with the app simply not running. Each device now gets five, and the build tool's leftover background process is shut down first so the memory is there to give.

- [CHANGE][ci] **Android test devices no longer run Google Play Services, which was starving the wallet.** The test emulators used a system image bundling Play Services even though the wallet needs none of it. With two devices on one CI machine, Play Services locked up for half a minute at a time and the operating system killed processes to cope — including, on the failing run, the wallet itself. The devices now use the plain Android image.

- [CHANGE][ci] **A test that failed one run in six no longer does.** The wallet deliberately staggers how long it waits before retrying a failed network sync, so many wallets don't all retry in lockstep. A test checked that the wait had elapsed by advancing its clock to a fixed point that fell inside the staggered range about a sixth of the time — so roughly every sixth pull request failed its coverage gate for no reason. The test now pins the stagger instead of gambling on it.

- [CHANGE][ci] **When an Android test run fails, the logs collected afterwards now contain something.** The step that exists to explain a failure read only the tail end of the device log and then filtered it, so on a run where the wallet never started it produced an empty section — no way to tell whether the app had crashed, been killed for memory, or simply never been asked to do anything. It now checks whether the app is running at all, reads the crash log, searches the whole buffer rather than its last few hundred lines, and always prints an unfiltered tail as a fallback.

- [CHANGE][ci] **Android test emulators get enough processors to actually do the work.** An earlier attempt to stop them competing for the machine gave each one half of it, which fixed start-up and then broke everything after it: both emulators came up fine and the first test sat for its entire thirty-minute budget without the wallet completing account creation. They now get the whole machine each — sharing it is fine, starving them is not — and the start-up flakiness is handled by a longer start-up budget instead.

- [CHANGE][ci] **The Android end-to-end run goes back to Linux.** Moving it to the larger Apple-silicon machines the iPhone tests use looked right — it is the kind of machine the Android harness was written for — but those machines are themselves virtualised and do not offer the hardware acceleration an emulator needs, so it died at start-up with "HV_UNSUPPORTED". Linux is the only hosted option that provides it. The contention that made the run flaky in the first place is handled instead by sizing each emulator to the machine it finds, and by a start-up budget that suits a busy CI machine rather than a fast laptop.

- [CHANGE][ci] **A guardian test that needs two operators no longer takes the whole guardian suite down on networks that have one.** Three switch specs decided at import time that a missing second operator was fatal, which aborted collection — so on the branch that tests against devnet the entire guardian job reported one setup error and none of the dozens of specs that would have passed ever ran. They now skip themselves, with the reason stated. A fourth spec had also hardcoded the local development guardian as its fallback, so on testnet it asked for an address that only exists on a developer laptop; it now reads the operator pair for whichever network is under test.

- [CHANGE][ci] **The Android end-to-end run now gets a machine that can actually run two emulators.** It was on a small Linux runner with about four processors, booting two emulators that each asked for more than that — the emulator said so in its own log — and the second one kept missing its start-up deadline while still booting perfectly normally. It now runs on the same larger Apple-silicon machines the iPhone tests already use, which is also the kind of machine the Android test harness was written against, and the harness sizes its start-up budget and processor count from whatever machine it finds rather than from hardcoded numbers.

- [CHANGE][ci] **A new infra-resilience E2E suite fault-injects backend dependencies and asserts the wallet degrades gracefully.** It stands up the hermetic localnet stack and, per wallet action, makes a dependency fail / flake / time out / blackhole, then asserts no silent failure, no fund loss, no misleading "$0 / delivered / up-to-date", bounded waits, and clean recovery — each check made falsifiable (it fails if the fault does not actually bite) so a passing test can't be a false green. Verified-faultable today and covered by guards: the **node read/sync path** at the fetch layer (SW + SDK worker) — an offline note is undiscoverable then catches up, and a claim during a node outage never strands funds (proven spendable on-chain); and the **guardian** HTTP path via the route seam — co-signed send/consume survive a transient `conflict_pending_delta`/5xx and a switch-guardian survives a transient register 5xx, each asserting the fault fired. Note the fetch seam reaches only the node's `fetch`-based reads: the delegated prover, the transaction submit, and note-transport delivery use a non-`fetch` transport it cannot retrofit (verified), so those require infra-level faulting (`docker pause`) and are out of the fetch seam's scope. Runs on push-to-main, nightly, and on demand. Adds only test-harness code plus small, off-by-default seams (env-overridable hosts, a `force` option on the sync test hook); no production behaviour change.
- [CHANGE][ci] **A passing test run no longer gets reported as a failed one because of the words in a test's name.** Each test's diagnostics are saved to a folder named after its title, and GitHub rejects an entire upload if any path in it contains a colon, angle bracket or similar. One test's name contains "miden:<publicKey>", so the whole Chrome suite ran green on main and the job still went red at the upload step — throwing away the very diagnostics that step exists to keep. Folder names are now sanitised in one shared place used by the Chrome, iOS and Android harnesses. This also fixes a quieter bug: a title containing a slash was splitting one test's artifacts across nested folders.

- [CHANGE][ci] **The two newest end-to-end workflows were each missing a setup step every other one has, and both failed on their first real run.** The stress suite never started the note-transport service, so a wallet could not even be created — creation syncs before it returns, the sync could not reach the service, and both wallets ended the run with no account at all. The Android suite never pre-built the Miden client binary, so it compiled from source _inside_ the first test and spent nine of its fifteen allotted minutes in the compiler before timing out. Both now do what the other suites already did.

- [CHANGE][ci] **Pull requests now check that iOS, Android and the desktop app still build.** The wallet ships four surfaces but only the browser extension was compiled on a PR, so a change that broke the Android, iOS or desktop build merged with nothing going red and was discovered by whoever next tried to ship that platform.
- [CHANGE][all] **"I forgot my password" — the one route that destroys your wallet — is now tested end to end, and doing so fixed the explanation you get when a reset fails.** No test anywhere touched Forgot Password or Reset Wallet, even though the flow wipes your local wallet before restoring it from your seed phrase. Two specs now drive the real screens from the locked unlock screen: one resets a funded wallet in place and proves the same account and the exact same balance come back under a **new** password while the old one is refused (nothing previously checked that the wallet was genuinely re-locked with the password you just typed), and one drives a reset that fails on purpose, reads the wallet's own storage to prove the wipe really happened, and pins the explanation left on screen. Writing the second spec turned up two problems with that explanation. It was the literal text "[object Object]", because errors coming back from the wallet's background service were not real `Error` objects and every screen that reads one falls back to stringifying it — they are now, which also repairs roughly twenty-five other error messages across the app. And once it was readable it still only said "Failed to create wallet"; a recovery that finds no account for your seed at that Guardian now says exactly that, which is the one thing you can act on. (#630 follow-up)
- [CHANGE][ci] **Taking an unclaimed payment back is now tested end to end — it never was before.** Every payment you send carries a recall window: a deadline after which, if the recipient still hasn't claimed it, you can pull the money back. Nothing in the test suite had ever reclaimed a send, so the one path (besides swaps) where your funds come _back_ was unverified. A new spec sends with a short recall window, confirms the recipient can see the payment while your own wallet still won't let you take it back, waits for the window to pass on chain rather than on a stopwatch, reclaims it, and then checks your balance went down by the amount sent and back up to exactly where it started, that the activity row reads "Reclaimed", and that the recipient's copy of the payment stops being claimable once you've taken it back — which is what stops the same money being spendable twice. It also covers the "Never" choice, which must produce a payment the sender genuinely cannot pull back. Runs on the local test chain only, since sizing a recall window in blocks needs a block rate the test can know. Test-only, apart from a review-screen hook that exists solely in E2E builds (there is no way to pick a window shorter than the date picker's 30-minute minimum by clicking) and the removal of a duplicated navigation call in the send submit path.
- [CHANGE][ci] **The end-to-end suites now test the network that matches the branch.** Devnet and testnet run different protocol versions, so a build for one cannot pass against the other. Both used to run on every push to main, with each gate passing if EITHER succeeded — which meant a real testnet failure was reported as green whenever devnet passed. Testnet now runs on `main` and devnet on `next`, each with a gate that fails if its own network fails.
- [CHANGE][all] **Connecting a dApp, signing for it and sending through it are now tested end to end — and doing that found two approval screens showing you the wrong thing.** The whole dApp path (a website calls the wallet, the approval window opens, you accept or reject, the website gets an answer) had exactly one automated check: that the wallet is detectable. A new browser suite now drives the real journey in the extension and asserts what the approval window actually says, and new unit tests cover the same two fixes on the mobile and desktop approval sheet, which no browser test reaches. Two real bugs fell out and are fixed on every platform. First, no approval other than "connect" named the site that was actually asking: the extension showed the wallet's own internal address, and mobile and desktop showed a display name the website itself supplies and can set to anything — so on all three a phishing page's prompt was indistinguishable from a legitimate one. Every approval sheet now names the origin the wallet verified, as the connect sheet already did. Second, the amount on a dApp's send approval was always scaled as if the token had 6 decimals, so any other token showed a wrong number (on mobile and desktop it showed the raw un-scaled figure), and very large amounts lost their last digits. Send approvals now show the amount using the token's own decimals, as claim approvals already did.
- [CHANGE][ci] **The Android end-to-end run now gets as far as starting an emulator.** Its first real run created the base virtual device successfully and then died in setup with "Base AVD not found": the tool that writes the device, the tool that lists it, and the test harness each resolved the Android home directory differently. All three are now pinned to one path, and the workflow fails at the create step if the emulator cannot see what was just written — instead of five minutes later, after paying for the SDK install and the APK build.
- [CHANGE][ci] **The stress / balance-conservation suite now actually runs.** It launches a real browser extension, which cannot run headless, and it was the one end-to-end workflow started without a virtual display — so every run died at browser launch with "Missing X server", after paying for the full local chain bring-up, and not a single stress iteration had ever executed.
- [FIX][all] **Deleting a saved contact, and disconnecting a connected app, now work at all.** Both ask "are you sure?" from inside a settings drawer, and the confirmation was drawn _underneath_ that drawer — so tapping a contact appeared to do nothing, no dialog was ever visible, and there was no way to finish either action. The dialog now sits above the drawer and stays clickable (an open drawer also switches off clicks page-wide, which kept its buttons inert regardless of what was on top), and answering the question no longer closes the drawer out from under you. A test that swallowed the same click and reported success — hiding this for as long as it existed — now keys on the dialog actually closing, and a new seconds-long test walks the whole delete without needing a chain.

- [CHANGE][ci] **Android now has automated end-to-end tests, and they actually run.** The Android harness existed but had no workflow and had gone stale against the app, so a regression that only reproduced on Android could ship with nothing going red. It now runs on every push to main. Fixing it turned up four separate faults, including an emulator that could never boot and a claim check that reported success when nothing had been claimed.
- [CHANGE][ci] **A stuck click in the test suite no longer burns ten minutes and reports nothing.** Playwright leaves action timeouts unbounded by default, so a click on a button that never becomes actionable — one under a modal still animating in — waited out the entire per-test budget and then failed with a closed-context error naming neither the step nor the element. Actions are now bounded, and the address-book delete that hit this reports what it was waiting for. Also corrects the pinned `miden-client` CLI version, which recorded 0.14.8 while the pinned revision builds 0.15.0 — a mismatched CLI fails against the node with an unrelated-looking "accept header validation failed".

- [CHANGE][ci] **The address book, the receive QR and the settings toggles are now covered by end-to-end tests — until now, nothing checked any of them.** Three new Chrome E2E specs walk the paths a person actually takes: saving another wallet in Settings → Address Book, then choosing that contact _by name_ in the send flow and confirming the payment is addressed to exactly that account (the existing send test already covers the rest of the money path, so this one stops before broadcasting rather than paying for a second one), then deleting the contact and confirming the picker goes empty; opening Receive and confirming the QR encodes exactly the current account's address and the copy button puts exactly that address on the clipboard (not the shortened version shown on screen); and flipping a General Settings toggle and confirming the choice is really stored — including in the copy the background service worker reads, which is the one that decides whether notes get claimed for you — plus switching the theme and confirming it applies immediately and survives a restart. Getting there needed a handful of test hooks added to the UI (the toggles, the address-book rows and buttons, the contact picker rows, the recipient name, the QR); no wallet behaviour changes.
- [CHANGE][ci] **PR test runs stop rebuilding the same Rust binaries every time.** Each E2E job compiled `miden-client-cli` and note-transport from source — about 12 minutes per job, roughly 70 minutes per PR across six jobs, more than the actual test execution. Two causes: nothing populated the cache on `main` (GitHub scopes a cache to the branch that wrote it, so a fresh PR could never read one), and the six workflows used different cache keys for byte-identical content, which also made those entries evict each other under the repo cache limit. They now share one key, warmed on `main`, narrowed to the pins that actually determine the binaries instead of all of `package.json`.

- [CHANGE][ci] **Changelog entries from parallel PRs no longer collide.** Every PR adds its line under the same heading, so any two open PRs conflicted on that one anchor — not because they disagreed, but because git could not tell that both lines were wanted. The file is now union-merged, which keeps both, and CI checks the result for the duplicate version headings that are union's only failure mode.

- [CHANGE][ci] **The Activity tab and the Cancel button are now covered by end-to-end tests — and that coverage found a real bug.** Nothing in the suite had ever opened Activity or a transaction's detail page, so a row showing the wrong amount or the wrong recipient could have shipped without a single test noticing, and nothing at all checked what cancelling a pending transaction actually does. A public send now additionally asserts that its Activity row reads the exact amount and the recipient it was really sent to, and that the detail page's Tx ID is the transaction's stored on-chain hash rather than its internal row id or another row's. A new spec cancels a send that has not started yet and proves the transaction never entered the build pipeline at all — and stays out of it after the queue is running again — which is what "your money stayed put" means for a transaction that was never built. To make that test trustworthy rather than a coin flip, the harness parks the transaction queue first, so the send provably never begins building instead of racing the wallet and occasionally sending for real. Adds only test hooks to the app (Activity row, transaction detail rows, status pill, Cancel button).
- [CHANGE][ci] **The pending-notes screen is now tested on what it actually shows you.** The claim tests read the wallet's internal store, so a build that printed the wrong amount next to a received token — the wrong number of decimals, a missing thousands separator, one row showing the sum twice instead of each note — passed them. The group-claim E2E test now receives two notes of one token and asserts the on-screen text before claiming: the grouped total on the summary row and each note's own amount in the detail view. It also covers the Home prompt that tells you tokens are waiting, which had no test at all and is the only notice you get for tokens that never claim themselves (only the native token does, whatever the auto-claim setting says) — including a check that the prompt is the carousel card actually on screen, not merely present in the page. Those checks run before the harness fills in token names on the wallet's behalf, since a test that writes the name it then checks proves nothing. Adds test-only `data-testid` hooks; no app behaviour changes.
- [CHANGE][ci] **Getting your password wrong is now tested against a real, funded wallet.** The unlock screen is the one you see most — the vault key only ever lives in the extension's service-worker memory, so every worker restart sends you back to it — yet no browser test had ever typed a _wrong_ password into it. So the escalating lockout (three wrong tries disables the field for a minute, and it frees itself with no action from you) was only ever checked against a stand-in for the vault, never the real one. A new end-to-end test funds a wallet with an exact amount, claims it, locks it, gets the password wrong three times, and unlocks properly — checking along the way that the field is still usable after the first two tries (three really is the minimum), that the countdown starts inside the advertised minute and actually ticks, and that the lockout held for a full minute measured on the wallet's own clock. It then checks the balance is exactly what it was before the lock, which proves the password genuinely re-derived the key that decrypts your accounts rather than just proving a screen changed. Test-only; the app gains one test hook attribute and no behaviour change.
- [CHANGE][ci] **The self-custody spine has end-to-end coverage for the first time.** Creating a wallet through the real onboarding UI, and the invariant that the seed phrase you are shown is the one that actually restores your account, were both untested — every existing spec started from a harness shortcut that skipped onboarding entirely. Two specs now drive the real screens: a full first-run create that ends by funding the wallet and asserting an exact balance, and a backup/verify round-trip that reads the displayed words and proves they restore the same account in a second wallet. (#615 follow-up)
- [CHANGE][ci] **The core money paths are now tested on the account type users actually have.** Production onboarding can only create Guardian (co-signed) accounts, but the send and claim specs ran exclusively against fully-private accounts that no user can reach. Private send and multi-note claim now run on both. The multi-note spec also finally claims — it was named "Multi-Note Claiming" and titled "claim all" while only asserting the pending total, so the batching path it advertised had no coverage at all. Both journeys live in one shared module rather than being copied per account type.

- [CHANGE][ci] **Generated Playwright output can no longer be committed by accident.** `.gitignore` enumerated `/test-results` and `/test-results-ios`, which silently missed the per-suite directories other configs write (`-android`, `-stress`) — three such files reached main this way. Replaced the enumeration with `/test-results*` so every suite's output is covered, including suites added later.

- [CHANGE][ci] **Fixed the iOS E2E claim wait, which could never succeed.** While the wallet sits on the transaction-progress screen waiting for a claim, nothing is mounted that refreshes the store's balances — that projection is only written by the poll inside the home/token screens. The claim wait polled it anyway, so it read a frozen number and timed out even when the consume had landed on-chain. It now drives the store's own `fetchBalances` action before reading, instead of navigating away (which would stop the mobile transaction driver).
- [CHANGE][ci] **The Playwright suite is lint-clean.** 316 accumulated ESLint problems across `playwright/` are fixed rather than tolerated. Almost all were formatting; the substantive ones were a closure in a retry loop capturing a mutable binding, a redundant regex escape, and Playwright fixtures declaring an empty destructuring pattern. The `self` references in service-worker `evaluate()` blocks are correct as written — `window` does not exist in that scope — so those carry a scoped disable with the reason instead of a rewrite.
- [CHANGE][ci] **Fixed an E2E assertion that failed working sends.** The three send specs asserted the sender was debited by reading their balance once, immediately after waiting on the _recipient_. The recipient seeing the note only proves it is on-chain — the sender's own balances projection settles separately — so under CI load the read sampled a vault that had not moved yet and reported `debited 0` on a transfer that had actually succeeded. The check now waits for the debit, and its timeout message distinguishes "never debited" from "still catching up".

- [CHANGE][ci] **A green E2E run now means more than it did.** Every PR is checked for assertion shapes that pass whether or not the wallet works — `expect(balance).toBeGreaterThan(0)` and `waitForBalanceAbove(0, …)` (true for the wrong token, the wrong amount, or a balance that was already non-zero), `waitForTimeout` over 2s (a bet on machine speed that surfaces as an unrelated failure further down the spec), and `expect()` inside an `if` branch (reports PASS when the feature never rendered, which is what the popup send/receive smoke tests were doing). The rules are AST-based, so the helper docs that quote these patterns to explain the ban are not themselves flagged. The 79 instances already in the suite are recorded per file in `playwright/e2e/eslint-rules/known-violations.json` and do not fail CI — the gate blocks _new_ ones and the recorded counts may only go down, so the backlog is visible and shrinking rather than quietly growing. `yarn lint:e2e`. Test-only; no app behaviour changes. (#615 follow-up) The baseline is a true ratchet: a tree that is _cleaner_ than the recorded budget now fails too, because every unused slot is room for a new unfalsifiable assertion to land green.
- [CHANGE][ci] **E2E failures now point at the step that actually broke.** Several page-object methods could succeed silently: a Chrome send that rendered an error screen was `console.log`ged and returned as if sent, a send whose submit button never detached was ignored on both Chrome and iOS, and an iOS claim that never produced a balance returned normally after its timeout. Each of those turned into a failure minutes later on the _recipient's_ balance, so a broken send read as a broken delivery and cost real time to re-attribute. Those methods now throw where the flow actually stopped, quoting the on-screen text. iOS `unlockWallet` also asserts it reached the unlocked state (Unlock screen gone + store Ready) instead of returning after a fixed sleep. Harness-only; no app behaviour changes. (#615 follow-up)
- [CHANGE][ci] **The Chrome E2E harness now waits for the wallet to say it's done instead of guessing with a stopwatch.** Every sync in the harness slept a flat 3.5s and then read `chrome.storage.local` — but the service worker's sync is fire-and-forget, so on a slow testnet round the harness read the _previous_ sync's data and drew conclusions from it (a note that had just landed looked like it hadn't, a claimed one looked unclaimed). It now waits for the service worker's `SYNC_COMPLETED` broadcast, which is emitted only after the sync data is persisted — the exact write the harness goes on to read. Eight other fixed sleeps (post-reload settles, claim-button renders, post-submit navigation, lock screen) were likewise replaced with the DOM or store state they were standing in for. Each wait keeps the old sleep as its timeout ceiling, so a run can only get faster, never slower; the sleeps that genuinely have no observable signal (a consume's head start after a Claim click) were deliberately left alone and are now labelled as such. Test-only. (#615 follow-up)
- [CHANGE][ci] **A localnet E2E run now reports every spec, and a spec that only passed on its retry is named out loud.** The hermetic per-PR suites stopped at the first failing spec, so a ~25-minute run answered one question and left the rest unknown — two broken specs cost two full runs to discover. Those suites (E2E_NETWORK=localhost: local, earn, swap, guardian, bridge-guardian) now report up to three independent failures per run instead of one — bounded deliberately, because letting every spec burn its full timeout could exceed the job budget and kill the run before it uploads any artifacts at all; the expensive live-network runs (devnet/testnet, real-testnet bridge) still stop at the first failure, since each spec there spends shared faucet funds and wall-clock. Separately, those jobs run with `--retries=1`, which quietly turned a real flake into a clean green tick: each job now prints a `FLAKY: <spec> — passed on retry N` line per retried-then-passed spec, so the flake rate is a number someone can read off the log instead of an invisible one. The retry budget itself is unchanged, and the new report can never fail a job.
- [CHANGE][ci] **Every failing E2E spec now leaves a diagnosable failure report, not just the two-wallet ones.** The harness's `report.json` — failure category, diagnostic hints, per-wallet state at the moment of failure, browser/service-worker errors, recent timeline — was produced in the second wallet's teardown, so any spec that never asks for a second wallet (the swap smoke and guardian fault specs, and the whole bridge and earn suites) failed with nothing but a Playwright stack trace, and a run that died while the first wallet was still launching produced nothing at all. Report generation now runs as an automatic per-test fixture, so every spec emits one regardless of how many wallets it used; each wallet still snapshots its own state while its page is alive, and the report is still written exactly once with the same shape the agentic mode and downstream tooling already read. Test-harness only. (#639)
- [CHANGE][ci] **The blockchain E2E suite now refuses to run a wallet built for a different network than the harness.** Bring-up only checked that `dist/chrome_unpacked/manifest.json` existed, so a leftover build from an earlier run — a testnet bundle, say — could be driven by the localhost (docker) harness: the miden-client CLI mints notes on one chain while the wallet syncs another. That run either burns 20 minutes and fails with product-shaped symptoms (notes never arrive, balances stay zero) or, worse, goes green without ever exercising the network it claimed to. The fixture now reads the `MIDEN_NETWORK` token actually baked into the built bundles and aborts before a browser is launched, naming the built network, the harness network and the rebuild command — and it also catches a half-rebuilt `dist/` whose service worker and UI disagree with each other. Test-harness only; no app behaviour change.
- [CHANGE][ci] **Two shipped behaviours were untestable because the E2E test-hook flag also switched them off.** `MIDEN_E2E_TEST` is supposed to only install the harness hooks, but it additionally forced onboarding to finish in-tab and made the wallet throw away any persisted endpoint override. So no E2E run has ever reached the Chrome side panel — the surface a real Chrome user spends the entire session in — or the developer endpoint-override flow, and a regression in either could ship unnoticed. Those two opt-outs now have their own build flags (`MIDEN_E2E_DISABLE_SIDEPANEL`, `MIDEN_E2E_DISABLE_ENDPOINT_OVERRIDES`), which the E2E build scripts set explicitly so every existing suite behaves exactly as it does today, while a new suite can keep the test hooks and still drive the real panel. No shipped build sets either flag, so app behaviour is unchanged. (#624 follow-up)
- [CHANGE][ci] **The balance-conservation invariant now actually runs.** `stress.spec.ts` has always asserted that the two wallets' combined balance is unchanged by a randomized send/claim run — the check that catches a note lost or minted from nowhere, which no single-wallet balance assertion can see — but no workflow ever executed it. Added a nightly localnet job that runs it with the strict flag set explicitly. Nightly and un-retried on purpose: it is adversarial by design, so a failure wants a human rather than a re-run. (#615 follow-up)
- [CHANGE][ci] **The E2E money specs now assert exact amounts.** Ten Chrome specs moved off `expect(balance).toBeGreaterThan(0)` onto exact base-unit assertions that name the token and check both sides of a transfer — the recipient credited by exactly N and the sender debited by at least N. Mints now assert the exact unconsumed-note total, and claims assert the vault went **up** by N _and_ the pending total went **down** by N, which is what distinguishes a real claim from a note that was merely discovered. (#615 follow-up)
- [CHANGE][ci] **E2E money assertions can now actually fail.** Adds symbol-aware, base-unit balance primitives and transfer/claim/conservation assertions for the E2E suite. The existing `getBalance()` discards the token symbol it is given and adds _unconsumed_ notes to the balance, so paired with `expect(balance).toBeGreaterThan(0)` it stayed true for a wrong-token, wrong-amount, or never-actually-claimed transfer. The new helpers keep spendable-vault and pending-note totals strictly separate, compare exact `bigint` base units, and assert both sides of a transfer. No behaviour change to the app. (#615 follow-up)
- [CHANGE][ci] **Paused the live bridge-out E2E while the Epoch solver declines to quote it.** Since 2026-08-12 the solver answers `200 OK` with `NO_QUOTE_AVAILABLE` for the throwaway faucet token this spec mints, and re-running the last green commit unchanged reproduces it — so it is an external change, not a wallet regression. The same bridge-out flow still runs on every PR against the hermetic fake allocator, and the AggLayer route still runs on main. Tracked in #627. (#627)
- [CHANGE][ci] **A failing bridge quote now says why.** The bridge E2E gated the Fast route on a `$` appearing in the fee, which is the same `—` placeholder whether the route wasn't a bridge, the token never loaded, or the quote service returned nothing — and would have passed on a `$0.00` from a missing price. It now waits for the quote to actually resolve and, on failure, reports the quote hook's own error string (which the app records and never surfaced) alongside the real Epoch HTTP status and body. Test-only. (#615 follow-up)
- [CHANGE][ci] **Stop the guardian-lifecycle E2E harness from failing runs that actually succeeded.** `claimAllNotes`/`claimNotesByGroup` billed their ~8-12s reload against the caller's drain budget, and the deadline is only checked between iterations, so a note that drained mid-iteration could expire the clock and fail a wallet that had in fact claimed it. Both now start the clock after the reload and share one tail that re-samples at the deadline, applying the same two-consecutive-zeros rule before failing. The failure dump also renders `status` by name and sorts by `initiatedAt`, so a Completed row no longer reads as in-flight. (#615)
- [CHANGE][all] **A completed transaction now records that it finished.** Successful rows kept whatever stage they were in when they completed — a finished key rotation stayed at "confirming", a finished Guardian claim at "guardian-synced" — so a finished transaction read as still running in diagnostics. Completion now stamps the terminal stage; failed rows still keep the stage they failed at. (#618)
- Add hidden developer endpoint configuration (7-tap the Welcome logo during onboarding) to override RPC / prover / note-transport / faucet / explorer / guardian endpoints and network ID; read-only view with reset-to-defaults in Settings.

### Fixes

- [FIX][all] **Transaction receipts stick around and claims get a proper one.** The Generating Transaction screen no longer auto-navigates home ~10s after completion — the success/failure receipt stays up until the user taps Done (or Hide). Claim (consume) transactions now render their own summary ("{amount} {symbol} → Consumed") and receipt: the note sender as "From", "Total Consumed" instead of "Total Paid", and a "Notes Consumed" row with the claimed note ids; the receipt's hash row is now labeled "Transaction ID" (was "Source TX") for all transaction types.
- [FIX][all] **Guardian provider names are spelled consistently across the wallet.** Onboarding, Settings, Activity and recovery now read each operator's display name from one canonical source, so "OpenZeppelin" and "LambdaClass" render the same everywhere (previously a mix of "Open-Zeppelin"/"Lambda Class"). (#464)
- [FIX][all] **A settled swap's order status no longer sticks on "Active" with a flickering spinner.** The order-tracking row reconciles the on-chain order lineage with the settlement this wallet has already observed: once the settlement (or reclaim) consume notes are seen locally, the status shows "Filled"/"Reclaimed" immediately, even if the lineage poll still lags behind reporting the order as active. The polling indicator is now tied to whether the order is genuinely still resolving rather than to each individual in-flight poll, so it no longer blinks off and on every couple of seconds while an order is active (most visible on iOS).
- [FIX][all] **Auto-consume no longer retries a permanently-unconsumable note forever.** The background auto-consume retry throttle was a sliding 30-minute window: old failures aged out, so a note that keeps failing on-chain every attempt (e.g. a deterministic kernel rejection the consumability check misses) dripped a fresh failed consume roughly every 5 minutes indefinitely — spontaneously reopening "Generating transaction", hammering the Guardian co-sign path, and piling up Failed rows in Activity. The throttle now counts a note's lifetime failures and backs off exponentially (5 min → 10 → 20 → … capped at 24 h), so a stuck note decays to at most a daily probe. It is a probe, not a permanent give-up: if the note later becomes consumable, the next attempt succeeds and it recovers on its own, and an explicit Retry still bypasses the backoff.
- [FIX][all] **A consumable note that fails to parse during sync is now logged instead of vanishing silently.** The sync parser dropped any note that threw mid-parse (corrupted metadata, an unexpected structure, a WASM-boundary issue) with a bare `catch`, so a note the user still owns on-chain would silently never appear — leaving them unable to see or report the missing funds. The failure is now logged (naming the note when its id was read) so a missing note is diagnosable; the note is still skipped for that sync and a later sync can complete it.
- [FIX][extension] **Recovering a wallet now opens in the side panel, matching first-run onboarding.** Creating a wallet handed off to the Chrome side panel on completion, but recovering an existing wallet (importing a seed with a Guardian, or the "forgot password" Guardian recovery) always finished in the onboarding tab instead. Both recovery flows now route through the same "Open wallet" handoff screen, so recovery opens in the side panel just like onboarding; non-extension builds and E2E keep the in-tab flow.
- [FIX][all] **A prover "missing procedure" mismatch is no longer misreported as a remote-prover timeout.** When the on-device prover rejects a transaction because it is missing a kernel procedure (a version/artifact mismatch between the app and its packaged prover — seen on Android as `… procedure with root digest 0x… could not be found`), the automatic remote→native fallback could relabel it as "Remote prover failed — please try again." That both hid the real cause and invited a retry that only re-fails on the same build. The transaction now shows a distinct message pointing at the app/prover version mismatch and noting that updating the app is what resolves it.
- [FIX][all] **A temporary proving-service outage no longer kills a transfer outright.** When a delegated (remote) transaction fails at the prove step — after the automatic local-proving fallback also can't complete — the transaction is now returned to the queue and retried with backoff instead of being marked Failed, so it completes on its own once the prover recovers (a proving-unavailable banner explains the wait, and the 30-minute queue cap remains the terminal limit). This is strictly a **pre-submit** recovery: a failure at the submit step is still terminal-failed, never retried, so a transaction that may already be on-chain can never be double-submitted. The failure message is also clearer — a provably pre-submit prove failure says the proving service was unavailable and no funds moved, while an ambiguous timeout says to check your balance before retrying — instead of a blanket "remote prover timeout, please try again."
- [FIX][all] **Token metadata fetched during sync is now persisted immediately.** Subsequent syncs reuse the existing metadata cache instead of repeatedly requesting the same faucet account from the node.
- [FIX][all] **A mistyped recipient address is rejected in the Send screen instead of failing after guardian approval.** Miden addresses are now validated with the SDK's full bech32 decode (`Address.fromBech32`) as you type, scan, or pick a contact — previously only the `mtst1`/`mm1`/`mdev1` prefix was checked, so a typo'd address sailed through to the transaction pipeline and surfaced as a bech32 error in Activity. A well-formed address for a different Miden network (e.g. a mainnet `mm1…` address on a testnet build) gets its own "different Miden network" message.
- [FIX][all] **The transaction-progress step durations no longer keep climbing after a step has finished.** Each step's elapsed time (e.g. "Guardian approved") was re-timed every time the underlying stage ticked — and because several stages map to the same visible step, a completed step's duration crept upward instead of freezing. Each step's start and end are now recorded exactly once, so the displayed durations settle the moment the step turns green.
- [FIX][extension] **Tapping a "note received" notification now opens the incoming-notes list instead of the wallet's QR/receive page.** The extension notification linked to the generic Receive screen, which told you nothing about the note that arrived; it now deep-links to Pending Notes, where the note and its claim action are shown — matching the mobile app's behavior. When Pending Notes is opened this way (in a fresh tab with no history), its back button now returns to the wallet home instead of doing nothing.
- [FIX][all] **The amount field now accepts a comma as the decimal separator.** On comma-decimal locales and keyboards (Spanish, German, French, …) typing `1,5` silently dropped the comma and became `15`, so you couldn't enter a fractional amount; the comma is now treated as the decimal point across Send, Swap, and Bridge.
- [FIX][extension] **A failed background sync-data write is no longer silent.** The extension's background sync wrote consumable notes and sync data to local storage without checking the result; if that write failed (e.g. storage quota exceeded) the error was swallowed and the wallet still signalled "sync complete", so open views could show stale or missing data. The write is now awaited, failures are logged, and the completion signal is only sent when the data actually persisted — the next sync retries.
- [FIX][mobile] **The "note received" notification no longer lingers after the note is auto-consumed.** Once the wallet auto-claims an incoming note, the "click to claim" notification is dismissed instead of staying around and opening to "nothing to claim" (on Android it previously had to be swiped away manually).
- [FIX][all] **The Swap screen now shows how much of a token you have available.** The "You Pay" field displays the spendable balance ("Available N TOKEN") so you don't have to leave the swap flow to check it.
- [FIX][mobile] **Passcode digits typed in quick succession are no longer dropped.** On the reveal/verify passcode screens, tapping numbers rapidly could register only the last digit; every tap is now captured.
- [FIX][all] **Production (minified) builds no longer crash on load with "React is not defined."** The SVG→React (SVGR) transform in the vite configs generated components with the automatic JSX runtime (importing nothing from `react`), but the bundler recompiles those `moduleType: 'jsx'` modules with the _classic_ runtime (tsconfig `jsx: "react"`, which neither the `esbuild` nor `oxc` JSX options override in rolldown-vite) — emitting `React.createElement` calls against an undefined `React`. Harmless in unminified dev builds (a stray `React` leaked into the chunk) but every minified production build crashed on first render. SVGR now emits the classic runtime so each generated component imports `React`, matching the compile. Applied to the extension, mobile, and desktop configs.
- [FIX][all] **Private-note delivery no longer silently fails on fast chains.** The wallet relayed a private note's transport hint _after_ waiting for the transaction to commit, so the hint (the client's sync height) could already be at or past the note's commitment block — the recipient scans forward from the hint and would never find the note. The note is now relayed _before_ the commit wait (the SDK's prompt-relay flow), so the hint stays below the commitment block and the recipient picks the note up when it lands; the commit wait is preserved so `Completed` status still requires on-chain confirmation. Latent on testnet (slow blocks kept the sync height ≈ the commitment block at relay time); reproduced deterministically on a fast devnet.
- [FIX][mobile] **Android hot-key security-audit fixes.** The Keystore hot-key plugin now works on Android 13 and earlier (the AndroidKeyStore provider only accepts MGF1-SHA-1 OAEP there, so hot keys generated on those devices could never sign; unwrap now tries both MGF1 digests, new blobs use a per-OS-compatible digest, and generation round-trip-verifies the blob before returning it), the legacy biometric fallback no longer crashes or permanently wedges into `BIOMETRIC_BUSY` on Android 9–10 (SDK-gated authenticators, negative button, and pending-state cleanup on every prompt failure and on activity destroy), Reveal Hot Key requires a fresh native biometric/device-credential confirmation before returning the secret, the TEE fallback verifies the generated wrapper key is actually hardware-backed (software-backed keys reject as `HARDWARE_UNAVAILABLE` so the JS fallback takes over honestly), hot-key deletion no longer swallows real Keystore failures, native inputs are strictly bounds-checked before decoding, the secp256k1 scalar is validated against the group order, and all four ECDSA recovery-id candidates are tried. New blobs are wrapped with MGF1-SHA-1 (the only OAEP parameter set every Android version and post-OS-upgrade key authorization accepts), and sign/reveal failures now carry stable codes (`UNWRAP_FAILED`, `KEY_INVALIDATED`, `BAD_CIPHERTEXT`, `INVALID_INPUT`) instead of misreporting OAEP/key mismatches as "Secure hardware unavailable: null" (seen on Galaxy S23 / Android 16, where the StrongBox path accepts key generation but fails the first real OAEP unwrap). Generation now proves the blob decrypts before returning it and retries on the ordinary TEE Keystore when the StrongBox attempt fails any step, so such devices get a working hardware-backed key instead of a dead blob; already-affected wallets need a hot-key rotation. Review follow-ups: cancelling the reveal prompt (or having no enrolled authenticator) is no longer misreported as a "secure hardware failure", `UNWRAP_FAILED`/`KEY_INVALIDATED` now surface a home-screen "rotate your device key" prompt that enqueues the replace-hot-key transaction, and the reveal presence prompt accepts the device credential (PIN/pattern/password) wherever androidx supports it so secure-lock-screen users without an enrolled biometric keep a working Reveal.

### Changes

- [CHANGE][all] **Frontend skill guidance now defaults standard actions to the shared wallet button.** The shared frontend skill directs standard wallet actions to `components/Button`, reserving native buttons for compact, icon-only, or inline controls that the full CTA cannot represent.
- [CHANGE][all] **Frontend implementation guidance is now shared across supported coding agents.** The repository now carries one vendor-neutral Miden Wallet frontend skill for component reuse, semantic styling, motion, accessibility, haptics, and cross-platform verification, with matching concise rules in the repository instructions.
- [CHANGE][all] **Every Guardian switch now requires fresh device authentication and leaves a provider audit trail.** Hardware-protected wallets prompt with their platform authentication, while passcode/password wallets verify the credential before anything is queued; cancellation or failed authentication leaves the Guardian unchanged. Guardian Settings now shows the active operator, endpoint, region, sync recency, and availability; the redesigned review clearly separates the current and destination providers with dark-theme-safe contrast. Activity and transaction details show the same provider transition (including custom hostnames and legacy `Unknown` sources) for pending, completed, and failed switch attempts.
- [CHANGE][all] **The balance card account footer now matches the refreshed design.** The footer labels the truncated value as an address, uses the compact outlined copy icon, and replaces the overflow glyph with a settings gear whose color follows the card's secondary tone; the account drawer's Settings action also keeps a readable foreground in dark mode.

### Features

- Add hidden developer endpoint configuration (7-tap the Welcome logo during onboarding) to override RPC / prover / note-transport / faucet / explorer / guardian endpoints and network ID; read-only view with reset-to-defaults in Settings.
- Dev-only: a "No guardian" option can be enabled from developer settings (7-tap the onboarding logo), which then shows a "No guardian" card on the Choose-your-guardian screen; selecting it creates a private single-key account with no guardian co-signer.

### Changes

- [CHANGE][ci] **The end-to-end suites now test the network that matches the branch.** Devnet and testnet run different protocol versions, so a build for one cannot pass against the other. Both used to run on every push to main, with each gate passing if EITHER succeeded — which reported green whenever one network passed, masking real failures on the other. Devnet now runs on `next` and testnet on `main`, each with a gate that fails if its own network fails.

## 1.15.19 (2026-08-05)

### Fixes

- [FIX][mobile] **Android: opening the keyboard no longer collapses the screen into a thin strip under the toolbar with a large empty gap above the keyboard.** On Android the app window is `adjustResize`, so the system already resizes the WebView to sit above the soft keyboard. The mobile keyboard-inset helper was _also_ adding a `--keyboard-height` bottom padding — needed only on iOS, where the WebView overlays the keyboard instead of resizing — which double-counted the keyboard height, so screens like the Send address/amount entry shrank to a band at the top with a blank void beneath. The `--keyboard-height` compensation is now iOS-only, and Android's `adjustResize` is pinned in `AndroidManifest.xml` so the native resize is deterministic.

## 1.15.18 (2026-08-05)

### Fixes

- [FIX][all] **The guardian picker no longer crashes when a guardian operator has no registered wordmark.** The Choose-Guardian screen asserted a logo entry for every option (`GUARDIAN_LOGOS[option.id]!`), so any guardian without a registered logo threw and blanked the screen; it now falls back to a generic avatar, mirroring the guardian-settings row's existing safe lookup.
- [FIX][all] **Assets on the Home page are ordered by fiat value.** The asset list keeps the native asset first, then ranks all other holdings by their current USD value from highest to lowest.
- [FIX][all] **Private notes now reliably arrive at their recipient.** Bumped the bundled web SDK (`@miden-sdk/miden-sdk`) to 0.15.9. Outgoing private notes are now relayed with a block hint so a recipient whose chain sync has advanced past the note's block still locates its on-chain commitment — previously such notes were silently dropped and never became consumable. This also pulls in miden-client 0.15.5's receive-side note-transport fixes (a transport delivery colliding with a note a local transaction is consuming no longer wedges sync; deliveries are validated on receipt).
- [FIX][all] **Dark-theme password and transaction screens keep their text and visibility icons readable.** Shared input labels, entered text, and icon slots now use theme-aware color tokens; both visibility-icon sets inherit the active text color instead of hardcoding black; and the transaction-processing helper no longer overrides its light/dark text token with the dark surface color. This fixes low-contrast unlock, reveal-key, send-processing, and swap-processing UI. (#430)
- [FIX][all] **A device-key rotation no longer intermittently leaves the new key unauthorized on the guardian.** After a hot-key rotation the wallet re-registers the new signer set on the guardian (this writes the guardian's request-auth allowlist); right after accepting the rotation delta the guardian can briefly reject that re-register while it canonicalizes. The old fixed 5×2s (~10s) retry budget could exhaust inside that window, silently leaving the new hot key unauthorized so every later request failed with a 401 "session expired". The re-register now retries with capped exponential backoff (~39s), comfortably clearing the canonicalization window.
- [FIX][mobile] **Bottom-sheet drawers no longer slide under the Android navigation bar.** The shared `DrawerContent` is `position: fixed; bottom: 0`, which anchors to the viewport — body's safe-area padding (mobile.html) never applies to fixed elements, so drawer CTAs (e.g. Guardian Settings' "Rotate Guardian") were clipped by the system nav bar. The drawer now pads itself with `env(safe-area-inset-bottom)` (0 on extension), and `GuardianInfoDrawer`'s per-instance workaround was removed to avoid double padding.
- [FIX][mobile] **Android launcher icon no longer renders as a cropped orange blob.** The adaptive-icon foreground PNGs drew the logo across ~66% of the canvas — right at the edge of the launcher's mask, so circular masks zoomed into the middle of the artwork. The foreground artwork is now scaled into the mask-safe area (~44% of the canvas, transparent padding), so the full logo shows inside the round/squircle mask.
- [FIX][mobile] **Seed-phrase screens block screenshots, and words are never cut off.** A new ScreenshotGuard plugin hides the seed phrase from screenshots/recordings while it's on screen (iOS secure-text-field layer trick, Android FLAG_SECURE) on the backup, reveal, verify, and import screens, the verify flow's word grid no longer truncates long words, and its note-down step gained a copy-to-clipboard button — so users copy/verify instead of saving a possibly-clipped screenshot. (#417)
- [FIX][all] **Imported seed phrases are cleaned up, and the verify-seed-phrase copy explains the procedure.** Typed or pasted seed words are now lowercased and stripped of stray whitespace (capitalized or padded words previously failed the wordlist check — this supersedes the earlier lowercase-only normalization), and a paste always fills exactly 12 slots. The verify-seed-phrase screens now say you'll note down the phrase and verify your knowledge by selecting its first and last words. (#445)

## 1.15.17 (2026-08-03)

### Fixes

- [FIX][mobile] **Bridge-in "Cross Chain" no longer hangs forever on "Preparing connection…" (iOS).** The native WalletConnect `configure` call passed `icons: APP_METADATA.icons`, but that shared `APP_METADATA` object is also handed to the web `@reown/appkit` modal (`createAppKit`), which wraps it in a reactive proxy in place — so `APP_METADATA.icons` is no longer a plain array. Capacitor's iOS bridge **silently drops** a plugin call whose arguments contain such a non-plain array, so `configure` never reached the native plugin and its promise hung forever, blocking `present()` and leaving the EVM connect stuck on "Preparing connection…". The arguments now pass a fresh plain array (`icons: [...APP_METADATA.icons]`), so the call serializes cleanly across the bridge. (The web E2E suite missed this because it drives the `connectUri` test hook, not the UI `configure` path.)
- [FIX][mobile] **Deduplicate `@capacitor/core` in the mobile bundle.** Without a `resolve.dedupe` entry, Rollup inlined a second copy of the Capacitor runtime into the walletconnect chunk; only one `createCapacitor()` becomes the live `window.Capacitor`, so any plugin that resolved against the other copy dispatched into a dead bridge. Added `resolve.dedupe: ['@capacitor/core']` in `vite.mobile.config.ts` (the same guard the repo already applies to `dexie` / `@miden-sdk`).
- [FIX][all] **Guardian Epoch fast-bridge sends now build a recallable P2IDE collateral note.** The Guardian send proposal could only mint a plain **private P2ID**, so the Epoch allocator rejected every Guardian fast bridge — it can't read a private note on-chain ("Miden note … not found on-chain"), and a plain P2ID has no recall window for the allocator to validate. The Epoch `bridged-send` branch now builds the same **public, recallable P2IDE** send request the standard-account path uses (from the row's recall height) and routes it through a custom multisig proposal, instead of `createP2idProposal` (which has no reclaim-height option). Regular Guardian sends are unchanged.
- [FIX][all] **Native-asset notes auto-consume in the background, not only on the Home screen.** Native (MIDEN) note auto-consume previously ran only in the Home page's effect, so an asynchronously-delivered native note — e.g. an Earn-withdraw payout that lands minutes later while the user is on a status screen or has the extension popup closed — sat in Pending until the user opened Home or tapped Claim. Native-note auto-consume now also runs in the background: the extension service worker (`sync-manager` `runSync`) and an always-mounted `NativeNoteAutoConsumeManager` on mobile/desktop consume eligible native notes on every route, mirroring how swap settlement is already split across the two contexts. The service worker (which has no `localStorage`) reads the auto-consume toggle from a platform-storage mirror, defaulting ON so existing users are unaffected, and a note about to auto-consume no longer triggers a redundant "click to claim" notification.
- [FIX][all] **Guardian sends now honor the expiration date (recallable P2IDE) instead of silently dropping it.** The multisig client's send proposal is P2ID-only, so on Guardian accounts the user-picked expiration never reached the chain — every guardian send was permanently non-recallable. Recallable guardian sends now go through a custom proposal built from a P2IDE send request (bytes persisted for retry, same rule as PSWAP), converting the relative recall offset to an absolute height at build time.
- [FIX][all] **Expiration picker: near expirations render precisely and past times are rejected.** The review row shows the recall window derived from the relative recall offset that is actually sent — under 3 minutes in seconds, under 30 minutes in exact minutes — so the displayed window always matches what's broadcast (previously it formatted the bare date, so a same-day expiration under 30 minutes away showed a nonsense coarse label). The drawer's time input rejects a time already in the past for the selected date — the time turns red with an inline message, and Confirm and dismissal are blocked until it's corrected.
- [FIX][all] **Future-gated notes are hidden until consumable, and self-sends are blocked.** The claimable-notes listing now uses the SDK's consumability-annotated query instead of the ungated `listAvailable`, hiding notes only consumable at a future block (a sender-side P2IDE reclaim before its height — attempts on those fail on the kernel's reclaim assertion, #308). The send flow additionally rejects sending to the account's own address (typed, scanned, contact-picked, or deep-linked into review) — a P2IDE to yourself consumes through the kernel's target branch, so recall semantics are meaningless and auto-consume would immediately claim it back.
- [FIX][all] **Send expiration (Recall Height) now lands at the chosen date instead of ~2× chain height.** The send flow's date picker computed an ABSOLUTE reclaim block height while the SDK-interface layer treated `recallBlocks` as a relative offset and added the current sync height again — so every recallable note's on-chain reclaim height was roughly doubled (a "30 minutes" recall became recallable ~9.6 days out at current testnet height) and every earlier recall attempt failed with "reclaim block height is not reached yet". The UI now produces a relative blocks-until-recall offset, matching the dApp and bridge paths, and the interface layer remains the single place that converts to an absolute height. This also removed the UI's block-height RPC fetch, whose silent failure used to send the default 7-day expiration with NO recall height at all (note not recallable, ever) — the default is now seeded synchronously and always applies. Already-sent notes can't be repaired (the height is baked into the note); they stay claimable by recipients and recallable once the erroneous height passes. (#308)

### Features

- [FEATURE][all] **Earn deposits now work on Guardian accounts.** Opening an Earn position from a Guardian account was blocked with "Earn deposits aren't available on Guardian accounts yet" because the collateral must be a recallable **P2IDE** note (with a reclaim height) and the multisig client's send proposal is P2ID-only. The deposit now routes through a custom proposal built from a P2IDE send request to the Epoch allocator — the same mechanism that made guardian sends recallable in 1.15.17 — converting the relative recall offset to an absolute height at build time and reusing the persisted request bytes across propose/sign/retry. The Deposit CTA is no longer gated on account type.
- [FEATURE][all] **Send a plain P2ID note ("Never" expiration).** The send flow always attached a reclaim/expiration height (defaulting to 7 days → a P2IDE note), with no way to remove it. The "Expiration Date" picker now offers a **"Never"** option that clears the reclaim height, so the send goes out as a plain **P2ID** note (recipient keeps it; the sender has no recall window). Default behaviour (7-day recall → P2IDE) is unchanged unless the user picks "Never".
- [FEATURE][all] **Your guardian is detected automatically when you recover from a seed phrase.** Entering your seed phrase now probes every known guardian operator in the background and preselects the one that actually holds your account — picking the highest account nonce, so after a guardian switch the new operator wins over the old one's stale state. The recovery-method screen shows the detected operator with a "use a different one instead?" disclosure over the classic picker, falls back to the manual picker (with a retry link) when nothing is found, and the Forgot-password recovery flow adopts the detected guardian silently.

## 1.15.16 (2026-07-30)

### Features

- [FEATURE][mobile][desktop] **Re-enabled bridge-in (Receive from EVM).** The "Cross Chain" deposit entry on the Receive page and the `/bridge/deposit` flow now ship to users — previously hidden behind a launch flag (`isBridgeDepositEnabled` gated on `MIDEN_E2E_TEST` / `MIDEN_ENABLE_BRIDGE_UI`).

### Fixes

- [FIX][mobile] **Inputs no longer hide behind the soft keyboard.** The keyboard height now insets the layout (`--keyboard-height` CSS var driven by `@capacitor/keyboard` events), so bottom-pinned inputs/CTAs ride above the keyboard with a synced animation; the iOS accessory bar (Done) is enabled — the config previously set the unrelated `resizeOnFullScreen` key — and search/amount/recipient fields set `enterKeyHint`.
- [FIX][extension] **A send interrupted by closing the browser mid-prove is now failed on restart instead of hanging on "Sending" for ~30 minutes.** An orphaned in-flight send kept its `processingStartedAt`, so the 30-minute stuck-transaction reaper wouldn't touch it and the loop skipped it as "in progress". A `runtime.onStartup` sweep now fails any still-in-flight transaction on cold start with a clear message so the user can retry immediately.

## 1.15.15 (2026-07-30)

### Features

- [FEATURE][all] **Koda (Kodax) guardian operator.** New testnet guardian option with its brand wordmark in the guardian picker; guardian operator logos are now a shared module and the Guardian settings screen shows the current operator's logo — and resolves the endpoint per-account first, fixing the stale pre-switch operator name shown after switching guardians.

### Fixes

- [FIX][mobile][android] **Android on-device (local) proving now works.** The Android native prover was built from a divergent pre-release miden revision, so its transaction-kernel MAST roots didn't match the SDK's — every local prove was rejected with `procedure with root digest … could not be found` (surfaced to the user as a proof-generation failure). Re-pinned the native-prover crate to the published `miden-client 0.15.4` the wallet's `@miden-sdk/miden-sdk` is built from and rebuilt the JNI `.so` binaries. Default (delegated/remote) proving was unaffected; only users who opted into local proving hit this.
- [FIX][mobile] **Failed on-device (local) proving no longer misreports as a "Remote prover failed" timeout.** The transaction error text was hardcoded to blame the remote prover for any failure at the proving stage — so a failed local/native prove (e.g. a Guardian tx with delegated proving turned off) showed a misleading "Remote prover failed — timeout" message. The message now reflects the prover that actually ran (remote when proving was delegated, local otherwise).
- [FIX][all] **Connectivity-state hook no longer render-loops on fresh profiles.** `useConnectivityState` passed an inline `{}` fallback to `useStorage`, so while no connectivity banner had ever been dismissed the hook received a new object every render and its storage-sync effect re-set state forever ("Maximum update depth exceeded" spam on every platform). The fallback is now a stable module-level constant.
- [FIX][mobile] **The Explore page now shows both faucet tiles.** The grid was rendering through a stale `id`-based filter (`swap-faucet`/`faucet`) that no longer matched the curated `EXPLORE_GRID_DAPPS` set (`faucet` + `forkchoice-faucet`), so only the first faucet tile appeared. The grid now renders `getExploreGridDapps()` directly.

## 1.15.14 (2026-07-29)

### Fixes

- [FIX][mobile] **Guardian transactions with local (non-delegated) proving no longer freeze the UI on iOS.** The guardian pipeline drives the raw client directly, and its local-proving branch always used the single-threaded WASM prover — which on iOS WKWebView runs on the main thread and locks the UI for the whole multi-second prove (~13s). It now routes to the native Rust prover on mobile (off the main thread), matching the non-guardian path and the guardian delegated-proving fallback.

## 1.15.13 (TBD)

### Features

- [FEATURE][all] **Earn: Epoch lending positions, end to end.** Opening a position (`earn-deposit`) sends a recallable collateral note to the solver's allocator and tracks the solver-fulfilled lending leg; Smart Withdraw (`earn-withdraw`) redeems the position and bridges the underlying back to Miden as one gasless intent, tracked through a Redeeming → Delivering → Received lifecycle (Failed is retryable by re-submitting the redeem intent). Both types render throughout the wallet: the earn deposit/withdraw screens, an in-progress summary badge (`{amount} {token} ↑ {protocol}-USDC`), dedicated Activity rows (the withdraw row is the single trace — its delivery consume is suppressed), and Deposit/Withdrawal detail cards with market, position owner, intent nonce and Sepolia/Miden links.

### Fixes

- [FIX][mobile] **iOS no longer crashes at launch / wallet load.** 1.15.12 dropped the `group.com.miden.wallet` App Group from the entitlements (it belongs to a former Apple team and isn't provisionable under the current one), but the native Reown/WalletConnect plugin still initialized its relay against that group — so on a real device the group's keychain / `UserDefaults(suiteName:)` ops failed and the app trapped (`EXC_BREAKPOINT` / `swift_unexpectedError`) within ~1s of launch, as soon as any EVM/bridge-aware screen mounted. Reown now uses the app's own already-entitled keychain access group (`$(AppIdentifierPrefix)com.miden.bread`), which needs no App Group or provisioning-profile change.

### Changes

- [CHANGE][extension] **Dropped the `tabs` permission from the extension manifest.** Nothing in the wallet reads other tabs' URLs/titles — `tabs.create/query/update/remove` and matching our own extension-page URLs work without it — so the permission was pure over-privilege (its "Read your browsing history" install warning was already subsumed by the content-script warning).

## 1.15.12 (2026-07-28)

### Features

- [FEATURE][all] **Failed transactions surface in Activity with friendly errors and retry.** The history list now includes failed rows (with user-cancelled ones rendered as grey "Cancelled" rows, and failed sends/receives kept under the Sent/Received filters); the details page shows the failure reason — opaque prover timeouts are rewritten to a friendly message with the raw error behind a "Show full error" disclosure — plus a Cancel button for still-queued rows and a Retry button that re-queues supported transaction types through the processing loop.
- [FEATURE][all] **Auto-settle swap order notes.** A settled order stays a single swap activity row (its settlement consume is suppressed) whose chip tracks settlement (Pending → Confirmed / Reclaimed), with the claimed and reclaimed note ids listed on its details page. Swap paybacks and expired-order remainders are consumed automatically by the swap lifecycle (per-order expiry + auto-consume controls on the swap review screen); swap-managed notes are hidden from the generic Pending Notes / Claim All flow. Swap quotes now come from the DEX `swap-eta` endpoint, seeding the receive field and driving the review Rate / fill-ETA rows.
- [FEATURE][all] **Wallet prompt system on Home ported from the earn branch.** `lib/wallet-prompts` now drives every Home prompt type: fund-wallet faucet prompt (mints from forkchoice + the official Miden faucet API in parallel, with PoW solving in `lib/miden-chain/faucet-api`), an active-bridge prompt that polls in-flight `bridged-send` transactions (Agglayer claim readiness / Epoch intent fills) and links to the newest one's history details, the verify-seed-phrase prompt, and a mobile-only hot-key hardware-failure report prompt (native hot-key op failures are recorded via `secure-hot-key` and surfaced with a copy-error action). `PromptCard` gains an inline action CTA with loading/success/failure status; `PromptCarousel` gains a drag-vs-click guard and late-mount track measurement.
- [FEATURE][all] **Receive-from-EVM bridge deposit flow.** The deposit amount screen gained a "wallet connected · Miden Bridge" header (via a new optional `title` slot on `SelectAmount`) and an ETH/USDC token picker (bottom-sheet drawer). Token choice constrains the route: USDC disables the native-only Slow (Agglayer) route, and ETH on the Fast (Epoch) route shows a "wraps to WETH" notice (that combo isn't submittable yet — the wrap is a follow-up). Choosing a route then opens a "Review Deposit Details" screen (built on the shared `ReviewLayout`/`ReviewRow`/`ReviewAmount`, same design as the Send review) with amount, source network, route, and the quoted amount received on Miden; its **Confirm Deposit** button runs the bridge submit (`executeEVMToMiden` for Fast/Epoch, the Agglayer `bridgeAsset` call for Slow), spins while signing, and blocks re-submits once in flight.
- [FEATURE][all] **Inbound EVM → Miden bridges are tracked end-to-end.** A deposit now inserts a tracking-only `bridged-receive` row whose phase (submitting → delivering → ready → received / failed) survives closing or reloading the wallet: the deposit screen waits for the EVM receipt, a reconcile pass on Home resumes Epoch intent / AggLayer indexer polling (including the `ready_to_claim`/`finalized` finality fields), and the delivered Miden note's consume is linked back to the bridge row — which renders as a single bridge entry in prompts, activity history, and the transaction details page. The deposit screen itself gained a dedicated submission/delivery status view, plus a connected-wallet header with a switch-wallet drawer.
- [FEATURE][all] **Pending-notes home prompt and redesigned Pending Notes summary.** The home carousel leads with a dismissible prompt showing the claimable notes' USD total ("See Now" opens Pending Notes; dismissing hides the current batch of note ids and resurfaces for new ones), and the Pending Notes page shows a Total Pending USD headline with per-asset rows (token amount, USD value, incoming-transfer count) over a full-width Claim All button.
- [FEATURE][mobile] **Pull-to-refresh on Home.** A downward pull from the top of the Explore list refreshes balances and claimable notes, with a threshold indicator and horizontal-gesture protection so the pane carousel keeps working.
- [FEATURE][all] **Send recipient and amount selection refined.** The recipient step now shows saved-contact identity, uses a destination-network drawer for valid Ethereum recipients, offers compact Address Book and supported-mobile QR scan actions, and requires Sepolia selection before continuing with a cross-chain send. The amount step uses a pill-style token selector and only shows its accent divider after both an amount and token are selected; route confirmation stays anchored at the bottom.

### Changes

- [CHANGE][all] **Receive-from-EVM bridge deposit is hidden pending launch.** The "Cross Chain" entry on the Receive page and the `/bridge/deposit` screen are gated behind a feature flag (`lib/feature-flags` `isBridgeDepositEnabled`), off for users. It stays enabled in E2E builds (so the bridge-in e2e keeps running) and can be turned on for manual testing with `MIDEN_ENABLE_BRIDGE_UI=true`. Send-to-EVM (typing a 0x recipient in Send) is unaffected.
- [CHANGE][all] **Swap asset selection and order tracking are more transparent.** The swap picker now includes the network-discovered native MIDEN asset with its actual decimals, while native-note auto-consume explicitly leaves swap-managed notes to their lineage-aware settlement path. Swap activity details now refresh active lineage and settlement-note state every 2 seconds and show a visible loading indicator during each lineage request.
- [FIX][all] **Guardian transactions survive a slow delegated prover.** When guardian proving is delegated to the remote prover and the proof exceeds its gRPC deadline (heavyweight multisig proofs can, under load), the co-signed transaction now re-proves with the local prover instead of failing outright — the same fallback the non-guardian path already had. This clears a guardian consume 409 "proposal conflicted" canonicalize-retry loop and claim timeout that surfaced in the local/swap e2e suites on this branch.
- [FIX][all] **Ported the non-Earn reliability fixes from the Earn integration branch.** Narrow layouts keep segmented actions readable; failed bridges stop polling and render as failed; AggLayer notes use the current bridge/faucet accounts with asset callbacks; faucet metadata is decoded from account storage; notification taps open pending notes; batch claims preserve every note and total their displayed amount; sync-banner retries run immediately and dismiss only the current failure episode; temporary PSWAP clients are terminated; and iOS full-screen routes repaint the bottom safe area after hiding the navbar.
- [CHANGE][all] **Transaction history details redesigned.** The detail page's hero now reuses the shared transaction summary badge (`{amount} {token} → {recipient}`) for sends, each section is introduced by a colored divider drawn from the transaction icon's accent, and the section cards drop their bordered shell for a compact pill title over plain key/value rows. The Notes section's on/off note-type row is gone.
- [CHANGE][all] **Hot-key rotation for recovered accounts is now automatic and blocking.** When the current account carries `requiresHotKeyRotation` (seed-phrase recovery, legacy migration, or an interrupted rotation), the wallet auto-initiates the `replace-hot-key` transaction and shows a full-screen blocking overlay until it lands; on failure the overlay shows the reason with a Retry button. The manual `ActivateHotKeyBanner` on Home is removed (the Settings rotation button stays). Also fixes the permanent post-rotation guardian 401 ("session expired"): completion now re-registers via a cold service rebuilt from the post-rotation account so the guardian's `/configure`-only request-auth allowlist gains the new hot key.
- [CHANGE][all] **Merged the in-protocol-DEX branch and re-homed the Ethereum bridge onto its send flow.** The swap flow, the split `lib/miden/transaction/` module and the redesigned transaction-progress view land alongside the bridge work. Cross-chain sends now follow the same shape as same-chain ones: the destination network moved onto the recipient step (Miden for a bech32 address, Sepolia for a `0x` one), a 0x recipient then picks Fast (Epoch) vs Slow (Agglayer) on a Route step, and the full-screen `/send/review` page owns the whole submit pipeline for both routes plus the live "you receive" USDC quote. `bridged-send` rows are carried into the new transaction module (initiate/complete/Guardian-proposal arms), and batch consume (`Claim All`) moves there too.
- [FIX][all] **Token detail Send/Receive buttons follow the theme.** They used one-off hard-coded navy/green fills (meant for history amount text) and now use the design-system primary/secondary button pair, so they track the network accent and light/dark themes; their icons inherit the button's text color.
- [CHANGE][all] **Recovery is seed-phrase only.** "Recover your account" now jumps straight to the seed-phrase entry screen; the import-type chooser and encrypted-wallet-file import path are removed.
- [CHANGE][all] **The Explore screen now shows only the two testnet faucets.** The curated Explore grid is trimmed to the existing Miden faucet plus a new "Forkchoice Faucet" tile (`faucets.forkchoice.xyz`), giving users two sources of testnet MIDEN; the other curated tiles (Zoro, Qash, Miden Name) are no longer shown in the grid.

### Fixes

- [FIX][extension] **Forkchoice faucet mints are allowed by the extension manifests.** `https://faucet-api.forkchoice.xyz` is granted as a host permission in both MV2 and MV3, and the background / content-script partial builds set `publicDir: false` so they can no longer overwrite the transformed extension manifest with the raw one.
- [FIX][android] Added ProGuard `-dontwarn` rules for JVM-only classes referenced by Reown/WalletConnect transitive dependencies (jackson-databind, junit, slf4j, msgpack) so release builds pass shrinking.
- [FIX][extension] **Swap E2E settlement checks now drive the service-worker auto-consume path.** Full-fill payback and partial-fill expiry assertions explicitly trigger sync and transaction processing while polling, instead of relying on a best-effort page interval that could be delayed by page lifecycle or transaction work.
- [FIX][mobile] **Import/recover flow asks for a passcode, not a password.** Recovering a wallet on mobile (both onboarding and forgot-password) now routes to the 6-digit passcode setup screen when hardware security isn't available, matching the create flow instead of showing the extension/desktop full-password screen.
- [FIX][all] **Failed Guardian transactions no longer leave an account locked behind a pending candidate.** The Guardian clients are upgraded to 0.16 and the transaction pipeline now requests `abandonCandidate` with the proposal nonce when execution, proving, or submission fails. Guardian verifies during its quarantine that an ambiguously submitted transaction did not land before releasing the account, while cleanup errors preserve the original transaction failure.
- [FIX][extension] The Receive screen's EVM entry point is now a "Cross-chain" action (icon row next to Share, replacing the "From another chain?" bordered button) and is hidden on the browser extension, where WalletConnect cannot connect (the Reown relay rejects the extension bundle's auth JWT with WebSocket close 3000).
- [FIX][all] **Receive-from-EVM bridge deposit keeps its state across the amount → route step.** The Receive screen drove the bridge deposit's two steps (amount entry, route selection) as separate outer-Navigator cards that both rendered a fresh `BridgeDeposit`, so advancing to the route step remounted `EvmBridgeDepositScreen` with empty state — the entered amount and Epoch quote were wiped and Confirm dead-ended. The bridge deposit now runs its own nested `Navigator` (mounted once, mirroring `SendManager`) so state survives step changes; the outer Receive navigator collapses to a single `ShowBridgePage` card, which also fixes the standalone `/bridge/deposit` route that previously threw for rendering without a `NavigatorProvider`.

## 1.15.11 (2026-07-26)

### Changes

- [CHANGE][ios] **In-app swap is now available on iOS.** The In-Protocol DEX swap — previously disabled on iOS under App Store Review Guideline 3.1.5(iii) — is re-enabled, so the swap tab, the home swipe pane, the `/swap` route, and the featured swap dApps are present on iOS just as on Android, the browser extension, and desktop. Availability stays gated by the single `isSwapEnabled()` flag.

### Fixes

- [FIX][mobile] **Tapping a home action (Send/Receive/Overview) slides between the carousel pages more smoothly.** The horizontal track is now pre-promoted to its own compositor layer (`will-change: transform`), so a programmatic slide no longer pays for layer creation — a full repaint — on its first frame; a finger drag was already on a live layer, which is why swiping felt smoother than tapping.
- [FIX][mobile] **The unfinished Earn screen is no longer reachable by swiping between the home pages.** Earn was mounted in the home swipe carousel and appeared when swiping past Receive; it is now feature-gated behind `isEarnEnabled()` (off) — the single source of truth for the home swipe pane and the top action-bar segment — so it stays hidden until the feature ships. Swap remains enabled everywhere.

## 1.15.10 (2026-07-23)

### Changes

- [CHANGE][mobile] The native hot-key plugins now reject with typed error codes: `HARDWARE_UNAVAILABLE` when the secure hardware (Keystore/StrongBox on Android, Secure Enclave on iOS) genuinely can't be used, and `DEVICE_LOCKED` (iOS) when the unwrap merely ran while the device was locked (`errSecInteractionNotAllowed`, a transient state callers should retry). JS-side report-prompt UI lands separately. The secure-hot-key facade now falls back to the JS (vault-envelope) hot-key implementation when native generation rejects with `HARDWARE_UNAVAILABLE`, so onboarding succeeds on devices without usable secure hardware; per-key operations route by blob format (native `<tag>:<payload>` vs JS hex) instead of by platform.

### Fixes

- [FIX][mobile] **iOS hot-key signing no longer fails with `errSecInteractionNotAllowed` (OSStatus -25308) when guardian sync signs while the device is locked.** The Secure Enclave hot key was minted `kSecAttrAccessibleWhenUnlockedThisDeviceOnly`, so the ~3s AutoSync tick's unwrap failed any time the screen was off/locked. New keys use `kSecAttrAccessibleAfterFirstUnlockThisDeviceOnly` (usable after the first post-boot unlock, still non-migratable); existing keys keep the old access control until the hot key is rotated.

- [FIX][mobile] **Android hot-key signing no longer fails with `INCOMPATIBLE_MGF_DIGEST`, leaving consume/claim stuck on the pending page.** The Keystore RSA-OAEP wrapper key was authorized for the SHA-256 digest only; pre-Android-13 Keymaster reuses that same digest list to authorize the MGF1 function (default SHA-1) and refuses any OAEP-MGF1 op unless SHA-1 is present, so hardware decrypt threw while software public-key encrypt silently succeeded (generate looked fine, sign failed). The wrapper key now authorizes both SHA-256 and SHA-1 digests, and explicitly authorizes both MGF1 digests on Android 15+ (`setMgf1Digests`). Existing keys minted before this fix must be regenerated (rotate the hot key or re-add the account).

## 1.15.9 (2026-07-22)

### Features

- [FEATURE][extension] **The custom-transaction confirmation now shows what the transaction actually does instead of an opaque "custom transaction" notice.** The wallet decodes the request for an instant declared preview, then runs a local dry-run (`executeForSummary`, no prove/submit) to show the ground-truth asset changes (what you send / receive), notes consumed/created, and storage impact — with the raw request under an Advanced disclosure. Genuinely-opaque signature requests (blind word / arbitrary / blind-commitment) now carry an explicit "you are blind-signing" warning so they are visually distinct from real transactions.

### Fixes

- [FIX][extension] **The consumable-notes cache is now scoped per account, so switching accounts no longer auto-consumes the previous account's notes.** The extension cached consumable notes under a single wallet-wide key and the claimable-notes hook ignored the account argument, so after an account switch account A's notes were served to — and auto-consumed under — account B without any user action. The cache read is now guarded by the account the notes belong to and cleared on switch.
- [FIX][all] **Restoring an encrypted wallet file now writes into the client's active database, so balances appear instead of staying 0.** The restore path imported the miden-client dump into a hardcoded store name (`miden-wallet`) while the running client reads from `MidenClientDB_<network>`, so the restored account/balance state was invisible (sync logged "No account header record found"). Restore now targets the active client's store name.
- [FIX][all] **Guardian consume/send no longer stays permanently "Failed" after the transaction already landed on chain.** When a guardian value-moving transaction submitted successfully but the local apply step then failed (`ApplyTransactionAfterSubmitFailed`, e.g. the account was briefly locked), the guardian branch cancelled it to a terminal `Failed` state even though the note was consumed on chain — losing track of the funds locally until the next reconcile that never came. Guardian consume/send/swap/execute now mark the transaction `Completed` in that case (matching the non-guardian path), so the next sync reconciles the note via `ConsumedExternal`.
- [FIX][extension] **The side panel no longer clips its right edge in Brave's narrow sidebar.** The side-panel document hard-clamped its root to `min-width: 360px` with hidden overflow, so any host giving less than 360px (Brave's sidebar, whose icon rail eats width) clipped the balance, Faucet button, and token amounts with no scrollbar. The floor is removed so the layout shrinks to the host viewport.
- [FIX][all] **Guardian transactions that hit a transient pending-delta conflict now requeue and retry instead of failing.** A guardian `409 conflict_pending_delta` that outlasted the ~60s inline retry budget fell through to a terminal `Failed`, even though the conflict is transient (a single in-flight delta clears on its own). Such transactions are now reset to `Queued` (with `processingStartedAt` cleared) so the processing loop retries them, bounded by the existing 30-minute queued-age cap.
- [FIX][extension] **The stuck-transaction heal alarm now recovers a closed database and surfaces failures instead of silently swallowing them.** After an MV3 service-worker respawn left a stale Dexie handle, the heal alarm hit `DatabaseClosedError` on every tick and only `console.warn`'d it, so it never recovered and there was no diagnostic surface. It now re-opens Dexie and retries on `DatabaseClosedError`, and persists a diagnostic record (`stuckTxHealDiagnostic`) to `chrome.storage.local` on persistent failure — a sink that works in the MV3 service worker and survives a broken IndexedDB — instead of swallowing them.
- [FIX][all] **Seed-phrase restore now validates the BIP-39 checksum.** The import screen only checked that each word was in the wordlist, so a checksum-invalid phrase (e.g. the same valid word repeated 12 times) passed and silently derived a brand-new, unrelated wallet while the user believed they were restoring an existing one. Restore now runs `validateMnemonic` on the full phrase and rejects invalid checksums with a clear error.

## 1.15.8 (2026-07-21)

### Features

- [FEATURE][all] **Guardian accounts now auto-detect and reconcile out-of-band guardian switches; dApps can read guardian info via `requestGuardianInfo()`.**

### Changes

- [CHANGE][all] Internal refactor: the transaction pipeline moved from `src/lib/miden/activity/transactions.ts` into `src/lib/miden/transaction/` split by lifecycle (`initiate` / `complete` / `get` / `cancel` / `helper`, with `generateTransaction` and the processing loop in `index.ts`); the `lib/miden/activity` barrel re-exports it so consumers are unaffected.

- [CHANGE][all] Redesigned the swap flow into a multi-step wizard — a You Pay / You Receive amounts screen (two stacked `SelectAmount` fields with a swap-direction toggle), a "Review Swap Details" screen (composed two-amount hero with a live Rate row), a swap variant of the transaction-progress view ("Generating Swap" title + a logo-less two-tone token→token summary badge), a "Swap Order Created!" success screen (summary pill, Expiration Date row with the funds-return note, "sourcing liquidity" footer, Done + View in Activities), and a bespoke Activities row ("Swap {offered} → {requested}" / "Via In Protocol Dex" with the requested amount on the right). Extracted the fixed devnet DEX token registry and price-quote logic into `lib/miden/swap/tokens.ts`, replacing the old single-page native-select swap UI. Swap details in Activities add a live order-tracking card (order status / fill rounds / amount filled, polled via `trackOrderId`), and the history status pill is driven by the transaction's actual status (adding a Failed state) instead of message sniffing. "Usually fills in", "Expires", and "Network fee" are static placeholders pending backend swap-quote metadata.

- [CHANGE][all] The transaction progress view now shows a per-step duration ("2 sec") on each completed step row, derived from the stage transitions observed while the screen is open.

- [CHANGE][all] The generating-transaction page is now addressed by transaction id (`/generating-transaction/:txId`) and observes that single transaction row directly instead of guessing which one to show by scanning the uncompleted-transactions queue. Completion and failure come straight off the row's status (`Completed` / `Failed`), removing the queue-emptying and failed-row-counting heuristics. The FIFO processing loop and the page's driver are unchanged.

- [CHANGE][all] Animation polish pass: hover/press color feedback across buttons, list rows, chips, and inputs sped up from 300ms to a tokenized 150ms `ease` curve (`ease-hover`); dropdown close and react-modal transitions now use `ease-out` (modals settle from `scale(0.96)` instead of zooming from `0.75`); the toggle knob animates via `transform` instead of layout-thrashing `left`; and `prefers-reduced-motion` is honored by the page navigators, the Settings seed-phrase overlay, the mobile page slide-in, and the sync shimmer (movement dropped, fades kept). Audit plans live in `plans/`.

- [CHANGE][extension] **Refreshed the network icons.** The default/testnet build's orange bread "B" is regenerated from a new transparent-background master (dropping the old opaque white card and padding so the B fills the frame), and the devnet icon is redesigned from the orange "B" with a blue corner wrench to a sage-green "B" with a centered developer wrench, so devnet is easy to tell apart from the default build at a glance. All sizes (16/32/40/48/128/234) were regenerated for both `logo-white-bg*` (default) and `logo-devnet*` (devnet) from 1536px masters with gamma-correct (linear-light) downscaling. Affects the Chrome extension (manifest icons + toolbar `action.default_icon`, notifications, and the full-page view) and the Tauri desktop window favicon; mobile is unaffected — it uses separate native app icons (`AppIcon`/`ic_launcher`) and `misc/brand/*` splash assets. Icon-swap wiring in `vite.extension.config.ts` is unchanged.

- [CHANGE][mobile] **The in-app swap is disabled on iOS.** Apple App Review classifies the In-Protocol DEX swap as a cryptocurrency exchange service under Guideline 3.1.5(iii), which requires per-storefront licensing the app does not yet hold, so the iOS build ships as a pure non-custodial wallet with no exchange surface. Swap availability is now gated by a single `isSwapEnabled()` flag (`!isIOS()`) applied to the Swap action-bar segment, the home swipe pane, and the `/swap` route (which redirects home on iOS); Android, the browser extension, and desktop are unaffected.

### Fixes

- [FIX][all] The transaction progress view no longer shows a spinning loader while the title already reads "Transaction completed" — a successful transaction now settles onto a green check hero for the beat before the success receipt appears, instead of reusing the in-progress spinner.

- [FIX][mobile] **iOS no longer prompts Face ID every few seconds while a Guardian account syncs.** Reverts the `.userPresence` gate (#299) on the Secure Enclave hot key: guardian sync signs with the hot key on the ~3s AutoSync tick, so the per-use presence flag turned into a continuous Face ID prompt loop. New hot keys are created with `.privateKeyUsage` only again (silent signing, key still SE-bound). Since hot signing is silent everywhere now, the `background` consume flag and its Guardian cold-key auto-consume detour (which existed only to dodge that prompt) are also removed — every consume takes the standard hot-bound path. Android gets the equivalent fix: the Keystore hot-key wrapper is no longer auth-bound (`setUserAuthenticationRequired`), so hot signing no longer pops a fingerprint `BiometricPrompt` per signature; legacy auth-bound keys keep working through a prompt fallback until the hot key is rotated.

- [FIX][mobile] **iOS now renders at the display's native refresh rate (up to 120Hz ProMotion) instead of being capped at 60fps.** Added `CADisableMinimumFrameDurationOnPhone` to `Info.plist`. Since iOS 15, iPhones cap every app — including its WKWebView — to 60fps unless the app opts in to high frame rates, so on ProMotion devices animations and scrolling felt stuck at 60Hz (the same as Low Power Mode). CSS/compositor animations and native scrolling now run at the full display rate.

- [FIX][all] **dApp transactions from a Guardian account no longer fail with "transaction is unauthorized".** A dApp/adapter supplies the account as its bare bech32 address (e.g. `mtst1…`), but `WalletAccount.publicKey` is a composite `<address>_<suffix>`; `isGuardianAccount` / `getOrCreateMultisigService` compared them with a raw `===`, which missed — so a Guardian account's dApp transaction was routed through the non-guardian path, no guardian co-signature was attached, and the on-chain guardian auth rejected it as `AUTH_UNAUTHORIZED` (e.g. registering a name on miden.name). Routing now matches through a shared `sameWalletAccountId` normalizer (both ids canonicalized to the SDK account id derived from the address portion), and `getOrCreateMultisigService` loads the SDK account by the matched account's stored `publicKey`. In-wallet Guardian sends (which already used the composite id) and single-sig accounts were unaffected.

- [FIX][mobile] **The bottom navigation pill no longer runs off the right screen edge.** The pill sits in a `flex-1` wrapper that was missing `min-w-0`, so the flex item couldn't shrink below the pill's min-content width (its wide internal padding makes that ~367px). On a 375px-wide viewport the wrapper ballooned to ~399px and the pill's right edge ran ~8px off-screen. Adding `min-w-0` lets the wrapper shrink to the row width so the centered pill fits within the viewport (verified via on-device WebView inspection: the pill's right edge moved from 383px → within the 375px viewport).

- [FIX][mobile] **Consuming/claiming a note on a Guardian account no longer freezes the wallet in "consuming" forever.** With the SDK bump to 0.15.5, the guardian transaction pipeline runs execute→prove→submit inside the SDK's `_withInnerWebClient` window; while that window is open the client runs any _other_ call inline (skipping its own serialization) and requires the caller to hold an external mutex over every other WASM path. The 5s balance poll deliberately bypassed `withWasmClientLock`, so it fired mid-consume, ran inline, and double-borrowed the WASM client's `RefCell` (`web-client` `platform.rs` "RefCell already borrowed") — trapping the single-threaded mobile WASM client so the consume never completed. The balance poll's account read now runs under a non-blocking attempt on the WASM lock and is skipped whenever the lock is held (a transaction or sync), so it can neither stall behind long writes nor race a transaction — covering the 5s poll, notification-driven background consumes, and the store's other balance-read call sites. Single-sig accounts and SDK 0.15.2 (the current store/TestFlight build) were unaffected.

- [FIX][mobile] **Guardian transactions on mobile no longer freeze the UI while proving.** The Guardian transaction pipeline proved on the single-threaded main-thread WASM prover regardless of platform or the delegated-proving setting — it drives the raw SDK client directly, whose default prover is the WASM one — so a Guardian send/consume locked the UI (buttons/navigation dead) for the entire multi-second proof. It now uses the same prover selection as every other transaction: delegated proving goes to the remote prover, and local proving uses the native iOS/Android prover (off the main thread) on mobile, so the UI stays responsive during proving. Desktop/extension proving is unchanged.

## 1.15.7 (2026-07-20)

### Changes

- [CHANGE][extension] **Chrome extension renamed to the full product name "Bread Wallet by Miden".** The manifest `name` — shown in the Chrome Web Store listing and on `chrome://extensions` — changed from "Bread" to "Bread Wallet by Miden". Devnet builds keep their automatic "(Devnet)" suffix, so they read "Bread Wallet by Miden (Devnet)" (`vite.extension.config.ts` appends the suffix to whatever `name` is). `short_name` (constrained-UI slot), the toolbar tooltip (`default_title`), and in-app naming are unchanged.

### Fixes

- [FIX][extension] **Onboarding "Your Wallet is ready!" no longer shows raw `<highlight>` tags.** The Chrome side-panel handoff completion screen (`OpenSidePanel`) rendered its title via plain `t('yourWalletIsReady')`, which returns the raw i18n string including the `<highlight>Wallet</highlight>` markup, so the tags appeared as literal text. It now renders through `<Trans>` (matching `Confirmation.tsx`), styling "Wallet" in the primary color; `Message.title` was widened to `ReactNode` to accept the styled node. Added a regression test that renders the screen with real i18n and asserts the tags are parsed.
- [FIX][devnet] **Restored the devnet developer (wrench) badge on the icon.** The v0 UI revamp (#248) rebranded the devnet icon to the plain Bread "B" and dropped the wrench badge, so devnet builds again looked identical to production. `public/misc/logo-devnet*.png` (234/128/48/40/32/16) now re-add the Advanced Settings / Developer wrench glyph in a white circle with a thin `#7286A0` ring, overlapping the B's bottom-right, so a devnet build is distinguishable at a glance. Wiring is unchanged — `vite.extension.config.ts` already swaps `logo-white-bg*` → `logo-devnet*` for `MIDEN_NETWORK=devnet` (manifest icons, action icon, and tab favicon).

### Docs

- [DOCS][all] **Privacy policy: added a dedicated Face ID / biometric data section.** Clarifies that the app collects, stores, shares, and retains no face or fingerprint data — biometric matching happens entirely in the device's Secure Enclave / hardware keystore and the app only ever receives a pass/fail result. Added in response to an iOS App Review request; the hosted policy at https://0xmiden.github.io/wallet/privacy/ is the source the Resolution Center reply quotes.
- [DOCS][all] **Added a Support page** at https://0xmiden.github.io/wallet/support/ (GitHub Pages, `docs/support/index.md`) with a contact email, GitHub issues link, feedback-form link, and a short FAQ. Satisfies the App Store Connect Support URL requirement (App Review Guideline 1.5) that was previously pointing at a page without support information.
- [DOCS][all] **Renamed the public docs product to "Bread Wallet by Miden"** (support + privacy policy titles/intros) to match the App Store listing name, so everything App Review sees is consistent. The privacy policy's biometric/Face ID wording is unchanged (it refers to "the App").
- [DOCS][all] **Play Store assets rebranded to Bread.** Replaced the outdated Miden-branded feature graphic + 512 icon in `screenshots/playstore/` with Bread branding, and added nine framed 1080×1920 Android store screenshots (`01_home`…`09_faucet`) matching the iOS marketing set. Google Play accepts up to 8 phone screenshots, so pick 8 at upload time.

## 1.15.6 (2026-07-09)

### Features

- [FEATURE][all] **v0 UI revamp.** Refreshes the wallet's visual design across onboarding, home, send, receive, and explore: new typography (Inter body, Nunito headings), brand colors, animated progress/segmented bars, a multi-step send flow (recipient → amount → review), a tabbed receive screen (Address / Pending), and shared UI primitives (`AssetRow`, `SearchInput`, `CardItem`). Guardian selection is unified into a reusable `ChooseGuardianScreen` (also used for switch-guardian) backed by a selectable operator list (`GUARDIAN_OPTIONS`). (#248)

### Changes

- [CHANGE][all] **Passcodes are now mobile-only; extension and desktop use a full password.** The extension/desktop create-wallet flow now goes Welcome → Create Password → Choose Guardian → Confirmation (no 6-digit passcode setup), and their unlock screen is a password form again instead of the numpad (desktop still tries hardware unlock first).
- [CHANGE][mobile] **Settings flows that unlock the vault now prompt with the passcode numpad on mobile.** Reveal seed phrase / private key / hot key / guardian keys, verify seed phrase, and the encrypted-wallet-file export now show the shared 6-digit `PasscodeEntry` (dots + numpad, auto-submit) on mobile passcode-protected wallets instead of a typed password field; extension/desktop keep the password input.
- [CHANGE][all] **Send review moved to its own full-screen page.** The send flow's review step now lives at `/send/review` (FullScreenPage, like the earn deposit review) and owns the whole transaction-creation pipeline; the form (recipient → amount → token) stays at `/send`, hands the inputs over via query params, and restores them on the Amount step when backing out of review. Submitting now hands off straight to the `/generating-transaction` in-progress page (tracking the tx via `?txId=`) instead of waiting silently on review. The bottom tab navbar is also hidden on the send-flow steps past recipient selection so each step's Continue button sits at the bottom of the screen.
- [CHANGE][all] **Guardian picker now shows operator brand logos.** `ChooseGuardianScreen` cards render each operator's wordmark (Open-Zeppelin, Gateway, Lambda Class) centered, with the "Operated by / Location" caption moved below the card, a "Default" banner on the recommended operator, and the selected card outlined in the brand color; logos recolor with the theme. The onboarding subtitle is shortened to "Select an option below". (#248)
- [CHANGE][all] **Send review screen restyled.** The transaction review screen drops its header and re-lays each detail row as a grey pill label above a large `font-heading` value: a "You are sending" hero caption, the full recipient address, the Miden network, and an expiration row whose "Edit" link is now inline and which explains that unclaimed funds return to the wallet automatically. Shared `ReviewRow`/`ReviewLayout`/`ReviewAmount` primitives carry the new look. (#248)
- [CHANGE][all] **Transaction success screen restyled to match the review screen.** The post-transaction receipt now leads with a "{amount} → {recipient}" summary pill (reusing the in-progress `TransactionSummaryBadge`), gives each receipt row the same grey `ReviewLabel` pill + large right-aligned value with an optional sub-line, retitles sends to "Payment Sent!", drops the header title and description paragraph, and adds a "View in Activities" secondary button. Bridge sends show the route as a speed value (Fast/Slow) with the provider as its sub-line. (#248)
- [CHANGE][all] **Activity list restyled; pending notes moved to a dedicated screen.** Activity rows now reuse the home action-bar glyphs (Send / Receive / Earn / Swap) rendered white on each glyph's own brand hue, with pill-shaped filter chips and a new pending-notes button in the Activity header. Claimable ("pending") notes moved out of the Receive tab switcher onto their own `/pending` page — Receive is now address-only.
- [CHANGE][all] **Transaction success screen is now per-type.** The post-transaction success receipt was split into a shared `TransactionSuccessLayout` shell plus per-type views (`DefaultSuccess`, `BridgeSuccess`, with `SwapSuccess`/`EarnSuccess` scaffolded) dispatched by `TransactionSuccess` on transaction type, so each type can render its own title, summary and rows. Bridged sends now also show a "Route" row naming the provider (Via Epoch / Via Agglayer). (#248)
- [CHANGE][all] **Send-flow contact picker is now a bottom sheet.** The address-book contact list opens as a fixed-height drawer over the recipient step (like the token picker) instead of a pushed full-screen sub-step.
- [CHANGE][all] **Accounts drawer on the home balance card.** The balance card's 3-dot "more" button now opens an `AccountsDrawer` bottom sheet with a Settings action (navigates to Settings) and a disabled "Add Account (Coming Soon)" placeholder. Drawers now also hide the bottom tab navbar while open. (#248)
- [CHANGE][all] **Balance-card color picker.** The Accounts drawer gains a "Card Color" section with five theme-aware swatches (slate, orange, blue, green, purple); the pick persists in localStorage (`balance_card_color`) and restyles the home balance card immediately. (#248)
- [CHANGE][all] **Balance card fits long balances.** The home balance card now shrinks the amount's font size (rem-based, 3.5rem down to a 1.5rem floor) so the full balance always fits instead of clipping, and the total is formatted with locale-aware thousands grouping (e.g. `$21,000,000.12`).
- [CHANGE][all] **Unclaimed-note indicator.** The Activity tab in the bottom nav and the pending-notes button in the Activity header now show a small red dot whenever the account has claimable notes. (#248)
- [CHANGE][all] **Pending-notes screen uses the shared `ScreenHeader`.** The standalone pending-notes page now renders the large `ScreenHeader` (title + circular back button) instead of the `PageLayout` toolbar's plain-text title. (#248)
- [CHANGE][all] **Earn deposit review moved to its own page.** The vault deposit flow now splits into two screens — an amount-entry page (`/earn/vaults/:id/deposit`) that carries the amount forward, and a dedicated review page (`/earn/vaults/:id/deposit/review`) with the projection chart, fees and "Open position" button — instead of rendering the review inline. The amount field also shows "0.00" as a proper placeholder and both confirm buttons sit snug to the bottom. (#248)
- [CHANGE][all] **Receive QR code restyled and shareable as an image.** The receive-address QR now renders via `qr-code-styling` with circular dots, rounded finder corners, the accent-primary brand color and the Miden logo centered (error correction bumped to `H` so the logo cutout stays scannable), replacing the plain black `react-qr-svg` output. The Receive screen's Share button now also attaches the QR image alongside the address text — on mobile via a cached PNG + native share sheet, and on the web via `navigator.share` files when supported (falling back to text/copy). (#248)
- [CHANGE][mobile] **Splash screens follow dark mode and drop the wordmark.** The native iOS/Android launch splash is now the centered bread mark only (no "Bread" text) on the app background — `#FBFBFB` light / `#191919` dark — via dark-appearance iOS asset variants, Android `-night` drawables and a `splash_background` color (Android 12+ no longer flashes an orange window). The pre-React `mobile.html` splash resolves the stored theme (or `prefers-color-scheme`) inline so it paints the right background before the bundle loads.
- [CHANGE][all] **Encrypted wallet file entry removed from Settings.** The Security section no longer offers the encrypted-wallet-file export flow.

### Fixes

- [FIX][all] **Reveal Private Key no longer crashes for standard accounts.** The reveal-private-key screen rendered its `AccountBanner` without an `account`, so the banner's `account.name` access threw `Cannot read properties of undefined (reading 'name')` for on-chain/off-chain accounts. (Guardian accounts route to the guardian-keys reveal, which shows no banner, so they were unaffected — which is why it looked intermittent.) The banner now receives the current account, and `AccountBanner` requires `account` as a prop so the type checker catches any caller that omits it.
- [FIX][extension,desktop] **Onboarding skips the "choose how to protect your wallet" step where biometric can't work.** On the browser extension and desktop there is no Face ID / fingerprint API, so wallet creation now goes straight from Welcome to passcode setup instead of offering a biometric option that always fails, and the onboarding progress stepper shows 3 steps (passcode → guardian → confirm) with passcode first. Mobile is unchanged — it still offers the choice and its existing "use passcode instead" fallback. (#322)
- [FIX][all] **Address-book contacts now persist and its UI follows dark mode.** `Vault.fetchSettings` was a stub returning `{}` so saved contacts were lost on relock/reload — it now decrypts the stored settings; the receive-address pill is tap-to-copy, and the address-book empty state, `CardItem` right icon and `CheckboxCircleFill` glyph no longer render black in dark mode (`currentColor` + theme-aware text tokens).
- [FIX][all] **Dark-mode gray palette tokenized.** The onboarding surface no longer stays light cream in dark mode (the `data-onboarding-root` override now has a `.dark` variant painting `#191919`), and the raw gray hexes are theme-aware CSS variables: `text-gray` (`#808080` → `#E1D8D8`), `text-gray-secondary` (`#8E8E93`), secondary buttons (`#F9F9F9` → `#484848`), `--surface-input` dark (`#363636`), and the search placeholder (white in dark). (#248)
- [FIX][all] **Light-mode app background is now pure white.** The fullpage and desktop pre-React HTML shells hard-coded the old brand cream (`#F6F4F2`) as their background; because the entry bundle blocks on a top-level WASM `await`, `initTheme()` was delayed and that cream stayed visible as the app background until React mounted. The shells now paint pure white to match the post-mount app background; onboarding keeps its `#FBFBFB` surface. (#248)
- [FIX][mobile] Guardian requests no longer CORS-fail in the Capacitor WebView: a guardian-scoped fetch interceptor (`lib/miden/guardian/native-http`) reroutes requests to registered guardian origins through native `CapacitorHttp`, since `@openzeppelin/guardian-client` hardcodes global `fetch` and most operators don't emit `Access-Control-Allow-Origin`.
- [FIX][mobile] Activity rows for a newly received token no longer show `MIDEN` / 6 decimals: metadata fetched by the claimable-notes hook is now persisted to `tokens_base_metadata` storage (which history reads) instead of only the in-memory store.

## 1.15.5 (2026-07-04)

### Fixes

- [FIX][all] **`window.miden.signBytes` now works for Guardian accounts** (external-multisig / Guardian `/configure` signer registration). A Guardian account's signing key lives under the secure-hot-key facade (keyed by `hotPublicKey`), while `connect` hands the dApp the signer _commitment_ — so the dApp sign path (`vault.signData`) looked the key up under the commitment, found nothing, and threw `INVALID_PARAMS: Some storage item not found` immediately after the user approved the signature. `signData` now routes `word`-kind requests for Guardian accounts through the hot (secure-hot-key) ECDSA signing path, resolved via the request's `sourceAccountId`, and returns the full serialized `Signature` that Guardian `/configure` accepts — matching the plain-account path and `@openzeppelin/miden-multisig-client`'s `MidenWalletSigner`, which strips the leading scheme-tag byte itself to recover the raw ECDSA signature Guardian verifies. Plain (non-Guardian) accounts are unchanged.

## 1.15.4 (2026-07-04)

### Features

- [FEATURE][all] Choose among multiple Guardian providers (OpenZeppelin, Gateway, or Lambda Class) during create onboarding, import, and Switch-Guardian settings, instead of always defaulting to OpenZeppelin.

### Fixes

- [FIX][all] **Custom (`execute`) dApp transactions work again.** The internal custom-proposal label defaulted to `'custom transaction'`, which the updated `@openzeppelin/miden-multisig-client` rejects (`proposalType '…' must be lowercase snake_case`), so every dApp custom transaction failed with a `WalletTransactionError`. The default is now the valid `'custom_transaction'`.
- [FIX][build] `MIDEN_NETWORK=localhost` (the E2E localnet build token) now resolves to the `localnet` wallet network instead of throwing in `getRpcEndpoint()`.
- [FIX][all] Switching a Guardian operator now actually moves the account to the new endpoint (front-end persists the per-account endpoint through the backend, guardian sync waits out canonicalization instead of re-registering the old operator every ~3s, and the settings screen reads the current endpoint reactively).
- [FIX][ci] **Stress-test conservation check no longer reports phantom "notes lost".** The final balance was measured with the non-invasive `quickBalanceSnapshot()` (which never calls `fetchBalances`) while the initial baseline used `getBalance()` (which does). The settle loop's `triggerSync()` auto-consumes pending notes, moving their value out of `miden_sync_data.notes` and into the account vault — but the stale Zustand `balances` the snapshot reads never picked that value up, so every note consumed during settle dropped out of `totalReportable` and strict conservation failed with a negative whole-token delta (a harness accounting artifact, not a real loss). The settle loop now refreshes the balance projection (`refreshBalances()`) before snapshotting, matching how the initial baseline is measured.

## 1.15.3 (2026-06-27)

### Features

- [FEATURE][extension] **New-wallet onboarding hands off to the Chrome side panel.** When creating a wallet, the final onboarding screen creates it while the tab spins ("Creating your wallet…"); once it's ready, the "Open wallet" button opens the side panel onto the finished wallet and closes the onboarding tab. Creating the wallet first means the panel opens from a live user gesture (Chrome requires one for `sidePanel.open()`) onto an already-functional wallet — no loading state in the panel. The side panel also becomes the primary surface (clicking the toolbar icon opens it instead of the popup — toggle back via the header's maximise-view control). Import flows and non-Chrome browsers keep the classic in-tab flow.

### Fixes

- [FIX][all] **Activating a device key on a migrated Guardian account no longer permanently breaks its sync.** After a structural rotation (`update_signers` for replace-hot-key, or the follow-up `update_procedure_threshold`), the wallet now pushes the new on-chain state back to the guardian. The OpenZeppelin multisig client only re-registers post-execution state on the guardian for `switch_guardian`; for the other structural ops it submitted the transaction on-chain but left the guardian serving the pre-rotation blob, so every subsequent ~3s guardian sync saw guardian-commitment ≠ on-chain and threw on `ensureSafeToOverwriteLocalState` — permanently, until a full reinstall. The re-registration runs before the hot-key pointer swap arms the sync, and the guardian sync now self-heals a lagging guardian by re-registering the current state once per run (which also recovers already-broken accounts without a reinstall). The legacy-Guardian migration additionally verifies the re-derived cold key matches the on-chain signer before flagging an account for activation. (#227)
- [FIX][all] **Guardian sync no longer spams "missing hotPublicKey" errors for un-activated accounts.** `syncGuardianAccounts` now only syncs Guardian accounts that actually carry a hot key, instead of every account not flagged `requiresHotKeyRotation`. A legacy single-signer Guardian record that hasn't been migrated yet — e.g. in the brief window after a wallet upgrade (new code, old storage) and before the forced re-unlock runs the migration — has no hot key, so `getOrCreateMultisigService` threw on it every ~3s AutoSync tick. There's genuinely no hot-bound service to build for such accounts, so they're skipped; recovery still happens via the migration → Activate Device Key banner path on the next unlock. (#227)
- [FIX][all] **Guardian operator endpoint is now tracked per account, and structural Guardian ops recover from a submit-then-local-apply failure.** Each Guardian account persists its own `guardianEndpoint` (set at create/recovery, updated on switch-guardian) instead of a single global setting, so two Guardian accounts on different operators no longer collide — older records without the field fall back to the legacy global value. Separately, when a `replace-hot-key` or `switch-guardian` transaction lands on chain but the local apply step throws, the wallet now runs the same finalization the happy path would (swapping the hot-key pointer, or re-registering on the new guardian and persisting its endpoint) instead of cancelling — which previously stranded the account signing with a rotated-out key or talking to the old guardian. (#227)
- [FIX][all] **Guardian co-sign transactions are now serialized per account.** The Guardian co-signs one delta per account at a time, so concurrent same-account transactions (e.g. auto-consume racing a user claim, or rapid successive claims) made the Guardian's expected commitment diverge from on-chain — stalling its canonicalization for minutes and returning `409 ConflictPendingDelta` while a prior delta was still finalizing, which surfaced as a Guardian claim/send that never completes. Guardian transactions now take a per-account lock so at most one is ever in flight, and proposal creation waits out a transient `409` (a prior delta mid-canonicalization) instead of failing the transaction. (#297)
- [FIX][mobile] **iOS hot-key signing now requires Face ID / Touch ID on device, matching Android.** The Secure Enclave hot key was created with `.privateKeyUsage` only — a _usage permission_ that, contrary to the old code comment, does **not** prompt for authentication — so user-initiated Guardian claims and sends signed silently on iOS, while Android (StrongBox + `setUserAuthenticationRequired`) already prompted. New hot keys now also set `.userPresence`, so every user-initiated hot signature and hot-key reveal requires user presence (Face ID / Touch ID with passcode fallback). `.userPresence` is used rather than `.biometryCurrentSet` so the key survives biometric re-enrollment instead of bricking until re-activation. Background auto-consume is unaffected — it is cold-signed in WASM and never touches the hot key. Scope: the gate applies to device builds only (the simulator / iOS E2E path keeps `.privateKeyUsage`-only silent signing), and existing hot keys keep their prior behavior until re-activated/rotated. (#299)
- [FIX][mobile] **iOS app builds again under Xcode 26.** Two `foundKey as? SecKey` downcasts in the hot-key plugin (`signWithHotKey` / `revealHotKey`) are no-ops for CoreFoundation types — they always succeed — which Xcode 26 now rejects as a hard error, breaking the iOS build. Replaced with a `CFGetTypeID(foundKey) == SecKeyGetTypeID()` guard plus a force-cast: both the correct defensive downcast and Xcode-26-clean. (#299)
- [FIX][all] **Guardian accounts can now connect to dApps (faucet, etc.) instead of failing with "Connection Failed" / `NOT_GRANTED`.** A Guardian account's auth component is built by `@openzeppelin/miden-multisig-client` and its procedures live in the `openzeppelin::auth::*` MASM namespace, so they don't MAST-match any bundled `miden-standards` template. The SDK's `AccountInterface` therefore classifies the component as `Custom` and `Account.getPublicKeyCommitments()` returns `[]`; the wallet's connect flow read that as "no public key" and rejected the connection (surfaced to the dApp as `NOT_GRANTED`). Public-key resolution now falls back, for accounts the SDK can't classify, to reading the hot signer's commitment directly from the account's `openzeppelin::multisig::signer_public_keys` storage map — the key the wallet actually signs with — so Guardian accounts resolve a usable session key. Plain single-key accounts are unaffected (their `AuthSingleSig` component is recognized as before). The same resolution covers the reveal-private-key and advanced-settings public-key views, which broke identically for Guardian accounts. (#300)
- [CHANGE][ci] **iOS E2E no longer hangs the full timeout when the simulator's CDP bridge wedges.** `CdpBridge.eval`/`evaluate` now race the WebKit `executeAtom` call against a 30s hard timeout (matching `evalAsync`), so a wedged RWI socket or a momentarily-blocked WebView main thread surfaces as a fast throw instead of an indefinite await. Previously `pollForCondition` could only check its deadline _between_ iterations, so a single hung `eval` stalled the whole test until Playwright's 15-minute kill (and the rest of the serial suite then skipped); now the poll enforces its own budget and `--retries` restarts on a fresh app + CDP. (#302)
- [CHANGE][ci] **Blockchain E2E retries the `miden-client` harness CLI on transient remote-prover connection failures.** The CLI deploy/mint/sync retry loop classified only node-RPC and nonce-lag errors as transient; an intermittent TLS/gRPC handshake failure to the delegated prover endpoint on the macOS runners (`failed to connect to the remote prover` / `transport error` / `no native certs found`) was treated as fatal, so a mint failed outright even though a sibling mint in the same test connected fine. These connection-level prover errors are now recognized as transient and retried with backoff, and the three duplicated classifiers were unified into one `isTransientCliError` helper. (#302)
- [CHANGE][ci] **iOS E2E mobile jobs run on dedicated `macos-26-xlarge` runners, and are more resilient to degraded shared runners.** The shared standard `macos-26` runner pool was intermittently degraded for hours at a time by noisy-neighbour IO contention — every `simctl` op crawled (97 CI samples: per-wallet `_simPair` setup p50 65s, p90 267s, max 401s vs. <5s healthy — so two sequential wallets took up to ~13 min and sometimes never finished even in 15 min), making the whole mobile suite un-runnable. All four mobile E2E jobs now use the dedicated Apple-Silicon `-xlarge` larger runner (2× vCPU/RAM, no noisy neighbours), which restores a healthy ~2-3 min setup and a full green suite. As belt-and-suspenders for any residual slowness, the `_simPair` fixture setup is capped (at 13 min, past the slowest observed completing setup) so a genuinely-hung CoreSimulator fails fast with a named error and a sim-subsystem restart instead of silently eating the whole per-test timeout, while a degraded-but-completing setup is allowed to finish rather than being killed mid-flight (no assertion is relaxed — purely tolerance for degraded IO); the subsystem recovery `simctl shutdown all`s to clear half-booted device state (the `SimError 405` signature); the per-test timeout is 25 min (from 15); and the non-guardian mobile suite runs with `--retries=2` (matching the guardian suite). (#302)
- [CHANGE][ci] **Guardian iOS E2E reads the on-chain auth structure with a pure storage parse instead of loading the multisig client.** The `verify_guardian_auth_structure` assertion's `__TEST_GUARDIAN_AUTH__` hook used to build a `MultisigService` (`getOrCreateMultisigService` → `MultisigClient.load`) and read it. Against the post-consume state — where the guardian's stored blob lags the on-chain account — that load entered a re-sign/realign loop (~48 `signWithHotKey` calls for this read vs. 26 for a full consume) that hung the single-threaded mobile WASM past the eval budget; the assertion never got far enough to run on iOS. The structure (signer set + procedure thresholds) is immutable and lives in the account's storage maps, so the hook now reads it directly with `AccountInspector.fromAccount` — a pure parse with no signing, no guardian HTTP, and no client load (just one `getAccount`, the same read the balance poll already does). Because even that lone `getAccount` was still starved by other main-thread WASM activity on iOS (the auth eval was observed taking 60s with all the wallet's own pollers paused), the structure is now captured in the wallet's own balance poll (`fetchBalances`, which reliably completes) and stashed on a global; the test reads it as a plain value with no WASM call at all. Finally, the iOS harness reads that stash over the SYNCHRONOUS `execute_script` atom (polled), not the async `execute_async_script` one: appium-remote-debugger's async atom delivers its completion callback in the `arguments[arguments.length-1]` slot as the boolean `true` on this iOS RWI bridge, so `cb(result)` threw `TypeError: cb is not a function`, the promise rejected unhandled, and every `evalAsync` hung to its timeout no matter how fast the script ran — which is why the auth read still timed out at 60s even with the stash already populated. Test-only, gated on `MIDEN_E2E_TEST` and tree-shaken from production. (#302)

## 1.15.2 (2026-06-22)

### Features

- [FEATURE][all] **Custom-transaction support for Guardian accounts.** Guardian accounts can now co-sign arbitrary transaction requests, not just sends and note consumption: the multisig client builds a custom proposal from the request bytes, co-signs it through the Guardian, and reconstructs the executable `TransactionRequest` from the Guardian's advice. (#153)

### Changes

- [CHANGE][all] **Guardian now runs on the Miden 0.15 protocol line.** The OpenZeppelin Guardian client packages (`@openzeppelin/miden-multisig-client`, `@openzeppelin/guardian-client`) are pinned to `0.15.0-rc.0` (built against `@miden-sdk/miden-sdk ^0.15.0`), replacing the published `0.14.9` — which targets SDK `0.14.5` and trapped with `RuntimeError: memory access out of bounds` inside `MultisigClient.create()` when run on the wallet's 0.15 SDK (WASM ABI skew between the 0.14 client and the 0.15 SDK). Guardian account creation, note consumption, and sends now work end-to-end on 0.15. **Guardian currently requires devnet:** the hosted devnet Guardian runs the 0.15 server, while the testnet/production Guardian is still on the 0.14 server and is incompatible with the 0.15 client — until OpenZeppelin publishes a 0.15 Guardian server release. (#153)
- [CHANGE][all] Bumped `@miden-sdk/miden-sdk` and `@miden-sdk/react` from `0.15.1` to `0.15.2` (and the matching `**/@miden-sdk/miden-sdk` resolution pin). `@miden-sdk/vite-plugin` stays at `0.14.11`. (#153)

### Fixes

- [FIX][all] Corrected the devnet Guardian endpoint host (`stg-guardian.openzeppelin.com` → `guardian-stg.openzeppelin.com`); the transposed hostname had no DNS record, so the default devnet Guardian URL never resolved. (#153)
- [FIX][extension] Chrome package no longer bundles a second (single-threaded) Miden SDK WASM. `@openzeppelin/miden-multisig-client` imports the eager `@miden-sdk/miden-sdk` entry; the service-worker config already redirected that to the multi-threaded build, but the extension/front config did not — so the front bundle pulled in an extra ~15 MB single-threaded WASM. The front config now applies the same eager→`mt/lazy` alias, restoring the Chrome package to ~18 MB (was ~29 MB).

## 1.15.1 (2026-06-19)

### Changes

- [CHANGE][all] Bumped `@miden-sdk/miden-sdk` and `@miden-sdk/react` to the stable `0.15.1` release, replacing the `0.15.0-alpha.7` prerelease (npm `next` dist-tag) that shipped in 1.15.0 with the GA build on the `latest` dist-tag. `@miden-sdk/vite-plugin` stays at `0.14.11` (no 0.15.x is published; it's SDK-version-agnostic), and the `**/@miden-sdk/miden-sdk` resolution pin tracks the same `0.15.1`.
- [CHANGE][ci] Mobile E2E iOS build now targets a generic simulator destination (`generic/platform=iOS Simulator`) instead of a named device (`name=iPhone 17`), matching the approach `build-mobile.yml`'s release build already uses. A build needs no concrete booted device, and the generic destination resolves even when Xcode's IDE-layer CoreSimulator goes blind to concrete sims on macos-26 runners — the flake that was failing `Mobile E2E` at the build step (`xcodebuild: Unable to find a device`). The now-redundant "wait for xcodebuild to see the simulators" gate is removed (it could hard-fail the job for a condition that no longer blocks the build); pinned sims are still pre-booted for the test-run phase, which reaches them via `simctl`.

## 1.15.0 (2026-06-12)

### Features

- [FEATURE][all] 3-key Guardian: cold co-signs `switch_guardian` (on-chain threshold-2 satisfied via `procedureThresholds`) and a new Settings → Rotate Device Key flow lets users replace the hot signer in-place via a single cold-signed `update_signers` proposal, persisting the new ciphertext before submission and finalizing `WalletAccount.hotPublicKey` (plus releasing the old SE/StrongBox wrapper) only after on-chain inclusion. Settings → Switch Guardian now requires an explicit confirmation step before initiating, mirroring the Rotate Device Key flow so the user acknowledges that the switch is cold-signed + guardian-co-signed. Guardian import-by-seed does lookup + adopt only (`MultisigClient.recoverByKey` per HD index until first miss); each adopted account is flagged `requiresHotKeyRotation` and the home view surfaces an Activate Device Key banner with a one-click CTA that fires the same cold-signed `update_signers` rotation — the banner self-hides once `Vault.swapHotKey` lands. The `swapHotKey` message now carries only `newHotPubKey`; the vault resolves the previous hot from the persisted `WalletAccount`, so the initial post-recovery activation and subsequent rotations share one code path.
- [FEATURE][all] 3-key Guardian accounts. Guardian creation now provisions a hot ECDSA key (random, lives outside the WASM keystore behind a new `secure-hot-key` facade — extension/desktop use a JS fallback; iOS wraps the secret under a per-account Secure Enclave P-256 key via `HotKeyPlugin.swift`; Android wraps it under a per-account StrongBox-backed RSA-OAEP key via `HotKeyPlugin.kt` with biometric-gated unwrap, identical wire format on both platforms) plus an HD-derived cold ECDSA key (kept in the SDK keystore for cold-routed flows) alongside the existing guardian co-signer. Threshold stays at 1 (hot OR cold + guardian); cold-only routing for `update_signers` / `update_guardian` / `update_procedure_threshold` is enforced client-side per the Phase 0 SDK reading. Storage adds `accColdSecretKeyStrgKey` and `WalletAccount.{hotPublicKey,coldPublicKey}` so role-aware `signWord` (Phase 3) can dispatch hot vs cold by storage entity. Hard cutover from the 1-of-1 Falcon scheme — pre-cutover Guardian accounts are unreachable from this build.
- [FEATURE][all] Guardian integration. Adds Guardian-backed accounts (1-of-1 multisig with on-chain Guardian signature verification), onboarding create/import flows with a dedicated recovery-method screen that accepts a custom guardian URL, Settings → Guardian Settings with an on-chain switch-guardian proposal that re-registers post-switch state with the new endpoint, service-worker routing for Guardian transaction signing via `MultisigService`, frontend Guardian sync outside the WASM lock, and per-stage progress labels (`creating-proposal`, `signing-proposal`, `submitting`, `registering-guardian`) in the transaction modal.
- [FEATURE][all] **Default auth scheme for new accounts switched from Falcon to ECDSA.** Existing accounts are unaffected — Miden seals the auth component at on-chain creation and can never rotate it, so any account previously created stays Falcon and continues signing with its existing keystore secret. Restore paths handle both schemes: mnemonic-only restore (`Vault.spawn`) probes the chain under each scheme to find the user's actual hdIndex=0 account; encrypted-file restore reads the new optional `authScheme` field on `WalletAccount` (legacy entries with no field default to Falcon, matching the historical default 1:1). Private-key import detects the scheme from the deserialized `AuthSecretKey` via the SDK's per-scheme accessor. New accounts created post-upgrade get ECDSA stamped into their `WalletAccount` record. Encrypted-file format change is purely additive — old files round-trip through restore as Falcon. (#229)

### Changes

- [CHANGE][all] **Migrated to the Miden 0.15 protocol line** — `@miden-sdk/miden-sdk` and `@miden-sdk/react` bumped to the `0.15.0-alpha` series (npm `next` dist-tag; `@miden-sdk/vite-plugin` stays at `0.14.11`, which is SDK-version-agnostic). User-visible consequences of the protocol bump:
  - **Local stores do not carry over.** 0.14 account IDs, note IDs, and nullifiers do not round-trip under 0.15 (account-ID version renumbered, note identity split into details-commitment + metadata-bearing ID, hashing changed). The SDK's IndexedDB store detects the version bump and re-creates itself; accounts re-register from the wallet seed and balances resync on unlock. Saved 0.14 wallet store files (Settings → export) cannot be imported into a 0.15 build.
  - Faucets minted by pre-0.15 SDKs can no longer be introspected for token metadata (`BasicFungibleFaucetComponent.fromAccount` reads the new metadata slot); their assets now display as "Unknown" instead of being blacklisted for the session.
  - The dApp `ImportPrivateNoteResponse.noteId` for details-only note imports (the common `noteBytes` path) now carries the note's **details-commitment hex** rather than a note-id hex, following `notes.import`'s new return contract.
  - Network/fee-asset discovery follows the protocol rename (`BlockHeader.feeFaucetId()`, formerly `nativeAssetId()`); the discovery cache is keyed `v2` so 0.14-cached IDs are not reused.
  - Account storage modes are `public`/`private` only (the chain's separate network-account flag is gone), and partial (metadata-less) input notes — which have no note ID until sync completes them — are filtered out of consumable/claimable listings.
- [CHANGE][ci] Blockchain E2E installs `miden-client-cli` from a pinned git rev (`midenClientCliGit` in package.json) while the 0.15 CLI is unreleased on crates.io; testnet E2E is expected red until testnet upgrades to node 0.15 (devnet already runs it) — covered by the at-least-one-network gate.

### Fixes

- [FIX][all] Guardian sync no longer leaks per-init web-client workers. `MultisigService.init` now reuses the shared `getMidenClient()` singleton instead of a throwaway `MidenClientInterface.create({})` client, and `getOrCreateMultisigService` coalesces concurrent init calls with an in-flight promise map so 3s sync ticks cannot stampede slow or failed initialization.
- [FIX][mobile] Wallet creation no longer panics the WASM client on iOS/Android (`RefCell already borrowed` → poisoned instance → claims and balances silently dead). On mobile the front-end and wallet back-end share one direct-path (`useWorker: false`) SDK client, and the front-end's balance polling overlapped the wallet-creation syncs; `@miden-sdk/miden-sdk@0.15.0-alpha.7` restores the SDK-side call serialization that makes those overlaps queue instead of panic ([web-sdk#184](https://github.com/0xMiden/web-sdk/pull/184)).
- [FIX][extension] The offscreen prover document now constructs the SDK's raw wasm-bindgen `WebClient` (prover-only — `createClient()` is never called) instead of the worker-shim wrapper. The wrapper forwarded every prove to its own method worker — a WASM instance whose rayon pool the offscreen document never initialized — and its implicit worker INIT performed a network round-trip against the SDK's default RPC endpoint, making local proving silently dependent on that endpoint being reachable and version-compatible. Proves now run on the offscreen document's own thread pool with no implicit network access (so local proving works offline), completing in ~5–6 s on a 10-core machine. Requires `@miden-sdk/miden-sdk` ≥ `0.15.0-alpha.6` (the explicit-prover fix in [web-sdk#182](https://github.com/0xMiden/web-sdk/pull/182)). Root-cause analysis in [web-sdk#180](https://github.com/0xMiden/web-sdk/issues/180#issuecomment-4686218893).

## 1.14.7 (2026-06-09)

### Fixes

- [FIX][extension] Local proving (`chrome.offscreen` MT-WASM path) no longer hangs the wallet. Every `consume` and `send` with `delegate_proof_setting_key=false` would deadlock inside the SDK's `_withInnerWebClient` callback at `inner.getInputNote(...)` — the callback's outer `_serializeWasmCall` slot awaited the inner `_serializeWasmCall` slot that was queued behind it (classic re-entrant-lock deadlock; see [web-sdk#152](https://github.com/0xMiden/web-sdk/pull/152) for the SDK-side root cause + fix). Downstream, the wallet's `SyncManager` saw the wasm-client mutex held for the full hang window and tripped its 5 s timeout three times, opening the circuit breaker and showing the user a misleading "cannot reach the miden node" banner. With the SDK fix in place, `consumeNoteId` / `sendTransaction` / `newTransaction` complete normally on the offscreen-doc prove path. Requires `@miden-sdk/miden-sdk@0.14.10`.
- [FIX][extension] `SyncManager` no longer surfaces a "cannot reach the miden node" banner during legitimate long local proves. Previously the 5 s `SYNC_TIMEOUT_MS` bounded the _combined_ lock acquisition + RPC call, so any wasm-client lock held >5 s by an in-flight prove looked indistinguishable from a real RPC stall — three of those in a row tripped the circuit breaker and `markConnectivityIssue('node')`. The timeout now wraps the RPC alone; queued syncs wait patiently behind the prove (which yields the lock via `yieldWasmClientLock` around the offscreen step, so the wait is short), then bound only the actual network call. Steady state correctness is unchanged: a sync after the yield clears any active reachability category as before.
- [FIX][all] Stale transaction-completion modal no longer blocks subsequent sends. After PR #230 the `TransactionProgressModal` auto-dismiss was correctly gated on terminal-state signals so the "Tx Completed → View on Midenscan" screen wouldn't be ripped away when the success-path `navigate('/')` fires after a long local prove — but with no other dismissal path the modal stayed up as a full-viewport `zIndex: 9999` overlay until the user explicitly tapped **Done**. Stress tests (and any user starting a second send before tapping Done) navigated to `/send`, found the SelectToken tile blocked behind the modal, and saw `locator.click` time out against `getByTestId('send-flow').locator('div.cursor-pointer')` in 80–99.5% of attempts. `SendManager` now closes any stale completion modal on entry — an explicit "starting a new tx" signal equivalent to tapping Done. Initially shipped in #245 gated on `lastCompletedTxHash !== null`, which only covered send completions; stress runs against #245 showed claim/dApp completions still left the modal sticky (the previous turn's recipient could not start its send because its post-claim completion modal blocked the SelectToken tile). Widened in this release to drop the `lastCompletedTxHash` gate — every open completion modal closes on send-flow entry, covering the `Receive` / `ConfirmPage` / `SendManager` modal-open paths uniformly. In-flight modals can't reach this code path because PR #217's pathname-watching effect in the modal already auto-dismisses non-terminal opens on navigation away.
- [FIX][all] **The "Cannot reach the Miden node" banner no longer flaps on a slow-but-healthy node.** Two issues compounded: (1) `SYNC_TIMEOUT_MS` was 5s while a testnet sync can legitimately take 5-25s, so healthy syncs routinely tripped the watchdog — and the timeout never actually freed the WASM client mutex early (`withTimeout` only rejects the outer promise; the underlying `syncState` keeps running and holding the lock until it settles), so the aggressive ceiling bought nothing and only manufactured false failures; (2) `markConnectivityIssue` ran on the _first_ failure, before the circuit-breaker check, so a single slow sync surfaced "node unreachable" even while block height was still advancing. `SYNC_TIMEOUT_MS` is raised to 30s (a true wedged-sync watchdog), and the connectivity banner is now gated on the same `MAX_CONSECUTIVE_SYNC_FAILURES` (3) streak that opens the circuit breaker, so it only appears when the node is persistently unreachable and clears on the next successful sync. Reported in #252.

### Changes

- [CHANGE][all] Bumped the Miden SDK to `0.14.11` — `@miden-sdk/miden-sdk`, `@miden-sdk/react`, and `@miden-sdk/vite-plugin` are kept in lockstep (with the `**/@miden-sdk/miden-sdk` resolution pin) to avoid mismatched-version WASM/type issues.

## 1.14.6 (2026-06-03)

### Fixes

- [FIX][extension] **A failed public-key fetch during dApp connect no longer leaves the wallet permanently stuck on "Connecting…".** In the extension connect path (`generatePromisifyRequestPermission` in `src/lib/miden/back/dapp.ts`), if `getAccountPublicKeyB64` threw (e.g. the account was momentarily unavailable during instability), the error was swallowed and a dApp session was persisted with `publicKey: null`. The direct-return path then handed that null public key back verbatim on every subsequent connect — with no confirmation popup — so the dApp's `connect()` never resolved and the only escape was clearing the session or reinstalling. The extension branch now fails closed (rejects with `NotGranted`, no session saved), mirroring the non-extension branch which already threw on the same failure, so a transient error surfaces as a clean, retryable rejection instead of a permanent wedge. Regression test added. Reported in #219.

- [UX][devnet] **Devnet build icon now carries a "developer" badge.** The devnet extension icon (`public/misc/logo-devnet*.png`) gains a small wrench badge — the Advanced Settings / Developer glyph in a white circle with a thin `#7286A0` ring — overlapping the `m.`'s bottom-right, so a devnet build is distinguishable at a glance beyond the existing blue-vs-orange color. Regenerated at every size (16/32/40/48/128 + the 234px master).

- [FIX][devnet] **Devnet tab favicon now matches the devnet icon.** The wallet's tabbed pages (`fullpage.html`) hard-coded the orange `logo-white-bg` favicon, so a devnet wallet opened in a browser tab showed the orange production logo. The extension build's `transformIndexHtml` now swaps the favicon to `logo-devnet` for `MIDEN_NETWORK=devnet`, mirroring the existing devnet manifest-icon swap.

- [FIX][all] **Legal links no longer 404.** The Privacy Policy and Terms links pointed at the dead `miden.fi` domain in two places — the onboarding password screen (`CreatePassword`) and Settings → About — and the Settings → About entries additionally rendered `<a href="#">` because they used `slug: '#'`. New `PRIVACY_POLICY_URL` / `TERMS_OF_USE_URL` constants in `src/app/constants.ts` point at the live `https://0xmiden.github.io/wallet/privacy/`. There is no dedicated Terms page yet, so `TERMS_OF_USE_URL` temporarily resolves to the Privacy URL until one ships — flip the constant when it does. Reported in #202 and #219.

- [FIX][all] **Advanced Settings now shows the account public key.** The "Account Public Key" row rendered only a label with no value, and the copy button copied nothing while the key was still resolving. It now shows the truncated key (`0x` + first 6 + `…` + last 4), reserves the row height with a non-breaking space so it doesn't jump on first paint, and disables the copy button until the key is available. Reported in #219.

- [FIX][all] **The toolbar close (✕) button is now visible in dark mode.** The `PageLayout` close button passed a literal `fill="black"` to its `Icon`, so the ✕ was invisible against the dark app background on every page that uses the toolbar (HistoryDetails was the reported case, but the bug was global). It now passes `fill="currentColor"`, inheriting the button's `text-black` token (black in light, white in dark). Reported in #219; follow-up to #265/#266.

- [FIX][all] **Activity loaders are now visible in dark mode.** The pending-transaction spinner in the activity list (`rotate.svg`, rendered by `TransactionIcon`) hardcoded `fill="black"`, so it was invisible on the dark app background. It now uses `fill="currentColor"` driven by a `text-black` class (`var(--color-text-primary)`), rendering black in light and white in dark — matching the app's other `currentColor` icons. The shared `Loader` (`components/Loader.tsx`) also defaulted `color` to `'black'`; it now defaults to `'currentColor'` so a bare `<Loader/>` inherits the themed text color instead of painting literal black. The header sync-spinner track moved from a hardcoded `#E5E7EB` to `var(--color-border-secondary)` (the orange progress arc is unchanged). Follow-up to #265; reported in [0xMiden/wallet-adapter#90](https://github.com/0xMiden/wallet-adapter/issues/90) (item 3). (#266)

- [FIX][all] **Dark mode now themes the whole UI.** The wallet was built light-first: ~160 sites across screens hardcoded Tailwind color utilities (the `grey-*` ramp, `gray-200..900`, arbitrary `[#hex]`, `pure-white`) that resolve to fixed values regardless of theme. Theming is driven by `darkMode: 'class'` + CSS custom properties in `src/main.css`, but these hardcoded utilities never flipped — so in dark mode light-gray captions vanished, inputs/cards/modals stayed white, hairline borders disappeared, and `Navigator`'s page-transition `backgroundColor` painted every routed page `#ffffff`. All such sites now use the var-backed semantic tokens that flip with the `.dark` class (`text-text-muted`, `text-heading-gray`, `border-border-card`/`-light`, `bg-surface-solid`/`-chip-bg`/`-input-bg`, `bg-gray-25/50/100`); `Navigator` reads `var(--color-app-bg)`; and pastel status alerts gained `dark:` fill/text variants (using valid `-500` text shades, since this palette has no `red`/`green` `-300`). The dApp browser surfaces (confirmation modal, load-error overlay, capsule bar, launcher) were co-themed so a themed background never leaves dark-on-dark text. Brand colors, white-on-saturated buttons, status accents, QR codes, dark scrims, and the inverted-selected chip are intentionally unchanged. 96 files, color utilities only — no logic or copy changes. Verified at the CSS/build level (`tsc`, `eslint`, `build:mobile` clean; generated utilities map to `var(--color-*)` and flip under `.dark`); runtime pixel verification still recommended. (#265)

- [FIX][all] **Consuming a note via the dApp API can no longer brick the wallet.** A dApp's `requestConsume` may carry serialized note bytes via `ConsumeTransaction`'s `noteBytes` parameter, which the wallet enqueues (`queueNoteImport`) and later imports via `NoteFile.deserialize` in `importAllNotes` (`src/lib/miden/activity/notes.ts`). That parameter has an undocumented, unvalidated requirement: it must be a serialized `NoteFile`. The natural thing for a dApp to pass — `note.serialize()` — produces a serialized `Note` instead (a different container, with no `"note"` magic header), so deserialization throws `notefile deserialization failed: invalid utf-8 sequence...`. The note itself is valid; only the serialization container is mismatched. (The companion fix below makes the wallet accept a `Note` here, so this specific mismatch no longer fails at all.) Previously `importAllNotes` aborted the whole batch on any failure and only cleared the persistent import queue (`miden-notes-pending-import`) after full success, so the bad note was retried on every transaction-loop iteration forever. Because `importAllNotes()` is the first call in `generateTransactionsLoop`, this permanently jammed ALL transaction generation (sends and consumes alike) — the queue lives in persistent storage, so the jam survived restarts and only a reinstall recovered. `importAllNotes` now imports each note independently (one failure no longer aborts the batch) and applies a bounded retry: a failing note is kept and retried, carrying an attempt count, and dropped only after `MAX_IMPORT_ATTEMPTS` (3) failures. This keeps the queue draining — a deterministically bad note can never loop forever and re-brick the wallet — while still giving genuinely transient failures (e.g. a `NoteFile::NoteId` import, which fetches the note over RPC and can hit a network blip) a chance to succeed, so a recoverable note (including a private note whose bytes are its only copy) isn't lost to a single blip. Processed-note removal happens inside the WASM lock and before `syncState`, so a sync failure can't leave processed notes queued for an unbounded retry. Queue entries gain an `{ bytes, attempts }` shape; legacy bare-string entries are normalized on read, so no migration is needed. Regression tests cover a failing note kept for retry with an incremented count, a poison note dropped after 3 attempts without `importAllNotes` ever throwing, the queue draining even when `syncState` throws, and concurrently-enqueued notes preserved across a pass. Reported in `0xMiden/wallet-adapter#87`.

- [FIX][all] **Importing note bytes now accepts a serialized `Note`, not just a `NoteFile`.** `MidenClientInterface.importNoteBytes` (`src/lib/miden/sdk/miden-client-interface.ts`) previously called `NoteFile.deserialize` directly, so any caller passing a serialized `Note` — the natural output of `note.serialize()`, and what a dApp typically has on hand for `ConsumeTransaction`'s undocumented `noteBytes` parameter — failed with `notefile deserialization failed: invalid utf-8 sequence...`. It now accepts either form: a `NoteFile` is used directly, and a bare `Note` is wrapped into a `NoteFile` (the `NoteDetails` variant, mirroring `NoteFile.fromInputNote` when no inclusion proof is available) before import. Bytes that are neither raise a clear, actionable error (`bytes are neither a serialized NoteFile nor a serialized Note...`) instead of the opaque deserialization failure. This is the trigger behind `0xMiden/wallet-adapter#87`: combined with the brick fix above, a dApp consuming a note via `requestConsume` with `note.serialize()` bytes now works end to end. Covers the consume path, custom-transaction `importNotes`, and the extension's import-note request, since all route through `importNoteBytes`. Reported in `0xMiden/wallet-adapter#87`.

- [FIX][dapp] **Generalized `requestTransaction` now handles `send` / `consume`, not just `custom`.** A dApp consuming a note through the typed `Transaction(TransactionType.Consume, new ConsumeTransaction(...))` API (rather than the dedicated `requestConsume`) hit `INVALID_PARAMS: Invalid CustomTransaction payload`: the wallet's `requestTransaction` handler (`generatePromisifyTransaction` in `src/lib/miden/back/dapp.ts`) read `req.transaction.payload` and validated it as a `MidenCustomTransaction` unconditionally, ignoring the tagged `MidenTransaction.type`. It now dispatches by `type` — `send` delegates to the send flow (`generatePromisifySendTransaction`) and `consume` to the consume flow (`generatePromisifyConsumeTransaction`), reusing their existing preview / confirmation / execution paths, while `custom` (and bare/legacy payloads) keep flowing through the custom path. The `type` discriminant is compared by its wire string value rather than importing the adapter-base `TransactionType` enum (that package is ESM-only and consumed type-only here). Fixes [0xMiden/wallet-adapter#88](https://github.com/0xMiden/wallet-adapter/issues/88).

- [FIX][mobile] **dApp wallet-connect no longer fails.** Two cascading bugs broke every `window.midenWallet.connect()` call on iOS + Android — the extension was unaffected because it goes through a different confirmation path. First crash: `getNetworkRPC()` in `src/lib/miden/back/dapp.ts` used a non-null assertion (`NETWORKS.find(n => n.id === net)!.rpcBaseURL`) — when a dApp called `connect()` without passing a `network` argument (which the testnet faucet, and any dApp using the standard `@demox-labs/miden-wallet-adapter-base` API, does — they just want to use whichever network the wallet is on), the `find()` returned undefined and the wallet threw `Cannot read properties of undefined (reading 'rpcBaseURL')` immediately. The error was wrapped into a `MIDEN_PAGE_ERROR_RESPONSE` and shipped back to the dApp before the permission modal ever rendered. `getNetworkRPC` now falls back to `getCurrentMidenNetwork()` when `net` is undefined and throws a clean `NetworkNotGranted` for genuinely unknown ids. Second crash, hidden behind the first: `DappBrowserProvider` was passing `shortAccountId` (the truncated `mtst1apsnk6...qq9wr6w` display string with literal `...`) into `DappConfirmationModal`. The modal echoed that truncated string back as `accountPublicKey` on Approve. The backend then called `midenClient.getAccount(accountId)` with the truncated form, the WASM bech32 decoder threw `invalid character (code=.)`, and `requestPermission` mapped it to `NOT_GRANTED` — so the dApp saw the permission denied even though the user had tapped Approve. The modal now receives the full bech32, truncates locally only for display via `truncateAddress()`, and sends the full id back through `onResolve`. End-to-end CDP repro on Android emulator confirms `connect()` now resolves with `{accountId, publicKey, privateDataPermission, allowedPrivateData}` in ~430 ms. Regression tests cover both: `getNetworkRPC(undefined)` returns the current network's RPC, `getNetworkRPC('unknown')` throws `NetworkNotGranted`, and the modal echoes the full bech32 (no `...`) on Approve.

- [FIX][all] Stale transaction-completion modal no longer blocks subsequent sends. After PR #230 the `TransactionProgressModal` auto-dismiss was correctly gated on terminal-state signals so the "Tx Completed → View on Midenscan" screen wouldn't be ripped away when the success-path `navigate('/')` fires after a long local prove — but with no other dismissal path the modal stayed up as a full-viewport `zIndex: 9999` overlay until the user explicitly tapped **Done**. Stress tests (and any user starting a second send before tapping Done) navigated to `/send`, found the SelectToken tile blocked behind the modal, and saw `locator.click` time out against `getByTestId('send-flow').locator('div.cursor-pointer')` in 80–99.5% of attempts. `SendManager` now closes any stale completion modal on entry — an explicit "starting a new tx" signal equivalent to tapping Done. Initially shipped in #245 gated on `lastCompletedTxHash !== null`, which only covered send completions; stress runs against #245 showed claim/dApp completions still left the modal sticky (the previous turn's recipient could not start its send because its post-claim completion modal blocked the SelectToken tile). Widened in this release to drop the `lastCompletedTxHash` gate — every open completion modal closes on send-flow entry, covering the `Receive` / `ConfirmPage` / `SendManager` modal-open paths uniformly. In-flight modals can't reach this code path because PR #217's pathname-watching effect in the modal already auto-dismisses non-terminal opens on navigation away.

## 1.14.4 (2026-05-28)

### Fixes

- [FIX][mobile] Mobile sync no longer hangs on WKWebView. The SDK's Web Worker shim runs every `client.*` call (including `syncState()`) inside a worker, but WKWebView Workers can't complete a gRPC-web fetch — so the very first auto-sync after wallet creation would hang forever, leaving `isSyncing: true` and `lastSyncedAt` frozen, with newly-minted public notes never surfacing in the Receive screen. Wallet now passes `useWorker: false` to `MidenClient.create` on mobile (the option lands in `@miden-sdk/miden-sdk@0.14.9` from web-sdk#149), so the SDK runs in the main thread on mobile (mirroring the pre-0.14.4 behavior). Desktop / Chrome extension keep `useWorker: true` (the Worker shim is fine there). Web SDK PR: #149.

### Features

- [FEATURE][mobile] **Native Rust transaction prover on iOS + Android.** On opt-in (Settings → General → Local proving), the wallet hands the SDK a `CallbackProver` whose closure routes prove calls through a new Capacitor plugin (`@miden/native-prover`) → JNI / Swift FFI → `LocalTransactionProver` linked as a static / shared lib. Same wire bytes as `RemoteTransactionProver`, so the SDK dispatch path is unchanged downstream. Single-consume timings: iOS 17 sim 1.575 s native prove (2.07 s end-to-end), iOS 17 real device ~3 s, Android arm64 emulator 4.99 s (5.12 s with base64 + IPC), expected ~3 s on real Android hardware. Replaces the prior fallback to the SDK's in-worker WASM single-thread prover (60–90 s on sim) — roughly 50–80× faster. Backwards-compatible: `DEFAULT_DELEGATE_PROOF` stays `true`, the native path is opt-in, and the Chrome extension is unaffected (worker shim stays enabled, no `CallbackProver` is constructed). Requires web-sdk's new `TransactionProver.newCallbackProver(jsFn)` + `ClientOptions.useWorker: false` (web-sdk #149) to keep the closure alive past the WebClient dispatch boundary. Ships an Android Playwright E2E harness alongside (`playwright/e2e/android/*`) mirroring the existing iOS one. Web SDK PR: #149. See `0xMiden/protocol#2906` for the underlying `miden-tx 0.14.6` regression (asset-callback kernel rewrite that broke every consume) — resolved by the upstream yanking 0.14.6, so 0.14.5 stays the resolver pick and no pin is needed on this branch. (#235)
- [FEATURE][ci] **Linked Web SDK PR pattern.** Wallet PRs that depend on an unpublished `@miden-sdk/miden-sdk` or `@miden-sdk/react` change can now include `Web SDK PR: #N` in the description. CI's new `.github/actions/inject-linked-web-sdk-pr` action clones the linked web-sdk PR, builds the SDK packages from source, and rewrites `package.json` to consume them via `file:` deps — so the wallet PR's CI builds against the actual upstream change without requiring it to publish first. A separate `check-linked-web-sdk-pr.yml` workflow keeps a `linked-web-sdk-pr-ready` commit status pending until the linked PR is merged AND a web-sdk release tag covering its merge commit is published, with a 15-min cron re-evaluation so the gate auto-flips green without re-pushing. Local-dev parity via `scripts/dev-with-web-sdk-pr.sh` (with `--clear`); a new `lefthook.yml` pre-commit hook blocks committing the patched state. Mirrors web-sdk's existing `Client PR: #N` pattern. See "Linked Web SDK PR (cross-repo CI)" in CLAUDE.md for full details. (#231)
- [FEATURE][extension] **Multi-threaded local proving + speculative pre-prove.** Switched the SDK import path from `@miden-sdk/miden-sdk/lazy` (single-threaded WASM, no COI requirement) to `@miden-sdk/miden-sdk/mt/lazy` (multi-threaded WASM with wasm-bindgen-rayon, requires cross-origin isolation). The wallet's MV3 manifest already declares COOP=`same-origin` + COEP=`require-corp`, so the requirement is satisfied automatically. Replaces the SW-bundled single-threaded prove path (~25-40s on a typical laptop) with a `chrome.offscreen` document hosting wasm-bindgen-rayon over `navigator.hardwareConcurrency` threads (~5-10s). The SW orchestrates execute → offscreen prove → submit → apply, releasing the WASM-client mutex during the prove via a new `yieldWasmClientLock` so background sync stays responsive across the multi-second prove window. A new `SpeculationManager` kicks off the prove the moment the SendDetails form is valid (debounced 500 ms) so by the time the user taps Confirm the proof is usually already cached — submit + apply runs in ~250 ms. If the user taps Confirm while the prove is still in flight, the wallet awaits the in-flight matching speculation rather than starting a duplicate. If the user changes form params mid-prove, the offscreen doc is terminated to abort the now-stale prove (cores freed for the new params); a non-speculative-prove counter blocks the abort whenever a real send / consume / new-transaction prove is in flight, so the user's actual transaction is never killed. The per-send "Delegate proving" toggle on the Send screen was dropped — local vs. delegated is now driven exclusively by Settings → General → Local proving. Off by default; built-time `MIDEN_USE_OFFSCREEN_PROVING=true` and `MIDEN_USE_SPECULATIVE_PROVING=true` enable it (defaulted on for desktop Chrome builds via `build:chrome`). Mobile keeps remote proving — there's no equivalent `chrome.offscreen` API on mobile platforms, and the mobile build pins both flags to `false`.

- [FEATURE][all] **Private key export & import.** Settings → Reveal Private Key now shows the hex-encoded auth secret for the current account (guarded by the existing password / biometric unlock). Import Account → Private Key accepts a hex secret plus optional name and rebuilds the account deterministically via `AccountBuilder` + `AccountComponent.createAuthComponentFromSecretKey`; the secret is persisted through the existing `keystore.insert` → `insertKeyCallback` path, so the reveal/sign pipeline treats imported accounts identically to HD-derived ones. Imported accounts are tagged `hdIndex: -1`. (#195)
- [FEATURE][all] Transaction-complete modal now surfaces a **View on Midenscan** action alongside **Done**. Desktop / extension opens the explorer in a new tab; mobile opens it as a native `InAppBrowser` overlay so dismissing the overlay returns the user to the completion screen with no state loss. URL resolved per-network via a new `MIDEN_EXPLORER_ENDPOINTS` map (testnet / devnet); localnet has no explorer → button hidden. The on-chain tx hash is plumbed through `SendManager.onSubmit` → `lastCompletedTxHash` in the Zustand store, cleared at the start of each send so the button never points at a stale hash. (#203)
- [UX][all] Transaction-complete modal no longer auto-closes 3 s after success — user now dismisses explicitly, giving time to read the confirmation and tap **View on Midenscan**. (#203)

### Fixes

- [FIX][all] Transaction-progress modal no longer auto-dismisses on the SendManager's success-path `navigate('/')` when the prove takes longer than the modal's 2-second post-open grace window. The auto-dismiss (PR #217) was sized for delegated proving (~1-2 s round trip); local proving runs in 5-10 s, so the success-path navigate fires WELL after the grace expires and used to close the modal before the user could see the "Tx Completed → View on Midenscan" screen — they'd land on Home with no visible result. The auto-dismiss is now gated on terminal-state signals (`lastCompletedTxHash` set, `transactionComplete` true, or `hasErrors` true); while the tx is genuinely in flight it still fires (preserving PR #217's click-through-blocker fix), but once we have a result the modal stays open until the user explicitly taps Done.

- [FIX][all] Connectivity-issue surface revamped end-to-end. The single misnamed `connectivity-issues` flag (which only ever fired for prover failures, persisted indefinitely after a single transient 502, and was never wired on mobile) is replaced by a categorized state machine in `lib/miden/activity/connectivity-state.ts` tracking `network` (user offline), `node` (Miden RPC unreachable), `prover` (remote prover down), and `resolving` (recovery probe in flight) independently. Categories auto-clear on the next successful operation of the relevant kind: `prover` clears on every successful `withProverFallback` invocation; `network`/`node`/`resolving` clear on every successful sync from both the SW (`sync-manager.doSync`) and the in-process mobile/desktop sync loop (`useSyncTrigger`). The banner in `Explore` now picks the highest-priority active category (network > node > prover > resolving), shows category-specific copy and an actionable Retry CTA where it helps (no CTA for prover, since fallback to local proving is already silent), and is no longer gated behind `!isMobile()`. The dead `sendConnectivityIssue` runtime-message path was retired (the `ExtensionMessageListener` is now a no-op). New i18n keys: `connectivityNetworkTitle/Body`, `connectivityNodeTitle/Body`, `connectivityProverTitle/Body`, `connectivityResolvingTitle/Body`, `connectivityRetry`, `connectivityRetrySync`. New tests cover the state-machine transitions, the classify heuristic (network vs node vs semantic-error), the prover-success clear path, and the sync-error categorization. (#141)
- [FIX][mobile] Unblocked app boot on iOS/WKWebView by switching `lib/miden-chain/native-asset.ts` from `@miden-sdk/miden-sdk` (eager WASM entry) to `@miden-sdk/miden-sdk/lazy`. This was the one straggler missed by the 1.14.2 lazy-path migration; because `native-asset.ts` is transitively imported by the backend bootstrap, Explore, balance fetch, and `useMidenFaucetId`, every cold start hit the eager TLA and the splash screen stayed up indefinitely on mobile. (#203)
- [FIX][mobile] Web-layer modals are no longer covered by the native navbar pill. The Home/Activity/Browser overlay lives in its own iOS `UIWindow` (and analogous Android `Dialog`) above the WebView, so web modals couldn't z-order above it. New `useHideNavbarWhileOpen` hook morphs the pill off-screen while any modal is open and back in on close, with a shared open/close reference counter so concurrent modals coexist. Wired into `CustomModal` (covers `AlertModal` / `ConfirmationModal` / `AddContactModal` via `ModalWithTitle`), `TransactionProgressModal`, and `RecallBlocksModal`. (#203)
- [FIX][extension] Popup and side panel no longer flash white on reopen in dark mode. `public/globals.js` (already loaded before the module entry on every extension HTML) now reads `theme_setting` from `localStorage`, resolves `'system'` via `prefers-color-scheme`, and applies `.dark` + the dark `<html>` background synchronously before first paint. External script because MV3 CSP forbids inline scripts. (#203)
- [FIX][extension] Content scripts (`contentScript.js`, `addToWindow.js`) are now built as standalone classic IIFE bundles instead of ES modules with code-split `chunks/*` imports. MV3 content scripts declared in `manifest.json` run as classic scripts, so the `import` statements at the top of the previous output silently failed to parse — no error on the extension card, no entry in DevTools → Sources → Content scripts, and `window.midenWallet` was never injected (dApps saw `WalletNotReadyError`). New `vite.contentScripts.config.ts` builds each entry separately with `format: 'iife'` + `inlineDynamicImports: true`, and stubs `lib/intercom/{mobile,desktop}-adapter` so the content-script bundle doesn't drag the wasm-bindgen SDK in via their transitive deps. Wired into `build:extension` / `build:chrome` / `build:firefox` / `build:safari` / `test:e2e:blockchain:build`.
- [FIX][all] Encrypted-wallet-file import now restores secret keys for every imported account, not just the first. The decrypted wallet payload carries the full `WalletAccount[]` (with `hdIndex` and `type` per account), and `Vault.spawnFromMidenClient` re-derives each auth key from the mnemonic and inserts it into the new keystore via `client.keystore.insert`. Previously the imported miden-client DB came over without keystore entries, so signing broke for any non-default account.
- [FIX][all] Encrypted wallet file export now includes wallet account metadata alongside the miden-client/wallet DB dumps, so import can preserve account names and HD indices instead of falling back to generic "Miden Account N" labels.
- [FIX][all] Encrypted-file password screen consolidates the hardware-vs-password branching around a single `hasHardwareProtector` check — hardware-only vaults skip password entry entirely, password-protected vaults keep the attempt/lockout flow.
- [FIX][all] Encrypted-wallet-file flow now filters imported accounts out of the exported payload and adds a red inline notice naming the count of omitted accounts, because the file format does not carry raw private keys. Restore (`Vault.spawnFromMidenClient`) silently skips miden-client accounts with no matching `WalletAccount` entry and any entry with `hdIndex < 0`, so a stray orphan no longer either aborts the restore or overwrites an imported account's real secret with a mnemonic-derived one under `m/44'/0'/0'/-1'`. (#195)
- [FIX][all] `ACCOUNT_NAME_PATTERN` in `app/defaults.tsx` is now anchored at both ends (`/^[^\s-].{0,15}$/`); the prior `/[^\s-].{0,16}$/` was missing the start anchor, which silently accepted names of ANY length because the regex engine could match any suffix. `EditAccountName` / `CreateAccount` / Import Account now all enforce the same 16-character ceiling. **Behaviour note:** existing wallets that have accounts named with 17+ characters (accepted by the old broken regex) will still DISPLAY those names fine, but the first attempt to rename one of those accounts will fail validation until the user shortens it to 16 characters or fewer. (#195)
- [FIX][all] `importAccount` now serializes through the unlock queue and re-reads the accounts list inside the serialized section, so two quick successive imports can't both pass the name-uniqueness check against stale data and lose one of the writes. Auto-generated default names (`Account N`) also walk forward past collisions instead of throwing when the user has manually renamed an earlier account to match the template. (#195)
- [FIX][all] Hex validation on private-key import rejects odd-length input and caps the input at 4 KiB (~1.6× the serialized Falcon-512 secret key size) before allocation, so a malformed or pathologically large paste — e.g. an accidentally-pasted seed phrase or encrypted-file body — fails fast with a clean `PublicError` instead of throwing inside `AuthSecretKey.deserialize` under the WASM lock. (#195)
- [FIX][all] Encrypted-wallet-file restore now fails fast with a clear `"Encrypted file contains no restorable accounts"` error instead of crashing on an unguarded `walletAccounts[0]!.publicKey` dereference when the file carries zero HD accounts (e.g. a wallet whose only account was imported and therefore filtered out on export). (#195)
- [FIX][all] Encrypted-wallet-file import mirrors the export-side warning: after successful decryption, if the file was exported with any imported accounts stripped, the restore screen now shows a red notice naming the omitted count and requires an explicit second "Continue Import" click before completing. Count is carried in a new optional `omittedImportedAccountCount` field on the decrypted payload; older files without the field restore silently as before. (#195)
- [FIX][all] Reveal Private Key flow is now gated by an "I understand this key cannot be rotated" checkbox in addition to the warning banner — the Continue button stays disabled until the user ticks it, turning a passive notice into an active acknowledgment. (#195)
- [FIX][extension] Popup no longer white-screens after an MV3 service-worker cold-start. `WalletStoreProvider` stopped gating the app tree on a racy single `GetStateRequest`, and `useIntercomSync` replaced its fixed 15 s retry budget + one-shot latch with a cancellable unbounded retry loop (250 ms → 3 s exponential backoff). The backend's existing post-init `StateUpdated` broadcast still hydrates the store as soon as the SW is ready; the popup now self-heals from a missed broadcast or slow port setup instead of staying blank until fully reopened. (#196, closes #113)
- [FIX][extension] Service-worker init race resolved across all known `Vault.*` cold-start entry points. The Vite SW build's `sw-patches` plugin strips top-level `await` and re-sequences module init through a `__initsReady` barrier, but `init_actions` was excluded from that barrier and `SYNC_REQUEST` bypassed it — so `Actions.init()`, `runSync()`, `isDAppEnabled()`, and the `miden-sync` alarm listener could read `Vault === undefined` and throw `TypeError: Cannot read properties of undefined (reading 'isExist')`. Downstream symptoms included send/consume txs stuck in `GeneratingTransaction` indefinitely, "Create Account" silently no-op'ing, and dApp permission checks failing on cold-start. New `getVault()` lazy accessor in `src/lib/miden/back/vault.ts` awaits `init_vault()` (idempotent via a one-shot promise) before returning the class; wired through `actions.ts` (`init`, `isDAppEnabled`), `sync-manager.ts` (`runSync` × 2), and the alarm listener in `background.ts`. `SYNC_REQUEST` removed from the `__initsReady` bypass list in `vite.background.config.ts`. Misleading "doSync is safe before start completes" comment in `background.ts:33-36` removed. Also patches a Rolldown-renaming regression: source-level `init_vault()` references were getting auto-renamed to `init_vault$1` to avoid colliding with the auto-generated factory name; a small post-bundle alias (`var init_vault = init_vault$1;`) appended to the chunk keeps the lazy accessor's runtime references resolvable. Full audit of 11 `Vault.*` call sites included; non-cold-start sites (post-`withInited`/`withUnlocked`) verified safe by gate. (#214, closes #212)
- [FIX][all] Encrypted-wallet-file import (and the consume-note screen) no longer crash with `useMiden must be used within a MidenProvider`. PR #151's React-SDK migration introduced `useImportStore` and `useConsume` calls without ever mounting the SDK's `MidenProvider` — a name collision with the wallet's own `MidenProvider` wrapper masked the missing import. `src/lib/miden/front/provider.tsx` now mounts `MidenProvider` from `@miden-sdk/react/lazy` (matching the lazy entry the call sites use) inside the existing provider composition, with `MidenConfig` derived from the same `MIDEN_NETWORK_ENDPOINTS` / `MIDEN_PROVING_ENDPOINTS` / `getNoteTransportUrl` constants `MidenClientInterface.create()` already consumes. `autoSyncInterval: 0` keeps the wallet's own SW-driven sync as the source of truth. (closes #200)
- [FIX][extension] `TransactionProgressModal` no longer shows "Transaction Completed" for transactions that actually failed. The modal's extension-branch polling loop only watched `getAllUncompletedTransactions()`, never `getFailedTransactions()`, and the local `error` state was only set on the non-extension code path — so any tx that flipped to `Failed` (via `cancelTransaction`'s WASM-kernel-error catch) dropped silently out of the uncompleted list and the modal rendered the green-checkmark success branch. Brought the modal to parity with the full-page `GeneratingTransaction` route: a second 5s SWR poll on `getFailedTransactions()`, an initial-failed-count snapshot captured in a ref on first load, and `hasErrors = error || (currentFailed - initialFailed) > 0` derived from the delta. The shared `<GeneratingTransaction>` sub-component now renders the failure branch correctly. (closes #211)
- [FIX][all] Auto-consume retry storm bounded with per-noteId cap and cooldown. When a Committed input note hit a deterministic-looking kernel auth failure (`miden::protocol::auth::request`), `initiateConsumeTransaction`'s dedup logic excluded `Failed` rows from its filter — combined with a 5s `Explore.tsx` polling cadence and a wallet-tab-switch remount amplifier, this produced 100+ Failed consume rows for a single noteId in <30 minutes. New constants in `src/lib/miden/activity/transactions.ts`: `MAX_CONSECUTIVE_CONSUME_FAILURES = 5`, `RECENT_FAILURE_WINDOW_SEC = 30 min`, `RETRY_COOLDOWN_SEC = 5 min`. The rw transaction now reads ALL consume rows for the noteId and partitions: non-Failed rows take the existing dedup branch; Failed-only rows gate on (a) cap on consecutive failures inside the recent window AND (b) cooldown since most recent. Preserves the documented "Failed → retry allowed" semantics for transient kernel failures (the reporter's followup confirms `auth::request` clears once chain state advances), but bounds the rate to ~1/5min after the cap. (closes #215)
- [FIX][extension] Send / consume transactions stuck in `GeneratingTransaction` for hours after MV3 service-worker idle-eviction now self-heal. Three independent gaps closed: (1) `setupTransactionProcessor`'s startup gate switched from `hasQueuedTransactions()` (Queued-only) to `getAllUncompletedTransactions().length > 0`, so an orphan in `Generating` is visible to SW-startup recovery — `safeGenerateTransactionsLoop`'s first action `cancelStuckTransactions()` reaps it within a tick. (2) New `STUCK_TX_HEAL_ALARM` (5-min period) calls `cancelStuckTransactions()` directly, independent of `startTransactionProcessing` running, so the documented 30-min `MAX_WAIT_BEFORE_CANCEL` self-heal honours its contract regardless of UI mount state. Plus a one-shot sweep at the tail of `setupTransactionProcessor` for already-aged orphans. (3) `requestSWTransactionProcessing()` no longer silently swallows errors with `.catch(() => {})` — failures are logged. (4) `TransactionProgressModal`'s recovery effect now subscribes to intercom `StateUpdated` (broadcast at the tail of `start()`, doubles as an SW-respawn signal) and re-runs `resumeIfNeeded` on every respawn instead of mount-once. The auto-cascade danger flagged in the issue (orphan-recovery → cancel → broken-auth → retry storm) is bounded by #215's new per-noteId cap landing in the same change. (closes #216)

---

## 1.14.3 (TBD)

### Features

- [FEATURE][all] Per-stage label in the transaction progress modal. Each observable phase boundary (`syncing`, `sending`, `confirming`, `delivering`) writes a stage marker during tx processing, and the modal renders a stage-specific title + description instead of a single opaque "Generating Transaction" for the whole 3-8s spinner window. Send-type sub-label varies by tx type (claim / execute / send), and the batch subtitle surfaces a remaining-count when more than one tx is in flight.

---

## 1.14.2 (TBD)

### Features

- [FEATURE][all] Typed sign-callback failure recovery via SDK `lastAuthError()`. When the wallet gets locked mid-transaction, the transaction is left Queued for retry after unlock instead of marked Failed. (#189)
- [FEATURE][all] `ApplyTransactionAfterSubmitFailed` handling via SDK `errorCode` dispatch. Transactions that submit on-chain but fail to apply locally are marked Completed (not Failed). (#189)
- [FEATURE][e2e] Transport-failure perturbation (`STRESS_TRANSPORT_FAIL_PROB`) in the stress suite for end-to-end coverage of the SDK's durable relay outbox (miden-client#2127). (#189)

### Fixes

- [FIX][mobile] Switched all `@miden-sdk/miden-sdk` and `@miden-sdk/react` imports to the explicit `/lazy` subpath. Both SDKs' default entries (post-split) await WASM at module top level for ergonomic dApp use; Capacitor's `capacitor://localhost` scheme handler interacts poorly with that TLA and hangs the host WebView indefinitely (React tree never mounts). The `/lazy` entries omit the TLA, leaving readiness to `MidenProvider`'s existing `isReady` flag.
- [FIX][all] Gated page-side SDK WASM init. `fetchTokenMetadata` and `SendDetails` used to race the SDK's lazy wasm-bindgen load when constructing `Endpoint`/`RpcClient` directly on the page thread, hitting `Cannot read properties of undefined (reading '__wbindgen_malloc')` and blacklisting the token via `autoFetchMetadataFails` for the rest of the session. New `ensureSdkWasmReady()` helper actively triggers the SDK's `loadWasm()` via a Vite-aliased deep import and probes readiness, wired up before any page-side RPC construction. (#187)
- [FIX][all] `clearStorage` no longer tears down live Dexie handles. The spawn-time reset used to call `Repo.db.delete() + db.open()`, which fired a `versionchange` event to every other open handle (notably the page's), forced them closed, and triggered `DatabaseClosedError` on subsequent page-side reads. Now clears only the transactions table; a new `resetStorageDestructive()` preserves the full-wipe semantics for the options-page "Reset Wallet" button that actually wants it. (#187)

---

## 1.14.0 (TBD)

### Features

- [FIX][mobile] Fixed iOS release build by removing stale CocoaPods references, using correct workspace target, fixing ExportOptions team ID, and adding auto-versioning from `package.json`. (#172)
- [FEATURE][mobile] **Embedded dApp browser** for iOS and Android with multi-instance tabs, parked-dApp switcher tray, and native navbar overlay.
- [FEATURE][all] Migrated backend from `WasmWebClient` to the new `MidenClient` TypeScript API. All service-worker WASM access now goes through `MidenClientInterface` wrapping the high-level `MidenClient` surface.
- [FEATURE][all] Migrated frontend to `@miden-sdk/react` hooks (`useMiden`, `useSyncState`, `useAccount`, etc.), replacing manual sync and balance-polling logic.

### Fixes

- [FIX][all] Fixed duplicate consume-transaction entries in wallet history when receiving a single note. `initiateConsumeTransaction` now dedups against all non-`Failed` consume txs for the same note (including `Completed`), preventing auto-consume from re-enqueueing while `getConsumableNotes` is still returning the note during chain-sync lag. Also replaced the sync poll's blanket clear of `extensionClaimingNoteIds` with a surgical remove so Explore's `isBeingClaimed` gate works correctly. (#184)

---

## 1.13.3 (2026-03-19)

### Features

- [FEATURE][arch][all] Moved to service-worker-first architecture. The WASM client now lives exclusively in the Chrome extension service worker, with the frontend communicating via intercom messaging. Eliminates duplicate WASM instances and fixes concurrency panics.
- [FEATURE][all] Complete UI revamp with new design system, updated layouts, and refreshed components across all screens.

---

## 1.13.2 (2026-03-16)

### Features

- [FEATURE][extension] Chrome Side Panel mode with popup toggle. Users can switch between popup (default) and side panel via the maximize/minimize icon in the header. Preference persists across sessions. (#176)
- [FEATURE][extension] Pin extension prompt shown once after fresh install, guiding users to pin the extension to the toolbar. (#176)
- [FEATURE][all] Color-coded Send (blue) and Receive (green) action buttons on the home page, matching the token detail page. (#176)

### Fixes

- [FIX][extension] Fixed `onInstalled` event handler not firing on Chrome MV3 due to webpack async module loading delaying listener registration. Handler moved to `sw.js` for synchronous registration. (#176)
- [FIX][all] Fixed `ConsumingNote` page using raw UA sniffing instead of `isMobile()` platform detection. (#176)
- [FIX][all] Fixed transaction recovery after network outages. Private accounts could enter a permanently broken state where all transactions fail with "initial state commitment does not match". Root causes: AutoSync loop died on the generating-transaction page, transactions were built against stale local state, and the transaction modal blocked on stale tx failures. Now syncs state before executing transactions, keeps AutoSync alive during transaction generation, cancels crashed/stale transactions properly, and shows correct "Failed" status instead of misleading "Executing". (#150)
- [FIX][all] Removed stale "Download Generated Files" button and output notes storage. The `useExportNotes` hook, `registerOutputNote`, and related storage key were unused dead code. Simplifies the transaction completion screen and its auto-close logic. (#160)
- [FIX][all] Removed the "Upload File" button and drag-and-drop note import from the Receive page. The freed space is now used by the notes list, making it taller. (#161)
- [FEATURE][all] Complete UI revamp across the wallet.

---

## 1.13.1 (2026-02-16)

### Features

- [FEATURE][all] Token metadata now fetched via `RpcClient` instead of IndexedDB lookups, improving reliability and reducing stale metadata issues. (#127)

### Fixes

- [FIX][extension] dApp-initiated transactions (e.g. from wallet adapter) now process in the background instead of requiring the user to keep the popup open. Previously, closing the extension popup during a dApp transaction could cause it to fail silently. (#130)
- [FIX][all] Fixed Note Transport Layer (NTL) connection failures caused by incorrect default port configuration. Updated faucet address to use the new testnet faucet. (#125)

---

## 1.13.0 (2026-02-12)

### Breaking Changes

- [BREAKING][rename][all] Miden SDK package renamed from `@demox-labs/miden-sdk` to `@miden-sdk/miden-sdk` and upgraded to v0.13.0. All imports and wallet adapter references updated accordingly. (#101)

### Features

- [FEATURE][mobile] **Mobile app for iOS and Android** via Capacitor. Includes FaceID/TouchID biometric authentication, in-app dApp browser with wallet adapter injection, QR code scanning and display, native local notifications for incoming notes, haptic feedback throughout the UI, hardware back button and swipe-back gesture support, and native file sharing for exports. (#81)
- [FEATURE][desktop] **Desktop application using Tauri** for macOS and Windows. Features native window controls, Touch ID unlock on macOS, and secure storage via Tauri's stronghold plugin. (#86)
- [FEATURE][desktop] **dApp browser for desktop** with dedicated browser window that injects `window.midenWallet` API into web pages. dApps can request wallet connections and transaction approvals via an in-window confirmation overlay. (#89)
- [FEATURE][mobile,desktop] **Hardware-first vault key security.** On devices with Secure Enclave, TPM, or TEE, the wallet now uses hardware-only protection with no password required. Eliminates password brute-force attack surface on mobile and desktop. Browser extension continues to use password-based protection. (#88)
- [FEATURE][all] **Note Transport Layer (NTL) support.** Enables private note delivery between wallets using encrypted peer-to-peer transport, allowing users to send and receive notes without exposing transaction details on-chain. (#45)
- [FEATURE][all] **Runtime language switching** with Spanish and Polish support. Language can be changed instantly in Settings without page reload. Unified i18n system using i18next exclusively, replacing the legacy custom `T`/`t` components. Updated branding from Demox Labs to Miden across the app. (#74)
- [FEATURE][all] **Transaction completion tracking.** Added `waitForTransactionCompletion` using Dexie's `liveQuery` so the UI can reliably wait for transactions to finalize rather than just queuing them. (#50)
- [FEATURE][all] **Improved seed phrase verification** during onboarding. Users now verify the first and last words of their seed phrase (similar to Coinbase Wallet) instead of always the 10th word, which caused confusion when users hadn't scrolled far enough during backup. (#51)
- [FEATURE][all] **Receive page overhaul with "Claim All" button.** Users can claim all pending notes in a single action. Claiming state persists across popup reopens, and new notes arriving during a claim show the button again. Includes error handling with retry support. (#55)
- [FEATURE][all] **Improved balance sync UX.** Balances default to 0 immediately on page load instead of showing a skeleton loader. A shimmer animation indicates sync progress on each token row, disappearing after the first chain sync completes. (#65)
- [FEATURE][all] **Subtle header spinner** replaces the wave loading animation for balance syncing, providing a less intrusive loading indication. (#100)
- [FEATURE][all] **i18n enforcement in CI.** All user-facing strings are now required to use translation keys, enforced by a linting rule. Prevents hardcoded English strings from being introduced. (#71)

### Fixes

- [FIX][all] **WASM client concurrency fix.** Removed all `Promise.all` usage with the WASM client to prevent "recursive use of an object" panics when multiple operations (e.g. fetching metadata for 2+ new tokens simultaneously) tried to access the client concurrently. (#53)
- [FIX][all] Fixed non-MIDEN tokens appearing delayed after sync. Previously, `fetchBalances()` released the WASM lock between `getAccount()` and metadata fetches, allowing AutoSync to grab the lock for 30+ seconds. All WASM operations now happen in a single lock acquisition. (#98)
- [FIX][all] Wallet now syncs state to chain tip before creating the first account during onboarding, resulting in faster initial setup. (#99)
- [FIX][all] Fixed autosync lock interfering with balance display. Balances are now fetched immediately when wallet becomes Ready (moved from React `useEffect` to `syncFromBackend` in Zustand store), eliminating a ~200ms delay. (#84)
- [FIX][all] Fixed generating transaction page showing success icon and "transaction complete" text even when the transaction failed. Error state now displays correctly with appropriate messaging. (#48)
- [FIX][all] Fixed custom transactions (e.g. from dApp swaps) not appearing on the activity/history page. The issue was that custom transactions don't have the note tag attached to the address. (#70)
- [FIX][all] Fixed undefined note metadata causing errors when handling private notes with request metadata. (#73)
- [FIX][extension] Fixed onboarding flow reopening in a new tab when the popup was clicked while onboarding was already in progress. The extension now redirects to the existing onboarding tab instead. (#52)
- [FIX][extension] Replaced the tab-opening pattern with an in-popup modal for transaction progress, unifying the UX across mobile, desktop, and extension. (#87)
- [FIX][extension] Disabled all CSS transitions and animations in the Chrome extension to prevent visual glitches. Fixed `isExtension()` detection to check both `browser.runtime.id` and `chrome.runtime.id`. (#83)
- [FIX][extension] Fixed receive page content overflowing the popup viewport. (#47)
- [FIX][extension] Fixed settings page bottom toolbar being cut off. (#75)
- [FIX][all] Fixed form fields (send flow, encrypted file export, etc.) broken after the react-hook-form v7 migration. Updated all FormField components to use the new register/validation API. (#66)
- [FIX][all] Fixed stability issues on the Consuming Note page that could cause errors during note consumption.
- [FIX][desktop] Fixed Windows desktop app icon displaying incorrectly. The `icon.ico` file was a PNG renamed to `.ico`; converted to proper ICO format required by the Windows Resource Compiler. (#93)
- [FIX][all] Resolved 63 dependency security vulnerabilities (66 down to 3). Removed `react-dev-utils` (3 critical), `node-forge` (4 high), replaced `analytics-node` with `@segment/analytics-node`, and updated `@svgr/webpack`, `nanoid`, and `translate` to patched versions. (#96)
